---
phase: quick-260922-mn7
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/assets/logo-mark.svg
  - src/assets/logo-icon.svg
  - src-tauri/icons/32x32.png
  - src-tauri/icons/128x128.png
  - src-tauri/icons/128x128@2x.png
  - src-tauri/icons/icon.ico
  - src-tauri/icons/icon.icns
  - src-tauri/icons/icon.png
  - src-tauri/icons/app-icon.png
  - src-tauri/icons/Square30x30Logo.png
  - src-tauri/icons/Square44x44Logo.png
  - src-tauri/icons/Square71x71Logo.png
  - src-tauri/icons/Square89x89Logo.png
  - src-tauri/icons/Square107x107Logo.png
  - src-tauri/icons/Square142x142Logo.png
  - src-tauri/icons/Square150x150Logo.png
  - src-tauri/icons/Square284x284Logo.png
  - src-tauri/icons/Square310x310Logo.png
  - src-tauri/icons/StoreLogo.png
  - src/components/Logo.tsx
  - src/components/Logo.test.tsx
  - src/App.tsx
  - docs/BRAND.md
autonomous: true
requirements: []

must_haves:
  truths:
    - "Mở app lên thấy logo git-plum (quả mận + nhánh git) trong thanh công cụ, cạnh chữ 'git-plum'"
    - "Icon ứng dụng trên taskbar/file explorer là quả mận + nhánh git, không còn icon Tauri mặc định"
    - "Người đóng góp mới đọc docs/BRAND.md hiểu được khái niệm logo và cách dùng đúng/sai"
  artifacts:
    - path: "src/assets/logo-mark.svg"
      provides: "SVG logo gốc, nền trong suốt, tay vẽ (path/circle đọc được)"
      contains: "circle"
    - path: "src/assets/logo-icon.svg"
      provides: "SVG logo có nền tile đặc — input cho `tauri icon`"
      contains: "rect"
    - path: "src-tauri/icons/icon.png"
      provides: "Icon ứng dụng đã sinh lại từ logo-icon.svg (không còn 1.8K mặc định Tauri)"
    - path: "src/components/Logo.tsx"
      provides: "Component <Logo/> hiển thị logo-mark.svg inline trong app"
      exports: ["Logo"]
    - path: "docs/BRAND.md"
      provides: "Tài liệu nhận diện thương hiệu — khái niệm, bảng màu, quy tắc dùng"
  key_links:
    - from: "src/App.tsx"
      to: "src/components/Logo.tsx"
      via: "import và render trong .toolbar-left"
      pattern: "<Logo"
    - from: "src-tauri/tauri.conf.json"
      to: "src-tauri/icons/*.png"
      via: "bundle.icon list"
      pattern: "icons/32x32.png"
---

<objective>
Thiết kế và triển khai nhận diện thương hiệu cho git-plum: một logo SVG tay vẽ theo
khái niệm "quả mận + nhánh git" (D-01), bộ icon ứng dụng sinh từ logo đó qua
`tauri icon`, component `<Logo/>` hiển thị trong thanh công cụ của app, và tài liệu
`docs/BRAND.md` ghi lại quyết định thiết kế.

Purpose: git-plum hiện chưa có bất kỳ nhận diện hình ảnh nào — icon ứng dụng vẫn là
bộ mặc định của `create-tauri-app` (1.8K PNG rỗng), và thanh công cụ chỉ có chữ
`git-plum` không kèm biểu tượng. Đây là lần đầu dự án có một dấu hiệu thị giác riêng.

Output:
- `src/assets/logo-mark.svg` — logo gốc, nền trong suốt, dùng trong app
- `src/assets/logo-icon.svg` — logo có nền tile đặc, dùng làm input sinh icon OS
- Toàn bộ `src-tauri/icons/*` được sinh lại từ `logo-icon.svg`
- `src/components/Logo.tsx` — component React hiển thị logo, đã gắn vào `App.tsx`
- `docs/BRAND.md` — tài liệu thương hiệu
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@src/styles/app.css
@src/components/icons.tsx
@src/App.tsx
@src-tauri/tauri.conf.json

<design_decisions>
**Khái niệm (D-01, khoá cứng):** Thân quả mận tròn; vùng cuống/lá vươn lên thành
một đường nhánh git rẽ nhánh, mang 2 nút commit. Phải đọc được là VỪA quả mận VỪA
đồ thị commit.

**Tên hiển thị (D-02, khoá cứng):** `git-plum` (chữ thường, có gạch nối) — giữ
nguyên ở mọi nơi: tiêu đề cửa sổ, `package.json`, `productName`, tài liệu thương
hiệu. KHÔNG đổi thành "GitPlum" hay "Plum".

**Bảng màu:** Tái dùng token đã có trong `src/styles/app.css` `:root`, KHÔNG bịa màu
mới. Thân quả mận dùng `--accent` (#9d7cd8, tím) — đây là màu định danh chính của
`.brand` trong thanh công cụ hiện tại. Đường nhánh git + 2 nút commit dùng `--success`
(#86b384, xanh lục) để tương phản rõ với thân mận tím và gợi liên tưởng "cành lá".
Cả hai theme (tối/sáng) đã có biến thể của hai token này trong `@media
(prefers-color-scheme: light)`.

**Hai bản SVG khác nhau — vì sao cần cả hai (ràng buộc bắt buộc của kế hoạch):**
- `logo-mark.svg`: nền TRONG SUỐT. Dùng cho `<Logo/>` trong app — nó ngồi trên
  `--bg` (nền app), một nền tile riêng sẽ tạo khung hình thừa không khớp UI.
- `logo-icon.svg`: có một `rect` nền TILE ĐẶC (bo góc, giống icon desktop app
  thông thường — VS Code, Slack — không phải một mark trần trên desktop). Dùng làm
  input duy nhất cho `npx tauri icon`, vì icon OS cần trọng lượng thị giác khi đặt
  cạnh các icon app khác có nền đặc trên desktop/taskbar.
- Cả hai file dùng CHUNG các `<path>`/`<circle>` vẽ quả mận + nhánh git — chỉ khác
  nhau ở có/không có `rect` nền. Không trùng lặp toàn bộ hình học nếu tránh được:
  `logo-icon.svg` có thể tham chiếu cùng toạ độ đã dùng ở `logo-mark.svg` (chép lại
  markup, không phải `<use>` — SVG dùng làm input rasterize của `tauri icon` không
  đáng tin cậy chạy `<use xlink:href>` liên tệp; giữ mỗi tệp tự chứa).

**Quy tắc theming của `<Logo/>` (đọc `src/components/icons.tsx` header trước khi
quyết) — quyết định CÓ CHỦ Ý, khác `icons.tsx`:**
`icons.tsx` bắt hai quy tắc cho ICON GIAO DIỆN (nút bấm, trạng thái): luôn
`stroke="currentColor"` + `aria-hidden`. Logo là DẤU HIỆU THƯƠNG HIỆU, không phải
icon giao diện — quy tắc 1 KHÔNG áp dụng nguyên văn: nếu tô toàn bộ logo bằng
`currentColor`, quả mận sẽ mất màu tím `--accent` đặc trưng (nó sẽ đổi theo màu chữ
`.toolbar` bất kỳ lúc nào, xoá luôn bản sắc thương hiệu — đây chính là điều D-01/2
yêu cầu KHÔNG được mất). Thay vào đó, `<Logo/>` dùng `var(--accent)` cho thân mận và
`var(--success)` cho nhánh git NGAY TRONG SVG (không phải `currentColor`) — hai biến
CSS này đã tự đổi theo `prefers-color-scheme`, nên logo vẫn theo đúng theme mà không
cần `currentColor`. Quy tắc 2 (`aria-hidden` trên phần tử SVG) VẪN áp dụng nguyên
văn: logo là trang trí cạnh chữ `git-plum` đã có sẵn trong `.toolbar-left`, chữ đó
đã là nguồn văn bản cho trình đọc màn hình — thêm `aria-label` trùng lặp trên SVG sẽ
đọc hai lần.

**Vị trí trong App.tsx:** `.toolbar-left` hiện có `<span className="brand">git-plum</span>`
(dòng ~267). `<Logo/>` chèn NGAY TRƯỚC span đó, cùng hàng flex (`.toolbar-left` đã
là `display: flex; gap: 10px`) — không cần CSS mới cho layout hàng ngang. Đây là
SỬA ĐỔI CỘNG THÊM DUY NHẤT vào `App.tsx`: chỉ thêm một dòng import + một phần tử
JSX, không đổi bất kỳ logic, state, hay JSX nào khác đang có trong cây làm việc.

**🔴 Rủi ro va chạm cây làm việc:** `src/App.tsx` và `src/styles/app.css` đang có
thay đổi CHƯA COMMIT từ phiên làm việc khác (xem git status — việc Phase 2:
`handleSelectRef`, cuộn đồ thị khi bấm ref). Task 3 KHÔNG được hoàn tác hay viết đè
các đoạn đó. Thêm `<Logo/>` là một chèn JSX cộng thêm ở đúng một vị trí
(`.toolbar-left`, ngay trước `<span className="brand">`) — không chạm phần còn lại
của file. Nếu executor thấy nội dung `App.tsx` khác với đoạn trích trong plan này
(do phiên khác đã commit trước), tìm lại `.toolbar-left` bằng grep, không thay thế
nguyên khối.
</design_decisions>

<interfaces>
Từ `src/components/icons.tsx` (quy ước icon hiện có, KHÔNG áp dụng nguyên văn cho
Logo — xem `<design_decisions>`):
```tsx
interface IconProps {
  size?: number
  className?: string
}
```
`<Logo/>` dùng cùng hình dạng props (`size`, `className`) để nhất quán với các icon
khác trong `.toolbar-left`, nhưng KHÔNG dùng `svgProps()` của `icons.tsx` (hàm đó ép
`stroke="currentColor"`, sai với quyết định giữ màu thương hiệu cố định).

Từ `src/App.tsx` (vị trí chèn, KHÔNG đổi gì khác trong file):
```tsx
<div className="toolbar-left">
  <span className="brand">git-plum</span>
  {activeRepo && (...)}
</div>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Vẽ hai bản SVG logo (nền trong suốt + nền tile đặc)</name>
  <files>src/assets/logo-mark.svg, src/assets/logo-icon.svg</files>
  <action>
    Tạo thư mục `src/assets/` (chưa tồn tại). Tay vẽ `logo-mark.svg`: `viewBox="0 0
    64 64"` (lưới lớn hơn 16px của `icons.tsx` vì đây là logo, không phải icon UI
    nhỏ — cần đủ chi tiết cho cả kích thước lớn trên trang giới thiệu lẫn nhỏ 28px
    trong toolbar). Thân quả mận: một `<circle>` hoặc `<path>` hình bầu dục hơi lệch
    (quả mận thật không tròn hoàn hảo), fill `var(--accent)`. Từ đỉnh quả mận, một
    `<path>` nét (stroke, không fill) đi lên rồi rẽ nhánh làm hai — đây là đường git
    branch — dùng `var(--success)` cho stroke, `stroke-width` khoảng 3-4px (tỷ lệ
    với viewBox 64). Trên hai nhánh đặt 2 `<circle>` nhỏ đặc màu `var(--success)`
    làm nút commit (đường kính khoảng 6-8px). Toàn bộ phần tử con của `<svg>` gốc
    phải là markup đọc được — path/circle/rect trực tiếp, KHÔNG base64, KHÔNG
    `<image>`, KHÔNG một `<path d="...">` khổng lồ kiểu vết trace ảnh bitmap (path
    data phải ngắn, đếm được số lệnh vẽ bằng mắt). SVG gốc (`<svg>` tag) có
    `xmlns="http://www.w3.org/2000/svg"` và `viewBox="0 0 64 64"`, KHÔNG khai `fill`
    hay `stroke` cố định ở tag gốc (để mỗi phần tử con tự quyết màu qua biến CSS).

    Sau đó tạo `logo-icon.svg`: sao chép cùng phần tử vẽ quả mận + nhánh + nút
    commit từ `logo-mark.svg`, nhưng thêm một `<rect>` nền TRƯỚC các phần tử đó
    (dưới cùng theo thứ tự vẽ), phủ toàn bộ `viewBox`, bo góc (`rx` khoảng 12-14 trên
    viewBox 64, tỷ lệ giống icon app macOS/Windows hiện đại), fill bằng một mã màu
    HEX CỐ ĐỊNH lấy từ `--bg` tối (`#16161a`) — icon OS không đi qua CSS runtime của
    app nên KHÔNG dùng `var(--accent)`/`var(--bg)` ở tệp này, phải là giá trị hex
    tĩnh. Thân mận và nhánh trong `logo-icon.svg` cũng dùng hex tĩnh tương ứng
    (`#9d7cd8` cho thân, `#86b384` cho nhánh) vì lý do tương tự — `tauri icon`
    rasterize tĩnh, không có ngữ cảnh theme.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); for (const f of ['src/assets/logo-mark.svg','src/assets/logo-icon.svg']) { const s=fs.readFileSync(f,'utf8'); if(!s.includes('<svg')) throw new Error(f+' thieu the svg'); if(s.includes('base64')) throw new Error(f+' co base64 - cam'); if(!/circle|path|rect/.test(s)) throw new Error(f+' thieu hinh hoc'); } console.log('OK')"</automated>
  </verify>
  <done>
    `src/assets/logo-mark.svg` tồn tại, nền trong suốt, dùng `var(--accent)`/`var(--success)`.
    `src/assets/logo-icon.svg` tồn tại, có `<rect>` nền tile hex tĩnh, cùng hình học
    quả mận + nhánh git với màu hex tĩnh. Không file nào chứa base64 hay `<image>`.
  </done>
</task>

<task type="auto">
  <name>Task 2: Sinh bộ icon ứng dụng từ logo-icon.svg qua tauri icon</name>
  <files>src-tauri/icons/*.png, src-tauri/icons/*.ico, src-tauri/icons/*.icns</files>
  <action>
    Chạy `npx tauri icon src/assets/logo-icon.svg` từ thư mục gốc dự án — CLI tự
    tìm `tauri.conf.json` và ghi ra `src-tauri/icons/` (thư mục mặc định, đã xác
    nhận qua `--help`). Lệnh này ghi đè TOÀN BỘ icon hiện có (bộ mặc định
    `create-tauri-app`, `app-icon.png` hiện chỉ 1.8K). Sau khi chạy, kiểm `icon.png`
    và `32x32.png` đã đổi kích thước byte (không còn khớp số byte cũ trong git
    status: `128x128.png` 299B, `32x32.png` 104B — hai con số này PHẢI đổi vì icon
    mặc định của Tauri là hình khác). KHÔNG sửa tay `tauri.conf.json` mục
    `bundle.icon` — danh sách bốn đường dẫn (`icons/32x32.png`, `icons/128x128.png`,
    `icons/icon.ico`, `icons/icon.icns`) đã đúng tên tệp mà `tauri icon` sinh ra,
    không cần đổi.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const s=fs.statSync('src-tauri/icons/icon.png').size; if (s <= 1800) throw new Error('icon.png van la ban mac dinh (' + s + ' bytes)'); console.log('icon.png moi: ' + s + ' bytes')"</automated>
  </verify>
  <done>
    `src-tauri/icons/` chứa icon mới sinh từ `logo-icon.svg` — `icon.png` lớn hơn
    1800 byte (khác bản mặc định 1.8K), mọi kích thước PNG/ICO/ICNS trong danh sách
    `bundle.icon` của `tauri.conf.json` được ghi đè bởi cùng một lệnh `tauri icon`.
  </done>
</task>

<task type="auto">
  <name>Task 3: Component &lt;Logo/&gt;, gắn vào App.tsx, viết docs/BRAND.md</name>
  <files>src/components/Logo.tsx, src/components/Logo.test.tsx, src/App.tsx, docs/BRAND.md</files>
  <action>
    Tạo `src/components/Logo.tsx`: component React đọc props `{ size?: number,
    className?: string }` (mặc định `size = 20` — lớn hơn icon UI 16px một chút vì
    đây là logo, nhỏ hơn nút `.icon-button` 28px để có khoảng đệm khi đặt cạnh chữ
    `.brand`). Render trực tiếp markup SVG của `logo-mark.svg` INLINE trong JSX
    (không `<img src="...">`, để `var(--accent)`/`var(--success)` bên trong resolve
    đúng theo `document` hiện tại — import ảnh qua `<img>` sẽ nạp SVG như tài nguyên
    tĩnh độc lập, không thừa hưởng biến CSS của trang cha nếu SVG dùng `<img>`, còn
    inline thì có). Copy chính xác các phần tử hình học từ `logo-mark.svg` (Task 1)
    vào JSX của component (chuyển `class`/`stroke-width` sang `className`/
    `strokeWidth` theo cú pháp JSX), viewBox giữ `0 0 64 64`. Thêm `aria-hidden="true"`
    và `focusable="false"` trên thẻ `<svg>` gốc — theo quy tắc 2 của `icons.tsx`,
    logo là trang trí cạnh chữ `git-plum` đã có sẵn trong `.toolbar-left`. Viết doc
    comment đầu file giải thích NGẮN GỌN vì sao component này KHÔNG dùng
    `svgProps()`/`currentColor` của `icons.tsx` (tham chiếu quyết định trong
    `docs/BRAND.md`, không lặp lại toàn bộ lý do ở đây).

    Viết `src/components/Logo.test.tsx`: test render `<Logo/>` xác nhận có phần tử
    `svg` với `aria-hidden="true"`, và test rằng markup KHÔNG chứa `stroke="currentColor"`
    hay `fill="currentColor"` ở cấp phần tử vẽ chính (khẳng định quyết định giữ màu
    thương hiệu cố định qua biến CSS, chống hồi quy nếu ai đó sau này "sửa lại cho
    giống icons.tsx").

    Sửa `src/App.tsx`: thêm `import { Logo } from '@/components/Logo'` vào khối
    import hiện có (cạnh các import từ `@/components/icons`), và chèn `<Logo
    size={20} />` làm phần tử ĐẦU TIÊN bên trong `<div className="toolbar-left">`,
    ngay trước `<span className="brand">git-plum</span>` đang có — đây là sửa đổi
    JSX cộng thêm DUY NHẤT, không đổi bất kỳ dòng nào khác trong file (xem cảnh báo
    va chạm cây làm việc ở `<design_decisions>`).

    Viết `docs/BRAND.md` theo cấu trúc: (1) Khái niệm — mô tả quả mận + nhánh git,
    lý do (D-01); (2) Tên hiển thị — `git-plum`, chữ thường, không viết hoa/đổi tên
    (D-02), liệt kê các nơi tên xuất hiện (tiêu đề cửa sổ, `package.json`,
    `tauri.conf.json` productName); (3) Bảng màu — bảng liệt kê `--accent` (thân
    mận) và `--success` (nhánh + nút commit) VỚI giá trị hex thực từ `app.css` cho
    cả hai theme (tối/sáng), ghi rõ "tái dùng token hiện có trong `src/styles/
    app.css`, không phải bảng màu riêng"; (4) Hai biến thể tệp — giải thích
    `logo-mark.svg` (nền trong suốt, dùng trong app) vs `logo-icon.svg` (nền tile
    đặc, dùng sinh icon OS qua `npx tauri icon`), và lệnh để tái sinh icon khi logo
    đổi; (5) Quy tắc dùng — ĐÚNG (dùng `<Logo/>` cho mọi chỗ cần logo trong app,
    không tự ý đổi màu thân mận/nhánh) và SAI (không dùng `<img>` để nhúng logo
    trong app vì mất theming, không thêm `aria-label` trùng với chữ `.brand`, không
    tạo bản logo màu khác cho từng theme thủ công — biến CSS đã tự lo).
  </action>
  <verify>
    <automated>npx vitest run src/components/Logo.test.tsx</automated>
  </verify>
  <done>
    `src/components/Logo.tsx` tồn tại, export `Logo`, render SVG inline với
    `aria-hidden`, màu thân/nhánh qua `var(--accent)`/`var(--success)` không phải
    `currentColor`. `src/components/Logo.test.tsx` xanh. `App.tsx` hiện `<Logo/>`
    ngay trước `<span className="brand">` trong `.toolbar-left`, không đổi gì khác
    trong file. `docs/BRAND.md` tồn tại với đủ 5 mục nêu trên, bảng màu trích đúng
    giá trị hex từ `app.css`, tên hiển thị ghi rõ `git-plum` (không viết hoa).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | Phạm vi quick task này thuần tuý là tài sản thị giác tĩnh (SVG, PNG, tài liệu) và một component hiển thị không nhận input từ người dùng hay mạng. Không có input động, không có dữ liệu tin cậy thấp đi qua bất kỳ tệp nào trong plan này. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-quick-260922mn7-01 | Tampering | src/components/Logo.tsx | accept | Component tĩnh, không props động ảnh hưởng render ngoài `size`/`className` (số và chuỗi class do chính codebase truyền, không phải input người dùng cuối). Không có bề mặt tấn công thực tế. |
</threat_model>

<verification>
1. `npm run typecheck` xanh (nếu có script này trong package.json — nếu không, `npx tsc --noEmit`).
2. `npx vitest run src/components/Logo.test.tsx` xanh.
3. `npx vitest run` toàn bộ (không có test nào khác bị vỡ do thêm import `Logo` vào `App.tsx`).
4. Mở `src-tauri/icons/icon.png` bằng mắt — không còn là icon Tauri mặc định.
5. `npm run tauri dev` (hoặc build) — xác nhận bằng mắt: logo hiện trong thanh công cụ cạnh chữ `git-plum`, không vỡ layout `.toolbar-left`.
</verification>

<success_criteria>
- `src/assets/logo-mark.svg` và `src/assets/logo-icon.svg` tồn tại, tay vẽ, không base64.
- `src-tauri/icons/*` đã được sinh lại từ `logo-icon.svg`, không còn bộ mặc định Tauri.
- `src/components/Logo.tsx` tồn tại, render trong `App.tsx`, có test.
- `docs/BRAND.md` tồn tại, đủ 5 mục, bảng màu khớp `app.css`.
- Không có thay đổi ngoài ý muốn trong các đoạn `App.tsx`/`app.css` đang dở của phiên khác.
</success_criteria>

<output>
After completion, create `.planning/quick/260922-mn7-thiet-ke-nhan-dien-thuong-hieu-git-plum-/260922-mn7-SUMMARY.md`
</output>
