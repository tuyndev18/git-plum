---
quick: 260923-na9
type: execute
autonomous: true
files_modified: [README.md]
must_haves:
  truths:
    - "Người mới clone repo đọc README hiểu git-plum là gì và Core Value là gì"
    - "README chỉ liệt kê tính năng đã CÓ MÃ CHẠY ĐƯỢC, không hứa hẹn tính năng chưa làm (AI, remote, bộ cài/updater)"
    - "Người mới clone repo làm theo README chạy được `npm run tauri:dev`, `npm test`, `cargo test` thành công trên máy Windows đã cài đủ prereqs"
    - "README nêu đúng giấy phép MIT dù chưa có tệp LICENSE trong repo"
  artifacts:
    - path: "README.md"
      provides: "Giới thiệu dự án, Core Value, tính năng hiện có, tech stack, yêu cầu môi trường, hướng dẫn dev/build/test, cấu trúc thư mục, tài liệu tham khảo, giấy phép"
      min_lines: 80
  key_links:
    - from: "README.md"
      to: "package.json scripts"
      via: "mọi lệnh npm nêu trong README phải khớp với scripts trong package.json"
      pattern: "npm run (dev|build|typecheck|test|tauri:dev|tauri:build|fixtures|check:chunks)"
    - from: "README.md"
      to: ".planning/ROADMAP.md + STATE.md"
      via: "danh sách tính năng 'đã có' chỉ gồm mục có [x] hoặc mã đã chạy theo STATE.md, không suy diễn thêm"
      pattern: "Phase 1|Phase 2|Phase 3"
---

<objective>
Viết `README.md` (tiếng Việt) ở gốc repo git-plum — chưa có README nào trong repo. Đây là
tài liệu đầu tiên người lạ đọc khi clone dự án (REL-03 ở Phase 8, hiện chưa làm, nhưng
README là tài sản độc lập hữu ích ngay từ bây giờ).

Purpose: Cho người đọc (bao gồm cả chủ dự án sau này, và cộng tác viên tiềm năng) một bức
tranh trung thực: dự án làm gì, đã làm được gì THẬT (không phải kế hoạch), cần gì để chạy
được trên máy họ, và cách tự build/test.

Output: `README.md` ở gốc repo, không sửa file nào khác.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@package.json
@dev.cmd
@rust-toolchain.toml
@src-tauri/Cargo.toml
@.github/workflows/ci.yml

<facts_verified>
Đã đọc trực tiếp các nguồn sau để tránh bịa số liệu — executor KHÔNG cần đọc lại các file
này trừ khi cần đối chiếu thêm, mọi số liệu cần thiết đã trích ở đây:

**Tiến độ thật (từ ROADMAP.md bảng Progress + STATE.md, không suy diễn):**
- Phase 1 (Nền tảng và lớp bọc git): 3/4 plan, "In Progress". Đã có: lớp bọc git ghim biến
  môi trường, sổ đăng ký lệnh, khoá theo repo_id, danh sách repo gần đây qua
  tauri-plugin-store, hàng đợi ghi theo repo. QA thủ công release Windows (01-04) CHƯA chạy.
- Phase 2 (Lịch sử và đồ thị nhánh): STATE.md nói rõ 7/7 plan đã có mã, nhưng ROADMAP chỉ
  đánh dấu xong 5 (02-06 mở `[ ]`, 02-07 mở `[~]` một phần) — **PHẢI dùng câu chữ thận trọng
  "đang phát triển", không nói "hoàn tất"**. Đã có mã: parser `git log` theo byte, thuật toán
  gán lane (TDD + snapshot insta + benchmark criterion), đồ thị canvas nhiều lane, danh sách
  commit ảo hoá (`@tanstack/react-virtual`), cache theo RepoId. Checkpoint hiệu năng 100k
  commit CHƯA chạy bằng mắt người — HIST-05 (mốc dưới 1 giây) còn ở trạng thái "chưa đo bằng
  mắt", chỉ có số đo máy sơ bộ (KHÔNG đưa số cụ thể vào README để tránh cam kết sai).
- Phase 3 (Xem khác biệt): 4/5 plan có mã (03-01 chưa chạy, 03-02..03-05 có mã). Đã có mã:
  trình xem diff CodeMirror 6 (tô màu cú pháp nạp lười, hai chế độ hợp nhất/hai cột, nhảy
  khối, bật tắt khoảng trắng), word-level diff, lịch sử của một tệp (`git log --follow`),
  nhận diện tệp nhị phân/quá lớn/con trỏ LFS. **CHƯA qua exit gate dogfood** — README phải
  nói "đang phát triển", không nói "đã hoàn tất Phase 3".
- Phase 4-8: "Not started", 0 plan. KHÔNG được liệt kê là tính năng "có" — chỉ nêu trong mục
  lộ trình/roadmap tương lai: vòng lặp commit, staging theo khối, nhánh/remote/xung đột, AI,
  phát hành.
- **Kết luận cho README:** README chỉ có 3 nhóm tính năng "đang có / đang phát triển":
  (1) Nền tảng — mở repo, bố cục ba vùng, nhật ký lệnh git, danh sách repo gần đây;
  (2) Đọc lịch sử — đồ thị nhánh nhiều lane, danh sách commit ảo hoá, chi tiết commit,
      tìm kiếm, nhãn nhánh/tag;
  (3) Xem khác biệt — trình xem diff CodeMirror 6, hai chế độ hiển thị, lịch sử một tệp.
  Tất cả ba nhóm phải ghi rõ **"đang phát triển, chưa qua kiểm chứng cuối phase"** vì theo
  STATE.md, 0 requirement nào của Phase 3 có bằng chứng từ mắt người và Phase 2 còn 2
  checkpoint nợ. KHÔNG dùng chữ "hoàn tất", "đã xong", "production-ready" cho bất kỳ mục nào.
  AI, remote/push/pull/merge, staging theo khối, bộ cài đặt/updater PHẢI xuất hiện dưới mục
  "Lộ trình / Chưa có" — không dưới mục tính năng hiện có.

**Tech stack thật (từ package.json + src-tauri/Cargo.toml, số version chính xác):**
- Frontend: Tauri v2 (`@tauri-apps/api` 2.11.1, `@tauri-apps/cli` 2.11.5) + React 19.3.0 +
  TypeScript 6.0.3 + Vite 8.3.0. State: Zustand 5.0.15. Virtualization:
  `@tanstack/react-virtual` 3.14.13. Diff viewer: CodeMirror 6 (`@codemirror/merge` 6.12.2 +
  các package `@codemirror/*` khác). Layout: `react-resizable-panels` 4.13.1.
  Plugin Tauri đã dùng: `tauri-plugin-dialog` 2.7.3, `tauri-plugin-store` 2.4.5.
- Backend Rust (`src-tauri/Cargo.toml`): gọi `git` CLI qua `tokio::process` (không dùng
  libgit2/gitoxide). Crate chính: `serde`/`serde_json`, `thiserror` 2.0.20, `bstr` 1.13.1,
  `memchr` 2.8.3, `lru` 0.18.4, `parking_lot` 0.12.5, `tracing` 0.1.44, `md-5` 0.10.6,
  `notify` 8.2.0 + `notify-debouncer-full` 0.6.0 (file watcher, dùng từ Phase 4).
  Rust edition 2021, `rust-version = "1.82"`.
- Test/bench Rust: `tempfile` 3.27.0, `insta` 1.48.0 (snapshot), `criterion` 0.8.2 (benchmark).
- Không có LICENSE file trong repo hiện tại, nhưng `package.json` khai `"license": "MIT"` và
  PROJECT.md nói rõ "Giấy phép mã nguồn mở (dự kiến MIT)". README phải nêu MIT là giấy phép
  dự định, và ghi chú tệp LICENSE chính thức sẽ được thêm ở Phase 8 (REL-03).

**Lệnh xác nhận tồn tại (đối chiếu package.json thật, dev.cmd, Cargo.toml, ci.yml):**
- `npm run dev` → `vite`
- `npm run build` → `tsc --noEmit && vite build`
- `npm run tauri:dev` → `tauri dev` (dùng cho phát triển desktop thật; script tiện ích
  Windows: `dev.cmd` ở gốc repo, tự thêm `%USERPROFILE%\.cargo\bin` vào PATH và dọn tiến
  trình mồ côi trước khi gọi `npm run tauri:dev`)
- `npm run tauri:build` → `tauri build`
- `npm run typecheck` → `tsc --noEmit`
- `npm test` → `vitest run`
- `npm run test:watch` → `vitest`
- `npm run fixtures` → `bash scripts/fixtures/make-fixtures.sh` (sinh bộ repo git mẫu dùng
  cho test — cần Git Bash trên Windows)
- `npm run check:chunks` → `node scripts/check-lang-chunks.mjs`
- Rust, chạy trong `src-tauri/`: `cargo build`, `cargo test`, `cargo test --all-features`,
  `cargo clippy --all-targets -- -D warnings`, `cargo fmt --all --check`,
  `cargo bench --bench graph`
- CI (`.github/workflows/ci.yml`) xác nhận đúng các lệnh trên chạy trên `ubuntu-22.04`,
  `windows-latest`, `macos-latest`.

**Yêu cầu môi trường (đối chiếu rust-toolchain.toml + CLAUDE.md + ci.yml):**
- Node.js ^20.19 hoặc >=22.12 (Vite 8 yêu cầu; dự án dev trên Node 22.16)
- Rust: `stable` (channel trong `rust-toolchain.toml`), components `rustfmt` + `clippy`. Trên
  Windows PHẢI dùng target MSVC (không dùng GNU) — cần Visual Studio Build Tools 2022, workload
  "Desktop development with C++", cộng rustup.
- Git CLI (bản gần đây; dev dùng Git 2.54.0 for Windows). Ứng dụng chỉ gọi `git` CLI, không
  dùng libgit2/gitoxide.
- Windows: cần WebView2 Runtime (thường có sẵn trên Windows 10/11 cập nhật).
- Linux: cần `libwebkit2gtk-4.1-dev` (KHÔNG phải 4.0 — Tauri v2 yêu cầu 4.1), cộng
  `libappindicator3-dev`, `librsvg2-dev`, `patchelf`, `libssl-dev`, `build-essential`.
  Khuyến nghị Ubuntu 22.04 trở lên (không nhất thiết `ubuntu-latest` vì AppImage build trên
  glibc mới hơn có thể không chạy được trên distro cũ — chi tiết trong `.github/workflows/ci.yml`).
- macOS: chưa được kiểm thử/dev chính thức (Windows là máy phát triển chính theo PROJECT.md),
  nhưng CI có job build trên `macos-latest`.

**Cấu trúc thư mục thật (từ `ls` gốc + `src-tauri/` + `src/`):**
- `src/` — React + TypeScript: `components/`, `stores/` (Zustand), `lib/`, `styles/`, `test/`,
  `assets/`, cộng `App.tsx`, `main.tsx`
- `src-tauri/` — Rust backend: `src/`, `benches/` (criterion), `tests/`, `capabilities/`
  (capability file Tauri v2), `icons/`, `Cargo.toml`, `tauri.conf.json`
- `docs/` — tài liệu nghiên cứu & quyết định kỹ thuật, gồm (tên file thật, đọc trực tiếp từ
  `ls docs/`): `01-research-competitors.md`, `02-phase4-commit-notes.md`,
  `03-phase1-qa-windows.md`, `04-phase2-degraded-graph.md`, `05-phase2-performance.md`,
  `06-graph-render-model.md`, `07-ui-reference-gap.md`, `08-phase3-dogfood.md`,
  `09-phase3-diff-decision.md`, `10-phase5-dogfood.md`, `10-research-git-gui-landscape.md`,
  `BRAND.md`, cộng thư mục `screenshots/`
- `scripts/` — `fixtures/` (sinh repo git mẫu cho test), `check-lang-chunks.mjs` (cổng kiểm
  bundle size cho lazy-load CodeMirror lang), `verify-work04-bytes.sh`
- `.planning/` — tài liệu quản lý dự án theo phương pháp GSD (PROJECT.md, ROADMAP.md,
  STATE.md, các phase con) — README chỉ nhắc ngắn, không đi sâu (đây là công cụ nội bộ của
  chủ dự án, không phải tài liệu cho người dùng cuối)
- File cấu hình gốc đáng chú ý: `dev.cmd` (script khởi chạy dev trên Windows, tự dọn tiến
  trình mồ côi), `rust-toolchain.toml`, `tsconfig.json`, `vite.config.ts`
</facts_verified>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Viết README.md hoàn chỉnh</name>
  <files>README.md</files>
  <action>
Tạo `README.md` mới ở gốc repo (chưa có file này) bằng tiếng Việt, theo cấu trúc sau. Dùng
CHÍNH XÁC dữ liệu đã liệt kê trong `<facts_verified>` — không thêm số liệu, tên gói, hay lệnh
nào không có trong danh sách đó.

1. **Tiêu đề + mô tả một dòng** — tên "git-plum", một câu mô tả ngắn lấy ý từ
   PROJECT.md ("What This Is").
2. **Core Value** — trích gần nguyên văn câu Core Value từ PROJECT.md/ROADMAP.md: đọc và
   hiểu lịch sử repo phải tức thì, đồ thị mở dưới 1 giây, cuộn mượt cả trên repo hàng chục
   nghìn commit.
3. **Tính năng hiện có** — ba nhóm (Nền tảng, Đọc lịch sử, Xem khác biệt) đúng như liệt kê
   trong `<facts_verified>`. Mở đầu mục này bằng một câu cảnh báo rõ: dự án đang phát triển
   tích cực (theo mô hình MVP theo phase), các mục dưới đây có mã chạy được nhưng một số
   checkpoint kiểm chứng bằng mắt người/hiệu năng vẫn còn treo — KHÔNG dùng các từ "hoàn
   tất", "đã xong", "sẵn sàng dùng". Dùng các cụm trung tính như "đã có" hoặc "đang phát
   triển". Trước mục này liên kết tới `.planning/ROADMAP.md` như nguồn theo dõi tiến độ chi
   tiết.
4. **Lộ trình (chưa có)** — liệt kê ngắn các nhóm chưa làm: vòng lặp commit (tạo commit,
   amend), staging theo khối và an toàn khi huỷ, nhánh/remote/xung đột, hỗ trợ AI soạn
   commit message, và phát hành (bộ cài, tự cập nhật). Không đi sâu chi tiết — chỉ nêu tên
   nhóm để người đọc biết đây là kế hoạch, không phải tính năng có sẵn.
5. **Tech stack** — bảng hoặc danh sách ngắn, dùng đúng tên gói + số version trong
   `<facts_verified>` mục "Tech stack thật". Chia hai phần: Frontend (Tauri v2, React,
   TypeScript, Vite, Zustand, TanStack Virtual, CodeMirror 6) và Backend (Rust, gọi `git`
   CLI qua `tokio::process`, không dùng libgit2/gitoxide, cùng các crate chính).
6. **Yêu cầu môi trường** — theo đúng nội dung mục "Yêu cầu môi trường" trong
   `<facts_verified>`: Node.js version, Rust stable-msvc trên Windows (kèm ghi chú Visual
   Studio Build Tools), Git CLI, WebView2 (Windows), `libwebkit2gtk-4.1-dev` cùng các gói hệ
   thống khác (Linux), lưu ý macOS chưa được dev/test chính thức.
7. **Cách chạy** — chia rõ ba phần con, mỗi lệnh đúng như trong `<facts_verified>` mục "Lệnh
   xác nhận tồn tại":
   - Cài đặt: `npm install` rồi (nếu cần cargo) cargo tự tải khi build lần đầu.
   - Chạy dev: `npm run tauri:dev` (hoặc trên Windows, chạy `dev.cmd` — giải thích ngắn nó tự
     dọn cổng 1420 và tiến trình cũ trước khi khởi chạy).
   - Build: `npm run build` (chỉ frontend) và `npm run tauri:build` (đóng gói ứng dụng đầy
     đủ).
   - Test frontend: `npm test` (chạy một lần) hoặc `npm run test:watch`.
   - Test Rust: `cd src-tauri && cargo test` (hoặc `cargo test --all-features` như CI).
   - Kiểm khác: `npm run typecheck`, `cargo clippy --all-targets -- -D warnings` (trong
     `src-tauri`), `cargo fmt --all --check` (trong `src-tauri`), `npm run check:chunks`,
     `cargo bench --bench graph` (trong `src-tauri`, benchmark thuật toán gán lane —
     không nêu số kết quả cụ thể).
   - Sinh fixture: `npm run fixtures` — cần Git Bash trên Windows.
8. **Cấu trúc thư mục** — danh sách ngắn `src/`, `src-tauri/`, `docs/`, `scripts/`,
   `.planning/`, mỗi mục một câu mô tả, đúng nội dung mục "Cấu trúc thư mục thật" trong
   `<facts_verified>`.
9. **Tài liệu tham khảo** — liệt kê các file trong `docs/` với tên file thật (đã xác nhận
   bằng `ls`) và mô tả một dòng suy ra từ tên file (ví dụ `01-research-competitors.md` —
   nghiên cứu đối thủ cạnh tranh; `05-phase2-performance.md` — số đo hiệu năng Phase 2; v.v.)
   Không bịa nội dung không suy ra được hợp lý từ tên file — nếu không chắc, mô tả chung
   ("ghi chú quyết định kỹ thuật cho Phase X").
10. **Giấy phép** — nêu MIT, dẫn theo `package.json`. Ghi chú rõ: tệp `LICENSE` chính thức
    sẽ được thêm ở Phase 8 (Phát hành) theo roadmap; hiện `package.json` đã khai `"license":
    "MIT"`.
11. **Đóng góp / liên hệ** — một đoạn ngắn, tuỳ chọn (không bắt buộc chi tiết, dự án còn ở
    giai đoạn phát triển nội bộ theo phương pháp GSD, xem `.planning/` cho quy trình).

Không tạo file LICENSE. Không sửa package.json, ROADMAP.md, hay bất kỳ file nào khác — chỉ
tạo README.md.
  </action>
  <verify>
    <automated>test -f README.md && test $(wc -l < README.md) -ge 80 && echo PASS</automated>
  </verify>
  <done>README.md tồn tại ở gốc repo, ≥80 dòng, có đủ 11 mục nêu trên, không claim tính năng
  chưa làm (AI/remote/release) là "đã xong".</done>
</task>

<task type="auto">
  <name>Task 2: Kiểm chứng mọi lệnh và đường dẫn nêu trong README thực sự tồn tại</name>
  <files>README.md</files>
  <action>
Đọc lại `README.md` vừa viết và kiểm chứng KHÔNG có lệnh, tên gói, hay đường dẫn bịa ra. Nếu
bất kỳ kiểm chứng nào thất bại, sửa `README.md` cho khớp thực tế rồi chạy lại. Không tự thêm
tính năng/lệnh mới ngoài phạm vi Task 1.

Kiểm chứng cụ thể (thực hiện thủ công, đọc file và đối chiếu — không cần script phức tạp):
1. Mọi `npm run <script>` xuất hiện trong README phải có mặt trong khối `"scripts"` của
   `package.json` (danh sách hợp lệ: dev, build, preview, tauri, tauri:dev, tauri:build,
   typecheck, test, fixtures, test:watch, check:chunks).
2. Mọi tên file trong mục "Tài liệu tham khảo" phải tồn tại thật trong `docs/` — đối chiếu
   với danh sách đã xác nhận trong `<facts_verified>` mục "Cấu trúc thư mục thật".
3. Mọi thư mục nêu trong mục "Cấu trúc thư mục" (`src/`, `src-tauri/`, `docs/`, `scripts/`,
   `.planning/`) phải tồn tại ở gốc repo — dùng `ls` để xác nhận.
4. Đọc lại mục "Tính năng hiện có": xác nhận không có từ "hoàn tất"/"đã xong"/"sẵn sàng
   dùng"/"production-ready" gắn với bất kỳ mục nào trong ba nhóm Nền tảng/Đọc lịch
   sử/Xem khác biệt. Đọc lại toàn văn README xác nhận "AI", "push", "pull", "merge",
   "rebase", tính năng "bộ cài đặt" CHỈ xuất hiện trong mục Lộ trình hoặc Yêu cầu môi
   trường/Cách chạy (không xuất hiện dưới mục Tính năng hiện có như đã có sẵn).
  </action>
  <verify>
    <automated>grep -oE "npm run [a-zA-Z:_-]+" README.md | sed 's/npm run //' | sort -u | while read -r s; do node -e "const p=require('./package.json'); if(!p.scripts['$s']){console.error('MISSING: '+'$s'); process.exit(1)}"; done && echo PASS</automated>
  </verify>
  <done>Mọi lệnh npm trong README khớp package.json scripts; mọi tên file docs/ và thư mục
  nêu trong README tồn tại thật; không mục tính năng hiện có nào dùng từ "hoàn tất/đã
  xong/sẵn sàng dùng"; AI/push/pull/merge/rebase/bộ cài chỉ xuất hiện ở mục Lộ trình hoặc
  Yêu cầu môi trường.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| README content -> public repo | README công khai trên repo mã nguồn mở; nội dung sai sự thật về tính năng/bảo mật có thể gây hiểu nhầm cho người dùng/cộng tác viên |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-quick-260923-01 | Repudiation/Information Disclosure (overclaim) | README.md | mitigate | Task 1 + Task 2 buộc mọi claim tính năng bám sát ROADMAP.md/STATE.md thật, có bước kiểm chứng riêng (Task 2) chống overclaim tính năng chưa xong hoặc chưa kiểm chứng (AI, remote, release) |
| T-quick-260923-02 | Tampering (lệnh sai trong hướng dẫn) | README.md | mitigate | Task 2 đối chiếu từng lệnh npm/cargo với package.json và cấu trúc thư mục thật, tránh người dùng chạy lệnh không tồn tại hoặc clone sai kỳ vọng |
</threat_model>

<verification>
- `README.md` tồn tại ở gốc repo, không sửa file nào khác ngoài phạm vi cho phép.
- Mọi lệnh `npm run *` và `cargo *` trong README khớp với `package.json` scripts và
  `src-tauri/Cargo.toml`/CI thật.
- Mọi tên file trong mục "Tài liệu tham khảo" tồn tại thật trong `docs/`.
- Mục "Tính năng hiện có" không dùng từ ngữ khẳng định hoàn tất; mục "Lộ trình" chứa đúng các
  nhóm chưa làm (AI, remote/nhánh/xung đột, staging theo khối, phát hành).
- Giấy phép MIT được nêu, kèm ghi chú LICENSE file sẽ thêm ở Phase 8.
</verification>

<success_criteria>
- Người lạ clone repo, đọc README, hiểu đúng git-plum là gì, Core Value là gì, tính năng nào
  đã có mã (có ghi chú "đang phát triển"), tính năng nào chưa có (roadmap).
- Người lạ làm theo mục "Cách chạy" chạy được `npm install`, `npm run tauri:dev`, `npm test`,
  `cd src-tauri && cargo test` không gặp lệnh sai/không tồn tại.
- Không có claim nào về AI, remote (push/pull/merge/rebase), bộ cài đặt/updater là "đã xong"
  hoặc "sẵn sàng dùng".
</success_criteria>

<output>
After completion, create `.planning/quick/260923-na9-vi-t-readme-md-cho-d-n-git-plum/260923-na9-SUMMARY.md`
</output>
