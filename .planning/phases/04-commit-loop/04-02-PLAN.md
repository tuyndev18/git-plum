---
phase: 04-commit-loop
plan: 02
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - src-tauri/src/commands/worktree.rs
  - src-tauri/src/commands/mod.rs
  - src-tauri/src/commands/diff.rs
  - src-tauri/src/lib.rs
  - src/lib/ipc.ts
  - src/stores/statusStore.ts
  - src/components/worktree/ChangeList.tsx
  - src/components/worktree/ChangeList.test.tsx
  - src/stores/statusStore.test.ts
  - src/lib/ipc.status.test.ts
  - src/styles/app.css
autonomous: true
requirements: [WORK-01, WORK-02]
must_haves:
  truths:
    - "Người dùng thấy ba nhóm tệp: đã stage, chưa stage, chưa theo dõi"
    - "Bấm một tệp mở diff của tệp đó trong thư mục làm việc"
    - "Bấm stage một tệp thì tệp chuyển nhóm ngay, không cần bấm làm mới"
    - "Bấm unstage một tệp thì nó chuyển về nhóm chưa stage ngay"
    - "Thao tác ghi trả trạng thái mới TRỰC TIẾP, không chờ watcher"
    - "Một tệp mà diff hỏng vẫn stage/unstage được"
  artifacts:
    - path: "src-tauri/src/commands/worktree.rs"
      provides: "get_status, stage_files, unstage_files"
      contains: "pub async fn stage_files"
    - path: "src/stores/statusStore.ts"
      provides: "trạng thái thư mục làm việc khoá theo repoId"
      contains: "byRepo"
    - path: "src/components/worktree/ChangeList.tsx"
      provides: "ba nhóm tệp bấm được"
      min_lines: 80
  key_links:
    - from: "src/components/worktree/ChangeList.tsx"
      to: "src/stores/statusStore.ts"
      via: "đọc entries, gọi stage/unstage"
      pattern: "useStatusStore"
    - from: "src-tauri/src/commands/worktree.rs"
      to: "src-tauri/src/git/parsers/status.rs"
      via: "parse_status trên stdout của STATUS_ARGS"
      pattern: "parse_status"
    - from: "src-tauri/src/commands/worktree.rs"
      to: "RepoStatus trả về từ lệnh ghi"
      via: "stage_files trả RepoStatus mới, không trả ()"
      pattern: "Result<RepoStatus>"
---

<objective>
Slice dọc đầu tiên dùng được: **thấy ba nhóm tệp và stage/unstage theo tệp**
(WORK-01 + WORK-02, tiêu chí thành công 1 và 2).

Purpose: Sau plan này người dùng làm được một việc họ chưa làm được trước đó — chọn
tệp nào sẽ vào commit tới. Đầu-cuối: React → IPC → Rust → `git`. Không tầng ngang;
không có "nền tảng cho phase sau".

Output: Ba command Rust, một store, một component, và diff của thư mục làm việc.

🔴 **Ràng buộc chống lệ thuộc (CONTEXT.md mục 0, R8).** Trình xem diff của Phase 3
**chưa ai dùng thật**; chủ dự án đã tìm **năm** lỗi hiển thị của nó chỉ bằng cách mở
ứng dụng, và cả năm đi qua 435 test. Nên: `ChangeList` **không được** phụ thuộc vào
việc diff render đúng. Chọn một tệp mà diff hỏng thì vẫn phải stage/unstage được tệp
đó. Nếu plan này làm vòng commit chết theo trình xem diff, plan này sai.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04-commit-loop/CONTEXT.md
@.planning/phases/04-commit-loop/04-01-SUMMARY.md

@src-tauri/src/commands/repo.rs
@src-tauri/src/commands/diff.rs
@src-tauri/src/git/runner.rs
@src-tauri/src/state/mod.rs
@src/stores/diffStore.ts
@src/components/history/FileList.tsx
@src/lib/commands.ts

<interfaces>
<!-- Trích từ mã thật. Executor dùng trực tiếp. -->

Từ `04-01` (plan trước — đọc SUMMARY của nó để lấy hình dạng chốt):
`RepoStatus`, `StatusEntry`, `StatusGroup`, `BranchInfo`, `WipCounts`,
`parse_status(&[u8]) -> RepoStatus`, `STATUS_ARGS`.

Từ `src-tauri/src/state/mod.rs` — PLAT-03, khoá ghi:
```rust
impl RepoHandle { pub fn write_lock(&self) -> Arc<tokio::sync::Mutex<()>> }
impl AppState {
    pub fn runner(&self, repo: Arc<RepoHandle>) -> crate::git::GitRunner;
    pub fn get_repo(&self, id: &str) -> Option<Arc<RepoHandle>>;
}
```
🔴 `write_lock` dùng `tokio::sync::Mutex` **có chủ ý** — khoá phải giữ qua `.await`.
Mọi lệnh **ghi** của plan này phải giữ nó. Đây là PLAT-03 và nó là gốc của việc
watcher (plan 04-04) chỉ phải xử lý thay đổi thật từ **bên ngoài**.

Từ `src-tauri/src/git/exec.rs`:
```rust
pub const DEFAULT_TIMEOUT: Duration = Duration::from_secs(30);
pub struct GitOutput { pub stdout: Vec<u8>, pub stderr: Vec<u8>, pub status: i32 }
impl GitOutput { pub fn is_success(&self) -> bool; pub fn stderr_lossy(&self) -> String }
impl GitCommand {
    pub fn new(repo_path: impl AsRef<Path>) -> Self;
    pub fn args<I, S>(self, args: I) -> Self;
    pub fn arg(self, arg: impl AsRef<OsStr>) -> Self;
    pub fn timeout(self, t: Duration) -> Self;
    pub async fn run(self) -> Result<GitOutput>;
}
```

Từ `src-tauri/src/error.rs` — 9 mã lỗi hiện có:
`spawn_failed`, `timeout`, `stdin_write_failed`, `command_failed`,
`not_a_repository`, `no_repository_open`, `unknown_repository`, `parse_failed`, `io`.

Từ `src/lib/ipc.ts` — khuôn đặt tên tham số (Tauri v2 đổi snake_case → camelCase):
```ts
getFileDiff: (repoId: string, commitId: string, path: string) =>
  invoke<FileDiff>('get_file_diff', { repoId, commitId, path }),
```
Sai tên tham số thì `invoke` thất bại **lúc chạy**, không lúc biên dịch — nên
`ipc.status.test.ts` phải khẳng định **cả** tên command **và** hình dạng đối số,
đúng khuôn `ipc.history.test.ts` đã làm.
</interfaces>

<pathspec_gate_warning>
🔴 **Plan này SẼ làm đỏ một test đang xanh — và đó là hành vi đúng.**

`src-tauri/src/commands/diff.rs` có cổng
`moi_lenh_co_pathspec_deu_co_dau_gach_ngang_truoc_path` (dòng ~1107) khẳng định **hai**
con số tuyệt đối:
- `so_moc == 5` — số lần xuất hiện mốc `PATHSPEC_SAU_DAU_GACH`
- `so_lenh_co_pathspec == 4` — số lệnh git mang pathspec

Cổng này cố ý đếm số lệnh để **đỏ khi có lệnh mới chưa được kiểm** (CONTEXT.md 3.5).
Lệnh diff-thư-mục-làm-việc của Task 2 mang pathspec, nên cổng sẽ đỏ.

**Cách xử lý đúng:** cập nhật **cả hai** con số và mở rộng doc comment nêu lệnh mới.
**Cách xử lý sai:** nới cổng thành `>=`, hay xoá phép khẳng định. Làm vậy là biến một
cổng đang có tác dụng thành cổng thứ bảy không thể fail.

Lệnh mới phải dùng đúng mốc `PATHSPEC_SAU_DAU_GACH(path)` và có `.arg("--")` **trước**
nó, nếu không cổng đỏ đúng chỗ. `stage_files`/`unstage_files` cũng mang pathspec; nếu
chúng ở `worktree.rs` thì cổng của `diff.rs` **không** phủ được — Task 1 phải có cổng
riêng cùng khuôn cho `worktree.rs`.
</pathspec_gate_warning>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: get_status / stage_files / unstage_files — lệnh ghi trả trạng thái mới trực tiếp</name>
  <files>src-tauri/src/commands/worktree.rs, src-tauri/src/commands/mod.rs, src-tauri/src/lib.rs</files>
  <behavior>
    - `get_status(repoId)` trên repo mẫu `status-cases` → `RepoStatus` có cả ba nhóm.
    - `stage_files(repoId, paths)` → trả **`RepoStatus` mới**, không `()`. Tệp vừa stage nằm ở nhóm `Staged`. Đây là ràng buộc 2.5 của CONTEXT.md, và kiểu trả về là cách bảo đảm nó: một command trả `()` **buộc** giao diện phải chờ watcher.
    - `unstage_files(repoId, paths)` → trả `RepoStatus` mới, tệp về nhóm `Unstaged`.
    - `paths` **rỗng** → không sinh lệnh git nào, trả status hiện tại. `git add` không pathspec sẽ stage **cả cây** — một lỗi phá hoại, và mảng rỗng đến từ webview.
    - Đường dẫn bắt đầu bằng `-` (ví dụ `-rf`) → **không** bị git đọc thành cờ. Đây là lý do `--` phải có.
    - Đường dẫn trùng tên nhánh (`main`, `HEAD`) → xử lý như đường dẫn, không như revision.
    - `.git/index.lock` tồn tại → **thử lại có giãn cách**, và `index.lock` **vẫn còn** sau khi thất bại. Xoá nó là làm hỏng repo của người dùng đang `git rebase` ở terminal (CONTEXT.md 2.3, R3).
    - Thất bại vì lock → lỗi có mã **riêng**, nói rõ "đang chờ một tiến trình git khác", không phải `command_failed` chung.
  </behavior>
  <action>
Tạo `src-tauri/src/commands/worktree.rs`. Đăng ký `pub mod worktree;` + `pub use
worktree::*;` ở `commands/mod.rs`, và ba command vào `generate_handler!` ở `lib.rs`.

**`get_status`** — lệnh **đọc**, qua `runner.read(...)` như `lay_diff_tep` làm, với
`DEFAULT_TIMEOUT`. Dùng `STATUS_ARGS` của 04-01 (**không** viết lại danh sách cờ ở
đây — hai nguồn là lệch). Đưa stdout qua `parse_status`. Tách phần logic ra một hàm
thường (`pub async fn lay_trang_thai(state, repo)`) ngoài `#[tauri::command]`, để test
tích hợp gọi được mà không dựng webview — khuôn `lay_diff_tep`/`lay_lich_su_tep`.

🔴 **Không lặp lại khuôn lỗi của `open_repository`.** CONTEXT.md mục 0 ghi:
`open_repository` báo sai "Không phải một repository git" khi git từ chối repo vì
`dubious ownership` (exit 128) — vì mã coi "exit khác 0" là **một** nguyên nhân. Ở đây
`git status` cũng exit khác 0 vì nhiều nguyên nhân (repo bị từ chối, index.lock, repo
hỏng). Phân loại theo **stderr** rồi trả mã lỗi khác nhau, và **luôn** kèm stderr
nguyên văn. Coi stderr là văn bản chẩn đoán mờ, không phải hợp đồng phân tích được.

**`stage_files` / `unstage_files`** — lệnh **ghi**:
- Giữ `repo.write_lock()` suốt thời gian chạy (PLAT-03).
- `git add -- <paths>` và `git restore --staged -- <paths>`.
  🔴 **`--` PHẢI trước pathspec.** Dùng mốc `PATHSPEC_SAU_DAU_GACH`.
- Chặn `paths` rỗng **trước** khi dựng lệnh.
- Xong thì gọi `lay_trang_thai` và **trả `RepoStatus`**. Doc comment ghi vì sao trả
  trạng thái thay vì `()`: chờ watcher làm giao diện trễ 250–300 ms sau **mỗi** cú
  bấm, và nếu watcher chết thì giao diện đứng im mà không ai biết (CONTEXT.md 2.5).

**Thử lại `index.lock` — một hàm dùng chung**, không lặp ở hai command. Giãn cách theo
số lần, tổng thời gian chờ có **chặn trên** (khoảng 1 giây tổng: giao diện không được
treo, và nếu người dùng đang rebase thật thì chờ lâu cũng không giúp gì). Nhận biết
bằng stderr có `index.lock`, **không** bằng `std::fs::exists` — kiểm tra tệp là một
cuộc đua, còn stderr là câu trả lời của chính git. **Không bao giờ** xoá tệp lock; ghi
ràng buộc đó thành doc comment kèm lý do.

Thêm variant `GitError` cho ca lock, với mã riêng (ví dụ `index_locked`), và thêm mã
đó vào union `GitErrorCode` ở `src/lib/ipc.ts`. Thông báo phải nói **đang chờ cái gì**
và rằng người dùng có thể đang chạy git ở nơi khác.

**Cổng pathspec cho `worktree.rs`** — cùng khuôn cổng của `diff.rs` (đọc `include_str!`
thân hàm, **bỏ dòng chú thích trước khi tìm**, cắt theo `GitCommand::new`, kiểm
`--` trước mốc trong **phạm vi từng lệnh**). Ghi rõ số lệnh mang pathspec. Đọc kỹ khối
comment "Bản đầu của cổng này VÔ DỤNG" trong `diff.rs` trước khi viết: phép so vị trí
`--` **toàn cục** trong cả thân hàm cho 0 test đỏ khi xoá `--` của một lệnh cụ thể.
Đừng viết lại đúng cái bẫy đó.
  </action>
  <verify>
    <automated>cd src-tauri &amp;&amp; cargo test --lib --tests worktree 2>&amp;1 | tail -5</automated>
    <automated>cd src-tauri &amp;&amp; cargo test --lib --tests pathspec 2>&amp;1 | tail -5</automated>
  </verify>
  <done>
Ba command đăng ký trong `generate_handler!`. Mọi mục `<behavior>` có test tương ứng.
Cổng pathspec của **cả** `diff.rs` (đã cập nhật số) **và** `worktree.rs` xanh, và bảng
đột biến chứng minh cả hai phân biệt được.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: diff của thư mục làm việc + statusStore + ChangeList ba nhóm</name>
  <files>src-tauri/src/commands/diff.rs, src/lib/ipc.ts, src/stores/statusStore.ts, src/stores/statusStore.test.ts, src/lib/ipc.status.test.ts, src/components/worktree/ChangeList.tsx, src/components/worktree/ChangeList.test.tsx, src/styles/app.css</files>
  <behavior>
    - `ChangeList` với 2 tệp staged, 3 unstaged, 1 untracked → hiện **ba** tiêu đề nhóm và **6** hàng tệp. Nhóm rỗng **không** hiện tiêu đề (nhóm rỗng có tiêu đề là ba dòng nhiễu trên một repo sạch).
    - Bấm một hàng tệp → `diffStore.selectedFileByRepo[repoId]` thành path đó.
    - Bấm nút stage của một hàng unstaged → gọi `ipc.stageFiles` **một lần** với đúng path đó, và store nhận `RepoStatus` **từ giá trị trả về** — không gọi lại `getStatus`.
    - Bấm nút unstage của một hàng staged → `ipc.unstageFiles` với đúng path đó.
    - 🔴 `ipc.stageFiles` **ném lỗi** → hàng vẫn còn, thông báo lỗi hiện, giao diện **không** trắng. Ca này là R3 (index.lock) ở tầng giao diện.
    - 🔴 **Diff hỏng không được chặn stage.** `getWorktreeDiff` ném lỗi → nút stage của hàng đó **vẫn bấm được** và vẫn gọi `ipc.stageFiles`. Đây là ràng buộc R8, và nó là một test, không phải một lời hứa.
    - Cùng một tệp ở **cả** staged **và** unstaged (XY = `MM`) → hiện ở **cả hai** nhóm, và bấm stage ở nhóm unstaged chỉ gửi **một** path.
    - `statusStore.refresh(repoId)` hai lần đồng thời → không hai lời gọi IPC trùng (khuôn `loadedRanges`/đang-bay của `historyStore`).
  </behavior>
  <action>
**Rust — diff của thư mục làm việc.** Mở rộng `commands/diff.rs` bằng một đường
`get_worktree_diff(repoId, path, staged: bool)`. `staged = true` → `git diff --cached`;
`false` → `git diff` (không revision). Tái dùng bộ phân tích unified diff, cổng kích
thước, và phép phát hiện nhị phân **đã có** — đừng viết lại.

🔴 **Không dùng `DiffCache` cho diff thư mục làm việc.** Cache của Phase 3 đúng vì
diff của commit **lịch sử** là bất biến; diff của thư mục làm việc đổi mỗi lần người
dùng gõ. Ghi lý do thành doc comment cạnh chỗ **không** gọi cache — một `put_diff` vô
tình ở đây cho người dùng nội dung cũ, và đây là phase đầu tiên mà dữ liệu cũ **nguy
hiểm** chứ chỉ sai mắt nhìn (CONTEXT.md 2.2).

Cập nhật cổng pathspec của `diff.rs` (xem `<pathspec_gate_warning>`): sửa **cả hai**
con số và mở rộng doc comment nêu lệnh mới. Không nới thành `>=`.

**TypeScript.** Thêm vào `src/lib/ipc.ts`: `StatusEntry`, `StatusGroup`, `BranchInfo`,
`RepoStatus`, `WipCounts` (khớp `camelCase` của serde), `ipc.getStatus`,
`ipc.stageFiles`, `ipc.unstageFiles`, `ipc.getWorktreeDiff`, và mã lỗi `index_locked`
vào `GitErrorCode`. `ipc.status.test.ts` khẳng định tên command + hình dạng đối số cho
từng hàm (khuôn `ipc.history.test.ts`).

**`src/stores/statusStore.ts`** — khuôn `byRepo[repoId]` của `historyStore`/`repoStore`
(PLAT-05). Giữ `RepoStatus | undefined`, `isLoading`, `error`. Hai loại action:
- `refresh(repoId)` — gọi `getStatus`, có chống trùng lời gọi đồng thời.
- `stage(repoId, paths)` / `unstage(repoId, paths)` — gọi IPC và ghi
  `RepoStatus` **từ giá trị trả về**. 🔴 **Không** gọi `refresh` sau đó. Gọi thêm là
  một lệnh git thừa cho mỗi cú bấm, và nó xoá mất cả điểm của việc lệnh ghi trả trạng
  thái. Có test khẳng định `getStatus` **không** được gọi trong đường stage.
- `applyExternal(repoId, status)` — điểm vào cho watcher của plan 04-04. Khai **ở đây**
  để 04-04 chỉ phải nối dây, không phải đổi hình dạng store.

**`src/components/worktree/ChangeList.tsx`** — ba nhóm, mỗi nhóm một danh sách hàng
bấm được. Khuôn `FileList.tsx`: bấm ghi vào `diffStore` **rồi** `runCommand(...)`
(PLAT-04) — đọc khối doc comment dài ở đầu `FileList.tsx` để hiểu vì sao đường này
được chọn thay vì truyền tham số qua sổ lệnh.

🔴 **Nút stage/unstage phải là phần tử riêng, không phải cùng phần tử với hàng.** Một
cú bấm = một ý định. Nếu bấm hàng vừa mở diff vừa stage thì người dùng không xem được
trước khi stage — và nếu diff hỏng (R8) thì họ mất luôn đường stage. Nút gọi
`stopPropagation`, và có test khẳng định bấm nút **không** làm đổi tệp đang chọn.

**Chưa ảo hoá danh sách này**, và ghi lý do vào doc comment: thư mục làm việc có hàng
chục tới hàng trăm tệp, không phải 100k — `useVirtualizer` ở đây thêm một nguồn tính
chỉ số vào đúng phase đang đụng vào bất biến virtualizer của WORK-11. Nếu dogfood cho
thấy chậm thì đó là plan riêng, có số đo.

**CSS** vào `src/styles/app.css`. ⚠️ Tệp này **đang có thay đổi chưa commit của
`git-plum-2a`** — xem `<in_flight_files>`. **Chỉ thêm** khối mới ở cuối, không sửa
luật đã có, không chạy formatter lên cả tệp.

⚠️ **happy-dom không tính CSS layout và không có cuộn thật** (CONTEXT.md 3.4). Mọi
khẳng định **bố cục** (ba nhóm không tràn, chiều cao hàng) là **chưa kiểm chứng** cho
tới khi có người xem trên Chromium thật. SUMMARY ghi "có mã, chưa kiểm", **không** ghi
"đạt", cho phần bố cục. Việc kiểm mắt người nằm ở checkpoint của plan 04-05.
  </action>
  <verify>
    <automated>rm -f .vitest/json/output.json &amp;&amp; npx vitest run --reporter=json --outputFile=.vitest/json/output.json &amp;&amp; node -e "const r=require('./.vitest/json/output.json');if(r.numFailedTests>0)process.exit(1);console.log('tests',r.numTotalTests)"</automated>
    <automated>npx tsc --noEmit &amp;&amp; echo "tsc sạch"</automated>
    <automated>cd src-tauri &amp;&amp; cargo test --lib --tests 2>&amp;1 | tail -3</automated>
  </verify>
  <done>
Frontend test tổng **> 435** và **0** đỏ. `npx tsc --noEmit` sạch. `npm run build`
sạch. Rust tổng **>** số của 04-01. Cổng pathspec của `diff.rs` xanh với con số **đã
cập nhật** (không nới lỏng).

🔴 **Suite phải chạy ≥ 3 lần** và xanh cả ba — lỗi #6 của CONTEXT.md 3.1 đỏ 1/5 lần và
sống qua cả một wave vì suite chỉ chạy một lần. `rm -f .vitest/json/output.json` trước
**mỗi** lần: tệp đó bị cache và đọc lại kết quả cũ.
  </done>
</task>

</tasks>

<mutation_table>
| # | Đột biến | Test phải đỏ |
|---|---|---|
| M1 | `stage_files` trả `()` thay vì `RepoStatus` | test "stage trả RepoStatus mới" (đỏ lúc biên dịch là đủ) |
| M2 | `statusStore.stage` gọi `refresh` sau khi stage | test "đường stage KHÔNG gọi getStatus" |
| M3 | Bỏ `.arg("--")` khỏi lệnh `git add` | cổng pathspec `worktree.rs` |
| M4 | Bỏ chặn `paths` rỗng | test "paths rỗng không sinh lệnh git" |
| M5 | Bỏ `write_lock` khỏi `stage_files` | test PLAT-03 cho đường ghi |
| M6 | Đường xử lý lock **xoá** `index.lock` | test "index.lock vẫn còn sau thất bại" |
| M7 | Nút stage dùng cùng phần tử với hàng (bỏ `stopPropagation`) | test "bấm nút không đổi tệp đang chọn" |
| M8 | `ChangeList` hiện tiêu đề cho nhóm rỗng | test "nhóm rỗng không hiện tiêu đề" |
| M9 | `ChangeList` vô hiệu nút stage khi diff lỗi | 🔴 test R8 "diff hỏng vẫn stage được" |
| M10 | `get_worktree_diff` ghi kết quả vào `DiffCache` | test "diff thư mục làm việc không vào cache" |
| M11 | Đổi tên tham số IPC `repoId` → `repo_id` | `ipc.status.test.ts` khẳng định hình dạng đối số |
| M12 | Nới cổng pathspec `diff.rs` từ `== 5` thành `>= 5` | ⚠️ **không** test nào đỏ — đây là phép kiểm bằng mắt của người rà plan: đừng làm |

**M9 là đột biến quan trọng nhất của plan.** Nó ghim ràng buộc R8, thứ duy nhất ngăn
vòng commit chết theo một trình xem diff mà **chưa ai dùng thật**. Nếu M9 cho 0 test
đỏ thì `ChangeList` đang lệ thuộc diff và plan chưa xong.

M12 không phải đột biến mã — nó là ca duy nhất mà bảng này không tự kiểm được. Ghi
trong SUMMARY rằng con số cổng pathspec đã được **cập nhật**, kèm giá trị cũ → mới.
</mutation_table>

<in_flight_files>
🔴 **`src/styles/app.css` đang có thay đổi chưa commit thuộc session `git-plum-2a`.**

Đã kiểm lúc lập kế hoạch: tệp ở trạng thái `.M` (worktree đổi, index sạch).

Quy tắc:
- **Chỉ thêm** khối CSS mới ở **cuối** tệp. Không sửa, không sắp xếp lại, không chạy
  formatter lên cả tệp.
- **Không** `git add` / `git commit` tệp này. Commit của plan này liệt kê **từng
  đường dẫn** — không `git add -A`, không `git commit -a`.
- Nếu tệp đã được session kia commit lúc executor tới đây, đường thêm-ở-cuối vẫn đúng.
</in_flight_files>

<verification>
- [ ] Frontend **> 435** test, 0 đỏ, **3 lần chạy liên tiếp** (`rm -f` tệp JSON trước mỗi lần)
- [ ] `npx tsc --noEmit` sạch; `npm run build` sạch
- [ ] Rust `--lib --tests` xanh, tổng tăng so với 04-01
- [ ] `cargo clippy --all-targets`: vẫn đúng **1** warning cũ (`diff.rs:1470`)
- [ ] Cổng pathspec `diff.rs` **và** `worktree.rs` xanh, con số đã cập nhật
- [ ] Bảng đột biến M1–M11 có test đỏ; M12 ghi chú bằng lời trong SUMMARY
- [ ] `git status --porcelain=v2` xác nhận **không** tệp nào của `git-plum-2a` bị commit

**Không** `npm run lint` — script đó **không tồn tại** trong `package.json`.
**Không** `git log` cho câu hỏi nào: trần cứng 50 dòng, thất bại im lặng. Dùng
`git rev-list` / `git merge-base --is-ancestor`.
</verification>

<success_criteria>
Người dùng mở repo có thay đổi, thấy ba nhóm tệp, bấm một tệp thấy diff của nó, bấm
stage/unstage và danh sách cập nhật **ngay** — không bấm làm mới, không chờ watcher
(watcher còn chưa tồn tại ở wave này; đó là bằng chứng ràng buộc 2.5 được tôn trọng
theo kiến trúc, không nhờ may).

Và: một tệp mà trình xem diff hỏng **vẫn** stage được, có test ghim.
</success_criteria>

<output>
Tạo `.planning/phases/04-commit-loop/04-02-SUMMARY.md`.

Ghi bắt buộc: bảng đột biến kèm tên test đỏ; **ba** lần chạy suite frontend kèm số;
con số cổng pathspec cũ → mới; và danh sách tiêu chí bố cục đang ở trạng thái "có mã,
chưa kiểm" để checkpoint 04-05 nhặt lên.
</output>
