---
phase: 04-commit-loop
plan: 05
type: execute
wave: 5
depends_on: [04-04]
files_modified:
  - src/App.tsx
  - src/components/history/CommitList.tsx
  - src/components/history/GraphCanvas.tsx
  - src/components/history/CommitList.test.tsx
  - src/styles/app.css
  - src/lib/graph-render/canvasRenderer.ts
  - docs/09-phase4-dogfood.md
  - .planning/phases/04-commit-loop/VERIFICATION.md
autonomous: false
requirements: [WORK-01, WORK-02, WORK-08, WORK-09, WORK-10, WORK-11]
must_haves:
  truths:
    - "Hàng WIP hiện ở đầu đồ thị, thẳng cột với văn bản ở mọi vị trí cuộn"
    - "Bấm hàng WIP mở vùng soạn commit"
    - "Commit xong hoặc huỷ hết thay đổi thì hàng WIP biến mất"
    - "Chạy git từ terminal ngoài → giao diện cập nhật dưới 1 giây, không bấm gì"
    - "Một commit THẬT vào một repository THẬT, tạo ra chỉ bằng git-plum"
  artifacts:
    - path: "docs/09-phase4-dogfood.md"
      provides: "ghi chép exit gate + dấu thời gian exe"
      contains: "git-plum.exe"
    - path: ".planning/phases/04-commit-loop/VERIFICATION.md"
      provides: "trạng thái từng requirement kèm loại bằng chứng"
      contains: "WORK-11"
  key_links:
    - from: "src/components/history/CommitList.tsx"
      to: "src/lib/graph-render/wipRow.ts"
      via: "commitRowY + contentOffset, KHÔNG tự tính ±1"
      pattern: "wipRow"
    - from: "src/App.tsx"
      to: "src/components/worktree/ChangeList.tsx"
      via: "render ChangeList + CommitBox trong vùng giao diện"
      pattern: "ChangeList"
---

<objective>
Nối dây và **kiểm bằng mắt người**. Đây là plan duy nhất của phase sửa các tệp mà
session khác đang giữ, và là plan mang **hai cổng chặn**.

Purpose: Bốn plan trước dựng logic có test. Nhưng CONTEXT.md mục 3.4 nói thẳng:
happy-dom **không** tính CSS layout và **không** có cuộn thật, nên **cả năm** lỗi hiển
thị của Phase 3 đều do chủ dự án mở ứng dụng mà tìm ra — cả năm đi qua 435 test. Với
Phase 4, mọi khẳng định về bố cục là **chưa kiểm chứng** cho tới khi có người xem.

Exit gate của phase: **một commit thật vào một repository thật, tạo ra chỉ bằng
git-plum.** Chủ dự án đã chọn "dừng lại, báo tôi" cho cổng bắt buộc. Kết quả đúng khi
cổng không đóng được là **một plan vá khoảng cách**, không phải đi tiếp Phase 5.

Output: Hàng WIP trên màn hình, `docs/09-phase4-dogfood.md`, `VERIFICATION.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04-commit-loop/CONTEXT.md
@.planning/phases/04-commit-loop/04-04-SUMMARY.md
@.planning/phases/03-diff-viewer/VERIFICATION.md

@src/components/history/CommitList.tsx
@src/components/history/GraphCanvas.tsx
@src/App.tsx
@src/lib/graph-render/wipRow.ts
@src/lib/graph-render/canvasRenderer.ts
@docs/08-phase3-dogfood.md
</context>

<blocking_precondition>
🔴 **Task 0 là một cổng, không phải một bước. Chạy nó TRƯỚC mọi việc khác.**

Bốn tệp plan này phải sửa — `src/App.tsx`, `src/components/history/CommitList.tsx`,
`src/components/history/CommitList.test.tsx`, `src/styles/app.css` — thuộc session
`git-plum-2a` và **chưa commit** lúc lập kế hoạch (2026-09-22, trạng thái `.M`).

`CommitList.tsx` là **đúng** tệp WORK-11 phải sửa. Không có đường tránh.

```bash
# Cổng: bốn tệp đã sạch chưa?
git status --porcelain=v2 -z --untracked-files=all | tr '\0' '\n' \
  | grep -E "src/App.tsx|CommitList|app.css" || echo "SẠCH — đi tiếp"
```

- **Sạch** (không dòng nào in ra) → session kia đã commit, đi tiếp bình thường.
- **Còn `.M`** → **DỪNG. Báo chủ dự án.** Không merge, không stash, không commit thay
  session khác. Hai session sửa cùng tệp là cách mất việc của một trong hai.

Chủ dự án đã chọn "dừng lại, báo tôi" cho cổng bắt buộc; cổng này cùng loại. Nếu phải
dừng, ghi rõ trong báo cáo: bốn plan trước **đã xong và có test**, chỉ còn bước nối
dây, và nó mở lại được ngay khi `git-plum-2a` giao lại.
</blocking_precondition>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 0: CỔNG — xác nhận bốn tệp của session khác đã được giao lại</name>
  <what-checked>
Bốn tệp `src/App.tsx`, `src/components/history/CommitList.tsx`,
`src/components/history/CommitList.test.tsx`, `src/styles/app.css` không còn thay đổi
chưa commit thuộc session `git-plum-2a`.
  </what-checked>
  <how-to-verify>
```bash
git status --porcelain=v2 -z --untracked-files=all | tr '\0' '\n' \
  | grep -E "src/App.tsx|CommitList|app.css"
```
Không dòng nào in ra = sạch = đi tiếp.
  </how-to-verify>
  <resume-signal>
Sạch → tự đi tiếp Task 1, không cần hỏi.
Còn bẩn → **dừng và báo chủ dự án**; đợi trả lời.
  </resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Task 1: Nối hàng WIP vào CommitList — dùng wipRow, không tự tính chỉ số</name>
  <files>src/components/history/CommitList.tsx, src/components/history/CommitList.test.tsx, src/components/history/GraphCanvas.tsx, src/lib/graph-render/canvasRenderer.ts, src/styles/app.css</files>
  <behavior>
    - 🔴 **Vẫn đúng MỘT lời gọi `useVirtualizer`** trong toàn bộ `src/components/history/`. Cổng đếm, và **đỏ** nếu thành 2. Đây là bất biến cốt lõi của Phase 2.
    - 🔴 `count` truyền cho `useVirtualizer` là `virtualizerCount(total, hasWip)` — và test khẳng định nó `=== total` **kể cả khi có** hàng WIP.
    - 🔴 **Thẳng cột ở ≥ 3 vị trí cuộn** (R1 đòi ≥ 3): với hàng WIP đang hiện, `y` mà cột đồ thị nhận cho hàng chỉ số `i` **bằng** `y` mà cột văn bản dùng, ở `scrollTop` = 0, giữa, và cuối. Cả hai bên gọi `commitRowY` của `wipRow.ts`.
    - `hasWip = false` → **mọi** test hiện có của `CommitList.test.tsx` vẫn xanh, không sửa một cái nào. Nếu phải sửa một test cũ để nó xanh, hành vi cũ **đã bị đổi** — dừng và xem lại.
    - `wipCounts = {modified: 3, added: 1}` → hàng WIP hiện chuỗi nhận diện được (`✏3` và `+1`).
    - `wipCounts = {0, 0}` → hàng WIP **không** render (tiêu chí 7).
    - Bấm hàng WIP → mở vùng soạn commit (gọi handler tương ứng), và **không** gọi `onSelect` với một commitId bịa.
    - Hàng WIP **không có SHA** và **không** vào `historyStore.commits` — test khẳng định `commits.length` không đổi khi hàng WIP bật/tắt.
    - `headLane >= 13` → hiện chỉ báo suy giảm (variant `clamped` của 04-04), **không** vẽ cạnh vào cột 12.
  </behavior>
  <action>
Sửa `CommitList.tsx`:
- `count: virtualizerCount(total, hasWip)` (hàm của 04-04; nó trả `total`).
- Hàng WIP là phần tử **anh em** ghim ở đầu vùng cuộn, cao `WIP_ROW_HEIGHT`, **ngoài**
  div cao `getTotalSize()`. Đây là **cách B** đã chốt ở 04-04 — đọc
  `<wip_index_decision>` của plan đó trước khi sửa; đừng cài lại theo cách A.
- Vùng nội dung bên dưới lệch `contentOffset(hasWip)`.
- `renderRows` dùng `commitRowY(v.start, scrollTop, hasWip)` thay cho
  `v.start - scrollTop` viết thẳng. 🔴 **Không** thêm phép `±` nào trong tệp này —
  mọi số học ở `wipRow.ts`.

`GraphCanvas`/`canvasRenderer`: vẽ cạnh hàng WIP. Giữ **interface** `GraphRenderer`
(checkpoint #2 của ROADMAP: đổi canvas↔SVG là sửa một tệp) — cạnh WIP đi qua tham số
của `draw`, **không** thêm phương thức đặc thù canvas. `GraphCanvas.tsx` vẫn **không**
gọi `getContext` (có cổng ghim điều đó).

⚠️ **Cập nhật cổng "đúng một lời gọi useVirtualizer"** nếu nó đếm số tệp/số khớp —
cùng loại với cổng pathspec của `diff.rs`: sửa **con số**, đừng nới thành `>=`.

**CSS**: chỉ thêm khối mới ở cuối `app.css`. Hàng WIP cao **đúng** `ROW_HEIGHT` (28px)
— đây là nơi Phase 2 đã hỏng hai lần (badge phá chiều cao hàng → đồ thị lệch). Không
padding/border làm hàng cao hơn 28px.

⚠️ Mọi khẳng định bố cục ở đây là **chưa kiểm chứng** (happy-dom không tính CSS). Test
chỉ chứng minh **số** khớp nhau; nó **không** chứng minh mắt người thấy thẳng hàng.
Task 2 mới làm việc đó.
  </action>
  <verify>
    <automated>rm -f .vitest/json/output.json &amp;&amp; npx vitest run --reporter=json --outputFile=.vitest/json/output.json &amp;&amp; node -e "const r=require('./.vitest/json/output.json');if(r.numFailedTests>0)process.exit(1);console.log('tests',r.numTotalTests)"</automated>
    <automated>npx tsc --noEmit &amp;&amp; npm run build</automated>
    <automated>node -e "
const fs=require('fs'),path=require('path');
const dir='src/components/history';
let n=0,files=[];
for(const f of fs.readdirSync(dir)){
  if(!/\.tsx?$/.test(f)||/\.test\./.test(f))continue;
  const src=fs.readFileSync(path.join(dir,f),'utf8')
    .split('\n').filter(l=>!l.trim().startsWith('//')).join('\n');
  const c=(src.match(/useVirtualizer\(/g)||[]).length;
  if(c>0){n+=c;files.push(f+':'+c);}
}
if(files.length===0){console.error('CỔNG ĐỎ: không thấy useVirtualizer nào — tiền đề sai');process.exit(1);}
if(n!==1){console.error('CỔNG ĐỎ: '+n+' lời gọi useVirtualizer',files);process.exit(1);}
console.log('OK: đúng 1 lời gọi useVirtualizer',files);
"</automated>
  </verify>
  <done>
Frontend tổng **>** số của 04-04, 0 đỏ, **3 lần chạy liên tiếp**. `tsc` và `build`
sạch. Cổng `useVirtualizer` in `OK: đúng 1` (và cổng **khẳng định tiền đề** — đỏ nếu
không tìm thấy lời gọi nào, chống lỗi #3 của CONTEXT.md 3.1).

**Không** test cũ nào của `CommitList.test.tsx` bị sửa để cho xanh.
  </done>
</task>

<task type="auto">
  <name>Task 2: Nối ChangeList + CommitBox vào App, rồi dựng bản release</name>
  <files>src/App.tsx, src/styles/app.css, docs/09-phase4-dogfood.md</files>
  <action>
Nối `ChangeList` (04-02) và `CommitBox` (04-03) vào `App.tsx`, và nối `statusStore`
vào vòng đời mở/đóng repo (`refresh` khi mở, watcher của 04-04 khi đang mở,
`commitStore.hydrate` để nháp hiện lại).

**Quyết định bố cục** — theo tiền lệ của 03-04 đã ghi trong `App.tsx`: vùng `main`
đổi thứ render bên trong chứ **không** đổi `AppLayout.tsx`. Vùng soạn commit + ba nhóm
tệp chiếm vùng `detail` hoặc `main` tuỳ lựa chọn; **ghi lý do** vào doc comment như
`App.tsx` đã làm cho diff viewer. 🔴 Đây là **thay đổi bố cục**, và Phase 2 + Phase 3
cho thấy bố cục là chỗ hay sai nhất (5 lỗi hiển thị Phase 3, 3 lỗi Phase 2) — nên nó
là **bước 1** của checkpoint Task 3 để chủ dự án xác nhận hoặc chọn khác.

Sau khi mã xong, dựng **release** và ghi dấu thời gian:
```bash
npm run tauri:build
ls -l --time-style=full-iso src-tauri/target/release/git-plum.exe
```
🔴 **Quy tắc 3.7 của CONTEXT.md:** đã xảy ra việc chủ dự án kiểm một exe **cũ hơn bản
sửa 32 phút**. Mỗi vòng checkpoint kết thúc bằng một bản dựng release, và lần giao lại
**phải dán dấu thời gian tệp exe**. Không dán = checkpoint không hợp lệ.

Tạo `docs/09-phase4-dogfood.md` theo khuôn `docs/08-phase3-dogfood.md`: khung ghi chép
cho exit gate, có chỗ dán dấu thời gian exe và commit sha của bản dựng.
  </action>
  <verify>
    <automated>npm run build &amp;&amp; npm run tauri:build 2>&amp;1 | tail -5</automated>
    <automated>ls -l --time-style=full-iso src-tauri/target/release/git-plum.exe</automated>
  </verify>
  <done>
`git-plum.exe` tồn tại, dấu thời gian **sau** commit cuối của plan này (dán cả hai vào
SUMMARY để so được). `docs/09-phase4-dogfood.md` tồn tại.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: CHECKPOINT hiển thị — 12 bước, chủ dự án chạy bản release</name>
  <what-built>
Vòng commit đầy-cuối: ba nhóm tệp, stage/unstage theo tệp, vùng soạn thông điệp có
công tắc `no-verify`, amend có cảnh báo, watcher tự cập nhật, và hàng WIP ở đầu đồ thị.

**Bản dựng:** `src-tauri/target/release/git-plum.exe`, dấu thời gian dán ở SUMMARY.
Chạy **từ Explorer**, không phải `tauri dev` (tiêu chí 5 của Phase 1: không cửa sổ
console đen nào nháy lên, chỉ thấy ở bản release).
  </what-built>
  <how-to-verify>
Mở repo `dau-tri-toan-hoc` (1140 commit, 39 merge — đã xác nhận có trên đĩa).
⚠️ `D:/MyCompanyProjects/quanly-truong-phong-so` là repo làm việc thật của chủ dự án:
**chỉ đọc**, đừng commit vào nó ở các bước dưới.

1. **Bố cục** — ba nhóm tệp và vùng soạn commit ở đâu? Có chỗ không? Có che mất đồ thị
   hay danh sách commit không? (Đây là lựa chọn của Task 2 — xác nhận hoặc yêu cầu đổi.)
2. **Ba nhóm** — sửa 2 tệp, `git add` 1 tệp ở terminal, tạo 1 tệp mới. Ứng dụng có
   hiện đúng ba nhóm với đúng tệp trong từng nhóm?
3. **Chọn tệp → diff** — bấm một tệp trong nhóm "chưa stage". Có thấy nội dung thay
   đổi của **tệp đó**? (Trình xem diff Phase 3 chưa ai dùng thật — lỗi ở đây là lỗi
   Phase 3 lộ ra, ghi riêng.)
4. **Stage/unstage** — bấm stage một tệp. Nó chuyển nhóm **ngay**? Bấm unstage, về lại?
   Có phải bấm làm mới ở đâu không? (Không được phải bấm.)
5. 🔴 **Hàng WIP thẳng cột** — có thay đổi chưa commit, đồ thị hiện hàng ở trên cùng?
   Chấm/cạnh của nó **thẳng** với dòng chữ của nó? **Cuộn xuống rồi lên lại vài lần** —
   cột đồ thị có lệch cột văn bản **một hàng** ở bất kỳ vị trí nào? (Lớp lỗi này đã
   xảy ra **hai lần** ở Phase 2. Đây là bước quan trọng nhất của checkpoint.)
6. 🔴 **Cạnh hàng WIP** — hàng WIP có một đường nối **xuống commit HEAD**, hay nó là
   một chấm trôi không cạnh? Đường đó nối vào **đúng** commit đang là HEAD?
7. **Số đếm** — hàng WIP hiện số tệp sửa và tệp mới. So với nhóm tệp ở bước 2: khớp?
8. **Bấm hàng WIP** — mở vùng soạn commit?
9. **Commit** — gõ thông điệp, bấm commit. Commit xuất hiện trên đồ thị **ngay**? Hàng
   WIP **biến mất** (nếu đã commit hết)? Ô soạn rỗng lại?
10. **Nháp** — gõ nửa thông điệp, **đóng ứng dụng**, mở lại. Nháp còn? Rồi mở repo
    khác, quay lại: nháp của repo đầu còn và repo kia **không** thấy nháp đó?
11. 🔴 **Watcher** — ứng dụng đang mở, ở terminal chạy `git add` rồi `git checkout` một
    nhánh khác. Giao diện tự cập nhật trong **dưới một giây**, không bấm gì? (Tiêu chí
    5 — chỉ kiểm được bằng cách này.)
12. **Amend** — bấm amend commit vừa tạo, sửa thông điệp. Kết quả đúng? Nếu commit đã
    push thì có **cảnh báo** — và nút **vẫn bấm được** (cảnh báo, **không** chặn)?

Cộng thêm, không đánh số: có cửa sổ console đen nào nháy lên trong suốt 12 bước?
  </how-to-verify>
  <resume-signal>
Gõ "đã duyệt", hoặc mô tả từng vấn đề kèm **bước số mấy**.

🔴 Cổng này **không tự phê duyệt được**. Chủ dự án đã chọn "dừng lại, báo tôi" cho cổng
bắt buộc. Executor **không** được ghi "đạt" cho bước nào dựa trên test tự động —
happy-dom không tính layout và không có cuộn thật, nên bước 5 và 6 **không thể** kiểm
bằng test. Nếu có vấn đề: sửa, dựng lại release, dán dấu thời gian **mới**, chạy lại
checkpoint từ bước 1.
  </resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: EXIT GATE — một commit thật vào một repository thật, chỉ bằng git-plum</name>
  <what-built>
Bản release đã qua checkpoint Task 3. Đây là điều kiện **đóng phase** theo ROADMAP.
  </what-built>
  <how-to-verify>
> **Exit gate (dogfood, bắt buộc)**: Một commit thật vào một repository thật, tạo ra
> **chỉ bằng git-plum**.

Khác checkpoint Task 3: đó là 12 bước theo danh sách; đây là **làm một việc thật**.
Chọn một thay đổi thật đang cần commit, rồi commit nó **chỉ bằng git-plum** — không
`git add`, không `git commit` ở terminal, không mở công cụ khác giữa đường.

Ghi vào `docs/09-phase4-dogfood.md`:
- Repo nào, commit sha, thông điệp
- Có phải chuyển sang terminal giữa đường không, và **vì sao** (đây là dữ liệu quan
  trọng nhất của gate: mỗi lần phải chuyển là một thứ còn thiếu)
- Cảm nhận: chỗ nào chậm, chỗ nào phải nghĩ, chỗ nào bấm sai
- Dấu thời gian exe đã dùng

Xác nhận commit tồn tại bằng `git rev-parse HEAD` và `git rev-list --count HEAD`.
🔴 **Không** `git log`: trần cứng 50 dòng ở môi trường này, **không** phụ thuộc cách
gọi, và thất bại **im lặng** (exit 1, 0 khớp — không phân biệt được với commit không
tồn tại). Một session khác đã kết luận sai rằng ba commit của nó bị xoá vì dùng
`git log | grep <sha>`.
  </how-to-verify>
  <resume-signal>
Gõ "đã duyệt" kèm commit sha, hoặc mô tả chỗ bị kẹt.

🔴 **Không tự phê duyệt được.** ROADMAP và CONTEXT.md mục 5: kết quả đúng khi cổng
không đóng được là **một plan vá khoảng cách**, **không** phải đi tiếp Phase 5.
  </resume-signal>
</task>

<task type="auto">
  <name>Task 5: VERIFICATION.md — ghi trạng thái thật, kèm loại bằng chứng</name>
  <files>.planning/phases/04-commit-loop/VERIFICATION.md</files>
  <action>
Theo khuôn `.planning/phases/03-diff-viewer/VERIFICATION.md`. Với **mỗi** requirement
(WORK-01, 02, 08, 09, 10, 11) và **mỗi** trong bảy tiêu chí thành công, ghi:

| Mục | Trạng thái | **Loại bằng chứng** |
|---|---|---|

🔴 **Cột "loại bằng chứng" là cột quan trọng nhất của tệp này.** Phase 3 có cả 5 wave
xanh và **0 requirement nào có bằng chứng từ mắt người** — và điều đó chỉ lộ ra khi
`VERIFICATION.md` buộc phải phân loại. Ba giá trị hợp lệ:
- `test tự động` — có test, chưa ai xem
- `mắt người (release)` — chủ dự án đã xem trên bản release, **ghi dấu thời gian exe**
- `chưa kiểm` — không có bằng chứng nào

Quy tắc cứng, từ CONTEXT.md 3.4: mọi khẳng định về **bố cục** (chiều cao hàng WIP,
thẳng cột đồ thị/văn bản, ba nhóm không tràn) mà chỉ có test happy-dom phải ghi
**`chưa kiểm`**, không phải `đạt`. happy-dom không tính CSS layout và không có cuộn
thật.

Nếu Task 3 hoặc Task 4 phát hiện vấn đề chưa sửa: ghi **Pending** kèm mô tả, và nêu
rằng phase **chưa đóng**. Đừng ghi "xong" cho một phase còn cổng đỏ — Phase 3 đã bị
đúng vấn đề đó và ROADMAP phải sửa lại sau.

Ghi thêm **nợ mang vào từ Phase 1–3** (bảng ở mục 0 của CONTEXT.md) để nó không mất
dấu: QA Phase 1 chưa kiểm, checkpoint 02-06 vòng 2, checkpoint #1 phần người thấy,
checkpoint 03-04, exit gate 03-05.
  </action>
  <verify>
    <automated>test -f .planning/phases/04-commit-loop/VERIFICATION.md &amp;&amp; grep -c "WORK-" .planning/phases/04-commit-loop/VERIFICATION.md</automated>
  </verify>
  <done>
`VERIFICATION.md` có cả 6 requirement và cả 7 tiêu chí, mỗi mục có **loại bằng
chứng**. Mục bố cục chỉ có test happy-dom ghi `chưa kiểm`. Nợ Phase 1–3 được nhắc lại.
  </done>
</task>

</tasks>

<verification>
- [ ] Task 0 (cổng tệp in-flight) đã chạy và cho kết quả **sạch**
- [ ] Cổng `useVirtualizer` in `OK: đúng 1`, và cổng **khẳng định tiền đề** (đỏ nếu không thấy lời gọi nào)
- [ ] Frontend xanh, tổng tăng, **3 lần chạy**, `rm -f .vitest/json/output.json` trước mỗi lần
- [ ] `npx tsc --noEmit` sạch; `npm run build` sạch; `npm run tauri:build` thành công
- [ ] `cargo clippy --all-targets`: vẫn đúng **1** warning cũ (`diff.rs:1470`)
- [ ] Dấu thời gian `git-plum.exe` dán vào SUMMARY, **sau** commit cuối
- [ ] Checkpoint Task 3: chủ dự án đã duyệt, hoặc vấn đề đã ghi theo **số bước**
- [ ] Exit gate Task 4: commit sha thật, hoặc chỗ kẹt đã ghi
- [ ] `VERIFICATION.md` có cột **loại bằng chứng** cho mọi mục
- [ ] Không test cũ nào bị sửa để cho xanh

**Không** `npm run lint` — script đó **không tồn tại** trong `package.json`.
**Không** `git log` cho câu hỏi tồn tại/reachability — trần cứng 50 dòng, thất bại im
lặng. Dùng `git rev-parse`, `git rev-list --count`, `git merge-base --is-ancestor`.
</verification>

<success_criteria>
Hàng WIP hiện ở đầu đồ thị, **thẳng cột** với văn bản ở mọi vị trí cuộn (mắt người xác
nhận, không phải test), nối **đúng** xuống HEAD, và biến mất khi không còn thay đổi.

Chủ dự án tạo được **một commit thật** vào một repository thật **chỉ bằng git-plum**.

`VERIFICATION.md` nói **thật** về loại bằng chứng của từng mục — kể cả khi câu trả lời
là "chưa kiểm". Đó là thứ Phase 3 thiếu và phải sửa lại sau.
</success_criteria>

<output>
Tạo `.planning/phases/04-commit-loop/04-05-SUMMARY.md`.

Ghi bắt buộc: kết quả cổng Task 0; dấu thời gian exe; kết quả **từng bước** của
checkpoint 12 bước; kết quả exit gate kèm commit sha; và — nếu có cổng đỏ — đề xuất
**một plan vá khoảng cách**, không phải đề xuất đi tiếp Phase 5.
</output>
