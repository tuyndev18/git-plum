---
phase: 02-history-graph
plan: 06
type: execute
wave: 6
depends_on: [05]
files_modified:
  - src/stores/selectionStore.ts
  - src/stores/refsStore.ts
  - src/components/history/CommitDetail.tsx
  - src/components/history/FileList.tsx
  - src/components/history/RefBadges.tsx
  - src/components/history/CommitSearch.tsx
  - src/components/RefSidebar.tsx
  - src/components/history/CommitList.tsx
  - src/lib/fileTree.ts
  - src/App.tsx
  - src/styles/app.css
autonomous: false
requirements: [HIST-06, HIST-07, HIST-08, HIST-09, HIST-10]
user_setup: []

must_haves:
  truths:
    - "Chọn một commit thấy ngay tiêu đề, nội dung đầy đủ, tác giả, thời gian, mã cha và danh sách tệp"
    - "Bấm một nút chuyển danh sách tệp giữa dạng đường dẫn phẳng và dạng cây thư mục"
    - "Nhãn nhánh và tag hiện trên đúng hàng commit, phân biệt được local với remote"
    - "Thanh bên liệt kê nhánh local, nhánh remote và tag kèm số đếm từng loại"
    - "Gõ một từ khoá tìm được commit theo thông điệp, tên tác giả, mã commit hoặc đường dẫn tệp"
  artifacts:
    - path: "src/stores/selectionStore.ts"
      provides: "Chỉ giữ selectedCommitId — tách chọn khỏi dữ liệu đồ thị"
      contains: "selectedCommitId"
    - path: "src/components/history/CommitDetail.tsx"
      provides: "Vùng chi tiết commit — HIST-08"
    - path: "src/lib/fileTree.ts"
      provides: "Hàm thuần: string[] -> cây thư mục. HIST-09"
      exports: ["buildFileTree"]
    - path: "src/components/RefSidebar.tsx"
      provides: "Thanh bên nhánh/tag kèm số đếm — HIST-07"
    - path: "src/components/history/CommitSearch.tsx"
      provides: "Hộp tìm kiếm — HIST-10"
  key_links:
    - from: "src/components/history/CommitDetail.tsx"
      to: "src/stores/selectionStore.ts"
      via: "đọc selectedCommitId, tự nạp chi tiết"
      pattern: "selectedCommitId"
    - from: "src/components/history/RefBadges.tsx"
      to: "src/stores/refsStore.ts"
      via: "tra ref theo mã commit của hàng"
      pattern: "byCommit|refsFor"
    - from: "src/components/history/CommitSearch.tsx"
      to: "src/lib/ipc.ts"
      via: "ipc.searchCommits có trì hoãn"
      pattern: "searchCommits"
---

<objective>
Năm requirement còn lại của giao diện: chi tiết commit, dạng cây/phẳng, nhãn nhánh-tag, thanh
bên, và tìm kiếm. Sau plan này cả sáu tiêu chí thành công của phase đều **có mã để kiểm**.

Một quyết định kiến trúc bắt buộc tuân thủ: **`selectionStore` chỉ giữ `selectedCommitId`,
không giữ dữ liệu commit** (ARCHITECTURE.md Pattern 3). Vùng chi tiết tự đọc id rồi tự lấy
dữ liệu. Nghiên cứu nêu tên phản mẫu: *"Không dựng lại toàn bộ graph khi chọn commit khác"* —
nhét dữ liệu commit vào store chọn là đường ngắn nhất tới việc vẽ lại cả đồ thị mỗi lần bấm,
và đó là mất Core Value ở chỗ dễ mất nhất.

Purpose: HIST-06, HIST-07, HIST-08, HIST-09, HIST-10 và tiêu chí thành công 3, 4, 5.
Output: vùng chi tiết, thanh bên, nhãn ref, hộp tìm kiếm — tất cả nối vào ứng dụng thật.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-history-graph/CONTEXT.md
@.planning/phases/02-history-graph/02-04-SUMMARY.md
@.planning/phases/02-history-graph/02-05-SUMMARY.md
@src/lib/ipc.ts
@src/stores/historyStore.ts
@src/components/history/CommitList.tsx
@src/App.tsx
@src/lib/commands.ts
@docs/screenshots/main-1.png

<interfaces>
Hợp đồng đã có từ plan 02-04 và 02-05 — dùng trực tiếp, không đi tìm:

Từ `src/lib/ipc.ts`:
```typescript
export interface Commit { id; parents: string[]; authorName; authorEmail; authorTime: number;
  committerName; committerEmail; committerTime: number; subject; body; hasInvalidUtf8: boolean }
export interface FileChange { status: string; path: string; oldPath: string | null }
export interface CommitDetail { commit: Commit; files: FileChange[]; truncated: boolean }
export type RefKind = 'localBranch' | 'remoteBranch' | 'tag' | 'other'
export interface GitRef { fullName; shortName; kind: RefKind; target;
  upstream: string | null; ahead: number; behind: number; isHead: boolean }
export const ipc = {
  getCommitPage: (repoId, skip, limit) => invoke<CommitPage>(...),
  listRefs: (repoId) => invoke<GitRef[]>(...),
  getCommitDetail: (repoId, commitId) => invoke<CommitDetail>(...),
  searchCommits: (repoId, query) => invoke<string[]>(...),
}
export function describeError(e: unknown): string
```
`GitRef.target` là **mã commit** — đây là cách neo nhãn vào hàng (HIST-06).

Từ `src/stores/historyStore.ts` (plan 02-05):
```typescript
interface HistorySlice { commits: Commit[]; graphRows: GraphRow[]; total: number;
  loadedRanges: Array<[number, number]>; isLoading: boolean; error: string | null }
export const useHistoryStore = create<HistoryState>(...)   // byRepo[repoId]
```

Từ `src/lib/commands.ts`:
```typescript
export type CommandCategory = 'repo' | 'view' | 'history' | 'branch' | 'help'
export function registerCommands(commands: Command[]): void   // Command.run KHÔNG nhận tham số
export async function runCommand(id: string): Promise<void>
```
Thao tác **có tham số** (ví dụ "chọn commit này") truyền handler qua prop, không nhồi vào sổ
đăng ký — xem ghi chú dài trong `src/App.tsx` giải thích lý do.
</interfaces>

**Bài học Phase 1 áp dụng:**
- `vi.clearAllMocks()` xoá `mockResolvedValue` đặt trong factory `vi.mock`; dựng lại mặc định
  trong `beforeEach`. `restoreMocks: true` đang bật trong `vite.config.ts`.
- Giả lập IPC ở `vi.mock('@/lib/ipc')`, không vá `invoke` toàn cục.
- Test logic store gọi `useXStore.getState()`, không render component.

**Định dạng thời gian:** `authorTime` là giây Unix. Dùng `Intl.DateTimeFormat` (quyết định đã
chốt: **không** `chrono` bên Rust, không thư viện ngày tháng bên JS). Nhân 1000 để ra
milliseconds — quên bước này thì mọi commit hiện năm 1970.

**Ảnh tham chiếu** `docs/screenshots/main-1.png`: học bố cục (cột BRANCH/TAG bên trái cột
GRAPH, bảng chi tiết bên phải). **Tự thiết kế phần nhìn.**

**Ranh giới phạm vi (CONTEXT.md `<scope_boundary>`):** danh sách tệp thay đổi thì **có**, nhưng
**bấm vào tệp để xem nội dung khác biệt là Phase 3**. Hàng tệp ở plan này không được có hành
vi bấm dẫn tới diff. Không blame, không file history.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: selectionStore + refsStore + cây tệp thuần</name>
  <files>src/stores/selectionStore.ts, src/stores/refsStore.ts, src/lib/fileTree.ts</files>
  <behavior>
`selectionStore`:
    - `select(repoId, commitId)` → `selectedByRepo[repoId] === commitId`.
    - Chọn ở repo A không ảnh hưởng repo B (PLAT-05).
    - `clear(repoId)` → `null`.
    - Store **không** có trường nào chứa `Commit`, `files` hay `graphRows` — test khẳng định
      bằng cách soát khoá của `getState()`. Đây là cách duy nhất giữ được Pattern 3 qua thời gian.
`refsStore`:
    - `load(repoId)` → `byRepo[repoId].refs` có dữ liệu; `counts` trả đúng số local/remote/tag.
    - `refsForCommit(repoId, commitId)` → chỉ những ref có `target === commitId`.
    - Hai ref cùng trỏ một commit (nhánh + tag) → trả cả hai.
    - Commit không có ref nào → mảng rỗng, không `undefined`.
    - `load` thất bại → `error` được đặt, không ném ra ngoài.
`buildFileTree` (HIST-09, hàm thuần):
    - `['a.txt']` → một nút tệp ở gốc.
    - `['src/a.ts', 'src/b.ts']` → một nút thư mục `src` chứa hai tệp.
    - `['src/x/y.ts', 'src/z.ts']` → `src` chứa thư mục `x` và tệp `z.ts`; lồng đúng độ sâu.
    - Đường dẫn chứa ký tự Unicode và khoảng trắng → giữ nguyên, không tách sai.
    - Đường dẫn chứa `\` (Windows) → **không** tách theo `\`; git luôn trả `/`. Test khẳng
      định `a\b.txt` là **một** tệp tên `a\b.txt`, không phải thư mục `a`.
    - Mảng rỗng → cây rỗng, không lỗi.
    - Thư mục sắp trước tệp, mỗi nhóm sắp theo tên — thứ tự tất định để test snapshot được.
  </behavior>
  <action>
`src/stores/selectionStore.ts` — zustand, `selectedByRepo: Record<string, string | null>`,
hành động `select(repoId, commitId)` và `clear(repoId)`.
Doc comment đầu tệp ghi rõ, đủ mạnh để chặn người sửa sau: store này **chỉ** giữ id.
Thêm `Commit` vào đây nghĩa là mỗi lần chọn commit sẽ làm mọi thành phần đọc store render
lại, gồm cả `CommitList` — và đó chính là phản mẫu "dựng lại toàn bộ graph khi chọn commit
khác" mà nghiên cứu nêu tên. Vùng chi tiết tự lấy dữ liệu từ `historyStore` hoặc IPC.

Nếu plan 02-05 đã dùng `useState` cho trạng thái chọn (xem `02-05-SUMMARY.md`), nâng lên store
này và sửa `App.tsx` + `CommitList.tsx` cho khớp.

`src/stores/refsStore.ts` — `byRepo[repoId] = { refs: GitRef[], isLoading, error }`, cộng:
- `load(repoId)` gọi `ipc.listRefs`.
- Một chỉ mục dẫn xuất `Map<commitId, GitRef[]>` tính **một lần** khi `refs` đổi (không quét
  cả mảng `refs` cho từng hàng — với 100k hàng × hàng nghìn ref thì đó là O(n·m) trong lúc
  cuộn). Tính bằng `useMemo` ở phía component hoặc lưu sẵn trong slice; chọn lưu sẵn trong
  slice thì đơn giản hơn và có test được.
- `refsForCommit(repoId, commitId): GitRef[]` đọc từ chỉ mục đó.
- `counts(repoId)` → `{ localBranches, remoteBranches, tags }` cho HIST-07.

`src/lib/fileTree.ts` — hàm **thuần**, không React:
```typescript
export type FileTreeNode =
  | { kind: 'dir'; name: string; path: string; children: FileTreeNode[] }
  | { kind: 'file'; name: string; path: string; change: FileChange }
export function buildFileTree(files: FileChange[]): FileTreeNode[]
```
Tách theo `/` **duy nhất** — git luôn dùng `/` kể cả trên Windows. Tách theo `\` sẽ hiểu sai
một tên tệp hợp lệ chứa dấu gạch chéo ngược thành thư mục. Có test cho ca này.
  </action>
  <verify>
    <automated>npm run typecheck &amp;&amp; npm test -- src/stores/selectionStore src/stores/refsStore src/lib/fileTree</automated>
  </verify>
  <done>
17 ca `<behavior>` có test riêng và đỗ. Test khẳng định `selectionStore.getState()` **không**
có khoá nào chứa dữ liệu commit. `refsStore` có chỉ mục theo commit, không quét tuyến tính
mỗi hàng. `buildFileTree` không tách theo `\`. `npm run typecheck` 0.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Vùng chi tiết commit + danh sách tệp phẳng/cây</name>
  <files>src/components/history/CommitDetail.tsx, src/components/history/FileList.tsx, src/styles/app.css</files>
  <behavior>
`CommitDetail` (HIST-08), giả lập `@/lib/ipc`:
    - `selectedCommitId` là `null` → hiện trạng thái rỗng đọc được ("Chọn một commit"), không lỗi.
    - Có id → hiện `subject`, **toàn bộ** `body` (không cắt), `authorName`, `authorEmail`,
      thời gian đã định dạng, và mã commit đầy đủ.
    - Commit hai cha → hiện **cả hai** mã cha. Commit **bốn** cha → hiện **cả bốn**. Đây là
      HIST-08 gặp ràng buộc octopus: chi tiết không được chỉ hiện `parents[0]` và `parents[1]`.
    - Commit gốc (`parents` rỗng) → hiện nhãn "commit gốc", không hiện danh sách cha rỗng.
    - `getCommitDetail` ném lỗi → hiện thông báo qua `describeError`, không trắng vùng.
    - `CommitDetail.truncated === true` → hiện cảnh báo danh sách tệp bị cắt kèm số.
    - Đổi `selectedCommitId` → gọi `getCommitDetail` cho id mới; chọn **lại cùng** id → **không**
      gọi lại (có cache theo commitId trong component/store; diff của commit lịch sử là bất biến).
`FileList` (HIST-09):
    - Mặc định dạng phẳng: mỗi `FileChange` một hàng, hiện `status` và `path` đầy đủ.
    - Bấm nút chuyển → dạng cây: thấy tên thư mục, và tệp hiện tên ngắn không kèm đường dẫn cha.
    - Bấm lại → về dạng phẳng. Trạng thái chọn dạng **còn nguyên** khi đổi sang commit khác
      (người dùng đã chọn cách xem, không phải chọn lại mỗi lần).
    - Tệp đổi tên (`oldPath` khác `null`) → hiện cả đường dẫn cũ và mới.
    - Danh sách tệp rỗng → thông báo đọc được, không bảng rỗng.
    - Đường dẫn chứa byte đã giải mã lossy (ký tự U+FFFD) → vẫn hiện một hàng (HIST-11).
    - Hàng tệp **không** có `onClick` dẫn tới diff — ranh giới phạm vi Phase 3. Test khẳng
      định không có thuộc tính đó, để việc "thêm tạm cho tiện" không lọt qua.
  </behavior>
  <action>
`src/components/history/CommitDetail.tsx` — vùng `detail` của `AppLayout`.
- Đọc `selectedCommitId` từ `selectionStore` và `activeRepoId` từ `repoStore`.
- Lấy metadata `Commit`: **ưu tiên** `historyStore.byRepo[repoId].commits` (đã có sẵn trong
  trang — ARCHITECTURE.md: chọn commit là **không** IPC nếu metadata đã nạp). Chỉ gọi
  `ipc.getCommitDetail` cho **danh sách tệp**, thứ mà trang commit không có.
- Cache `CommitDetail` theo `commitId` trong một `Map` cấp module hoặc một store nhỏ, có chặn
  trên số mục (ví dụ 200). Diff/danh sách tệp của commit lịch sử **bất biến**, nên cache này
  không bao giờ cần vô hiệu hoá (ARCHITECTURE.md bảng cache). Ghi điều đó vào doc comment.
- Bố cục: tiêu đề (`subject`), thân (`body` trong `<pre>` giữ xuống dòng), bảng metadata
  (tác giả, email, thời gian tác giả, người commit nếu khác tác giả, thời gian commit), danh
  sách mã cha (mỗi cha một mục, bấm được để **chọn** cha đó — dùng `selectionStore.select`,
  đây là điều hướng trong Phase 2 chứ không phải xem diff), rồi `<FileList>`.
- `body` hiện trong `<pre>` hoặc `white-space: pre-wrap`, **không** `dangerouslySetInnerHTML`.

`src/components/history/FileList.tsx`:
- Props: `files: FileChange[]`, `truncated: boolean`.
- Nút chuyển phẳng/cây. Trạng thái lưu ở một store giao diện nhỏ hoặc `uiStore` — **không**
  `useState` cục bộ, vì nó phải sống qua việc đổi commit (yêu cầu ở `<behavior>`).
  Đăng ký lệnh `history.toggleFileView` qua sổ đăng ký (PLAT-04), category `'history'`, và nút
  gọi `runCommand('history.toggleFileView')` theo quy ước dự án.
- Dạng cây dùng `buildFileTree`. Thư mục gập/mở được, mặc định mở hết (danh sách tệp của một
  commit thường nhỏ).
- `status` hiện thành nhãn đọc được: `A`→Thêm, `M`→Sửa, `D`→Xoá, `R`→Đổi tên, `C`→Sao chép,
  `T`→Đổi kiểu. Ký tự lạ → hiện nguyên ký tự, không bỏ hàng.
- **Không** `onClick` trên hàng tệp (Phase 3).

CSS trong `src/styles/app.css`. Tự thiết kế phần nhìn.
  </action>
  <verify>
    <automated>npm run typecheck &amp;&amp; npm test -- src/components/history/CommitDetail src/components/history/FileList</automated>
  </verify>
  <done>
13 ca `<behavior>` có test riêng và đỗ. Test bốn cha khẳng định **bốn** mã cha hiện ra. Test
khẳng định hàng tệp không có handler bấm. `/usr/bin/grep -rc 'dangerouslySetInnerHTML' src/`
→ `0`. `npm run typecheck` 0.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Nhãn ref trên hàng commit + thanh bên + tìm kiếm</name>
  <files>src/components/history/RefBadges.tsx, src/components/RefSidebar.tsx, src/components/history/CommitSearch.tsx, src/components/history/CommitList.tsx, src/styles/app.css</files>
  <behavior>
`RefBadges` (HIST-06):
    - Commit có `refs/heads/main` → nhãn `main` với class của **nhánh local**.
    - Commit có `refs/remotes/origin/main` → nhãn `origin/main` với class của **nhánh remote**,
      **khác** class của local. Test so class, không so màu — màu là chuyện CSS.
    - Commit có tag → nhãn kiểu tag, khác cả hai loại trên.
    - `isHead === true` → có thêm dấu hiệu HEAD.
    - Commit không ref nào → **không render gì**, và **không** chiếm chiều cao (chiếm chiều cao
      là lệch hàng — ràng buộc HIST-04).
    - Commit có 12 ref → hiện một số nhãn rồi chỉ báo `+N`, không làm hàng cao lên hay tràn.
`RefSidebar` (HIST-07):
    - Ba nhóm: Nhánh local, Nhánh remote, Tag — mỗi nhóm có **số đếm** trong tiêu đề.
    - Nhóm rỗng vẫn hiện tiêu đề với số `0` (người dùng biết là rỗng, không phải bị thiếu).
    - Nhánh có `ahead`/`behind` khác 0 → hiện hai số đó.
    - Nhánh là HEAD → có dấu hiệu riêng.
    - HEAD tách rời (không ref nào `isHead`) → hiện một dòng nói rõ đang ở HEAD tách rời.
      Đây là ca biên thứ năm của nghiên cứu mục 4 ở tầng giao diện.
    - Bấm một nhánh → gọi `onSelectCommit` với `ref.target` (điều hướng tới commit đó). Phase 2
      **không** checkout — đó là Phase 6.
`CommitSearch` (HIST-10):
    - Gõ vào hộp → **không** gọi `searchCommits` ngay; chỉ gọi sau khi ngừng gõ (trì hoãn ~250ms).
      Test bằng `vi.useFakeTimers`. ARCHITECTURE.md Anti-Pattern 4 cấm chạy git mỗi lần gõ.
    - Xoá hộp về rỗng → xoá kết quả, không gọi IPC.
    - Kết quả có mã commit → hiện số lượng khớp và cho nhảy tới khớp kế tiếp/trước đó.
    - Không khớp → thông báo "không tìm thấy", không bảng rỗng im lặng.
    - `searchCommits` ném lỗi → hiện lỗi, hộp vẫn gõ được.
    - Nhảy tới một khớp → gọi `scrollToIndex` của virtualizer với đúng chỉ số hàng của mã đó.
  </behavior>
  <action>
`src/components/history/RefBadges.tsx` — nhãn hiển thị trong hàng của `CommitList`:
- Props: `refs: GitRef[]`. Không tự gọi store (được truyền vào từ hàng) — hàng render tới hàng
  nghìn lần khi cuộn, mỗi component tự subscribe store là chi phí vô ích.
- Ba class theo `kind`: `.ref-badge--local`, `.ref-badge--remote`, `.ref-badge--tag`. HIST-06
  đòi **phân biệt được** local với remote, nên đây là hai class khác nhau, không phải một
  class khác màu qua inline style.
- `refs` rỗng → `return null`.
- Nhiều hơn N nhãn (ví dụ 3) → hiện N rồi `+{còn lại}` có `title` liệt kê đủ.

`src/components/history/CommitList.tsx` — thay đổi **tối thiểu**: thêm `<RefBadges>` vào cột
văn bản của hàng, lấy ref qua `refsStore.refsForCommit`. **Không** đụng tới `useVirtualizer`,
không đụng vùng cuộn, không đụng `GraphCanvas`. Nhãn nằm **trong** `div` của hàng nên nó tự
thẳng hàng; chiều cao hàng **không** được đổi theo số nhãn — đặt `overflow: hidden` và chiều
cao cố định `ROW_HEIGHT` trên hàng. Nhãn làm hàng cao lên là cách trực tiếp nhất phá HIST-04,
và test "không chiếm chiều cao" ở `<behavior>` tồn tại vì thế.

`src/components/RefSidebar.tsx` — vùng `sidebar` của `AppLayout`. Đọc `refsStore`. Ba nhóm gập
được, mỗi nhóm `{tên} ({số})`. Nhánh hiện `shortName` cộng `↑{ahead} ↓{behind}` khi khác 0.
Dòng HEAD tách rời khi không ref nào `isHead`.

`src/components/history/CommitSearch.tsx` — HIST-10:
- Hộp nhập cộng nút trước/sau cộng bộ đếm `{i}/{n}`.
- Trì hoãn 250ms (`setTimeout` + dọn trong `useEffect`). Huỷ lời gọi đang bay khi có lời gọi
  mới (giữ một số thứ tự và bỏ kết quả cũ) — cuộn/gõ nhanh không được làm kết quả về ngược thứ tự.
- Kết quả là `string[]` mã commit. Đổi thành chỉ số hàng bằng cách tra trong
  `historyStore.byRepo[repoId].commits` (dựng một `Map<id, index>` một lần). Mã không có trong
  phần đã nạp → bỏ qua kèm ghi chú số lượng bỏ qua trong giao diện, vì backend tìm trên toàn
  bộ lịch sử trong cache còn giao diện có thể chưa nạp hết trang.
- `scrollToIndex` lấy từ virtualizer: `CommitList` phơi ra một `ref` bắt buộc (`useImperativeHandle`)
  hoặc nhận prop `scrollToIndexRef`. **Không** tạo virtualizer thứ hai — đây là chỗ dễ vi phạm
  ràng buộc "một virtualizer" nhất trong cả phase.
- Đăng ký lệnh `history.search` (mở/tập trung hộp tìm kiếm), keybinding `Ctrl+F`, category
  `'history'` qua sổ đăng ký.
  </action>
  <verify>
    <automated>npm run typecheck &amp;&amp; npm test -- src/components/history/RefBadges src/components/RefSidebar src/components/history/CommitSearch &amp;&amp; test "$(/usr/bin/grep -rc 'useVirtualizer' src/components/ | /usr/bin/grep -v ':0' | wc -l)" = "1" &amp;&amp; test "$(/usr/bin/grep -c 'useVirtualizer' src/components/CommitList.tsx)" = "1"</automated>
  </verify>
  <done>
18 ca `<behavior>` có test riêng và đỗ. Vẫn đúng **một** tệp chứa `useVirtualizer` trong
`src/components/` (kiểm bằng lệnh ở `<verify>` — đếm số tệp có khớp, không đếm số khớp, vì
proxy grep của shell này có thể trả mã lạ; chạy riêng nếu cần). Test trì hoãn dùng
`vi.useFakeTimers` và khẳng định **không** gọi IPC trước khi hết hạn trì hoãn.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Kiểm tiêu chí thành công 3, 4, 5, 6 trên repo thật</name>
  <what-built>
Vùng chi tiết commit (HIST-08) kèm chuyển phẳng/cây (HIST-09), nhãn nhánh-tag trên hàng
(HIST-06), thanh bên có số đếm (HIST-07), và tìm kiếm bốn trục (HIST-10). Tất cả đã nối vào
ứng dụng thật; toàn bộ 48 ca test đơn vị của plan này đã xanh.

Còn lại phần chỉ người dùng mới kết luận được: dữ liệu hiện ra có **đúng** không, và bốn tiêu
chí thành công 3, 4, 5, 6 có đạt không.
  </what-built>
  <how-to-verify>
`npm run tauri:dev` (hoặc `dev.cmd`). Mở repository của chính dự án này.

**Tiêu chí 3 — chi tiết commit.**
1. Chọn một commit thường: có đủ tiêu đề, nội dung đầy đủ, tác giả, thời gian, mã cha, và danh
   sách tệp thay đổi không? Thời gian có đúng (không phải năm 1970 — dấu hiệu quên nhân 1000)?
2. Bấm nút chuyển sang **dạng cây**: đường dẫn có gom thành thư mục không? Bấm về **phẳng**:
   trở lại đủ đường dẫn? Chọn commit khác rồi quay lại: dạng đã chọn còn nguyên chứ?
3. Mở `target/fixtures/octopus`, chọn commit merge: có hiện **cả bốn** mã cha không?
4. Chọn commit **đầu tiên** của lịch sử (commit gốc): danh sách tệp có hiện không, hay báo lỗi?

**Tiêu chí 5 — nhãn và thanh bên.**
5. Nhãn nhánh có nằm đúng **hàng** của commit mà nó trỏ tới? Nhánh local và nhánh remote trông
   **khác nhau** rõ ràng chứ?
6. Hàng có nhãn có cao hơn hàng không nhãn không? **Phải không** — cao hơn là lệch đồ thị.
   Nhìn cột đồ thị ở đúng vùng có nhãn để chắc.
7. Thanh bên: ba nhóm có số đếm đúng chứ? So với `git branch | wc -l`, `git branch -r | wc -l`,
   `git tag | wc -l` chạy trong terminal.
8. Mở `target/fixtures/detached`: thanh bên có nói rõ đang ở **HEAD tách rời** không?

**Tiêu chí 4 — tìm kiếm.**
9. Gõ một từ có trong thông điệp commit: có tìm thấy? Nhảy tới khớp kế tiếp có cuộn tới đúng
   hàng đó?
10. Gõ **tên tác giả**. Gõ **bảy ký tự đầu của một mã commit**. Gõ **một đường dẫn tệp** (ví
    dụ `src/git/exec.rs`). Cả bốn trục của HIST-10 có hoạt động?
11. Gõ nhanh một câu dài rồi xoá: giao diện có đứng hình không? (Trì hoãn 250ms phải chặn được
    bão lời gọi.) Mở bảng nhật ký lệnh (`Ctrl+\``) và xem số lệnh `git log` đã chạy — phải ít,
    không phải một lệnh mỗi ký tự.

**Tiêu chí 6 — byte không UTF-8.**
12. Mở `target/fixtures/non-utf8`. Trang lịch sử có hiện **đầy đủ** không? Đếm số hàng trên
    giao diện và so với `git log --all --topo-order --format=%H | wc -l` trong terminal — **hai số phải bằng nhau**.
    Ký tự lạ hiện thành `` là chấp nhận được; **một hàng bị mất là không**.
  </how-to-verify>
  <resume-signal>
Trả lời "approved" nếu cả 12 bước đạt. Nếu không, nêu **số bước** và hiện tượng. Với bước 7 và
12 ghi kèm **hai con số** đã so (giao diện và terminal) — đó là bằng chứng của tiêu chí, không
phải cảm nhận.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| tên nhánh/tag → DOM | Tên ref do người khác đặt, có thể chứa ký tự lạ |
| đường dẫn tệp → DOM | Tên tệp từ repo bất kỳ |
| `query` người dùng gõ → IPC → git | Chuỗi tự do đi tới tham số git |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-20 | Tampering | Tên nhánh hoặc đường dẫn tệp chứa HTML | mitigate | React escape mặc định; **cấm** `dangerouslySetInnerHTML`, kiểm bằng `grep -rc` = 0 trong `<verify>`; tên ref chỉ vào `textContent` và `title` |
| T-02-21 | Denial of Service | Gõ tìm kiếm sinh một lệnh git mỗi ký tự | mitigate | Trì hoãn 250ms có test bằng `vi.useFakeTimers`; bỏ kết quả của lời gọi cũ theo số thứ tự; bước 11 của checkpoint kiểm bằng nhật ký lệnh thật |
| T-02-22 | Denial of Service | Commit có hàng nghìn ref trỏ vào làm hàng phình | mitigate | `RefBadges` chỉ vẽ N nhãn rồi `+N`; hàng có chiều cao cố định `ROW_HEIGHT` và `overflow: hidden` |
| T-02-23 | Information Disclosure | Email tác giả hiện trong vùng chi tiết | accept | Dữ liệu công khai trong repo người dùng đã tự mở; Phase 2 không gửi gì ra ngoài máy |
</threat_model>

<verification>
1. `npm run typecheck` → 0. `npm test` → toàn bộ đỗ, không hồi quy.
2. `npm run build` → thành công. `npx tauri build --debug --no-bundle` → thành công.
3. `/usr/bin/grep -rc 'dangerouslySetInnerHTML' src/` → `0`.
4. Vẫn đúng một tệp chứa `useVirtualizer` trong `src/`.
5. `cd src-tauri && cargo test` → không hồi quy (plan này không sửa Rust, chạy để chắc).
6. Checkpoint Task 4: 12 bước, có "approved" và hai cặp số so sánh của bước 7 và 12.
</verification>

<success_criteria>
- Chọn commit → tiêu đề, **toàn bộ** nội dung, tác giả, thời gian đúng, **mọi** mã cha (kể cả
  bốn cha), danh sách tệp. Commit gốc cũng chạy.
- Chuyển được phẳng ↔ cây, lựa chọn sống qua việc đổi commit.
- Nhãn nhánh/tag neo đúng hàng, local khác remote, và **không** làm hàng cao lên.
- Thanh bên có ba nhóm kèm số đếm khớp với `git branch`/`git branch -r`/`git tag`.
- HEAD tách rời được nói rõ.
- Tìm kiếm phủ bốn trục HIST-10, có trì hoãn, nhảy tới đúng hàng.
- Repo `non-utf8`: số hàng trên giao diện **bằng** `git log --all --topo-order --format=%H | wc -l`.
- `selectionStore` chỉ giữ id — không dữ liệu commit.
</success_criteria>

<output>
Sau khi hoàn thành, tạo `.planning/phases/02-history-graph/02-06-SUMMARY.md`.
Ghi rõ: kết quả 12 bước checkpoint kèm **hai cặp số** của bước 7 và 12, cách `CommitSearch`
lấy được `scrollToIndex` mà không tạo virtualizer thứ hai, và mọi hiện tượng còn lại dù đã
chấp thuận.
</output>
