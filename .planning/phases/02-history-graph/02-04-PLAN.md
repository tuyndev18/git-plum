---
phase: 02-history-graph
plan: 04
type: execute
wave: 4
depends_on: [02, 03]
files_modified:
  - src-tauri/src/git/parsers/refs.rs
  - src-tauri/src/git/parsers/mod.rs
  - src-tauri/src/cache/mod.rs
  - src-tauri/src/cache/repo_cache.rs
  - src-tauri/src/commands/history.rs
  - src-tauri/src/commands/mod.rs
  - src-tauri/src/state/mod.rs
  - src-tauri/src/error.rs
  - src-tauri/src/lib.rs
  - src/lib/ipc.ts
autonomous: true
requirements: [HIST-01, HIST-02, HIST-06, HIST-07, HIST-11]
user_setup: []

must_haves:
  truths:
    - "Gọi get_commit_page(repoId, skip, limit) trả về đúng một trang commit kèm GraphRow tương ứng"
    - "Gọi lại cùng một trang không sinh thêm tiến trình git (cache theo RepoId hoạt động)"
    - "Gọi list_refs(repoId) trả nhánh local, nhánh remote, tag, phân biệt được loại, kèm ahead/behind"
    - "Ref trỏ đúng mã commit để giao diện neo nhãn vào hàng commit"
    - "Kiểu TypeScript trong ipc.ts khớp từng tên khoá với payload Rust"
  artifacts:
    - path: "src-tauri/src/git/parsers/refs.rs"
      provides: "parse_refs(&[u8]) -> Vec<Ref>"
      exports: ["parse_refs", "REFS_FORMAT"]
    - path: "src-tauri/src/cache/repo_cache.rs"
      provides: "cache commit/graph/refs theo RepoId"
      exports: ["RepoCache"]
    - path: "src-tauri/src/commands/history.rs"
      provides: "get_commit_page, list_refs, get_graph, search_commits"
      exports: ["get_commit_page", "list_refs"]
    - path: "src/lib/ipc.ts"
      provides: "Kiểu TS + hàm gọi cho các command lịch sử"
      contains: "getCommitPage"
  key_links:
    - from: "src-tauri/src/commands/history.rs"
      to: "src-tauri/src/git/runner.rs"
      via: "GitRunner::read() — Phase 2 chỉ đọc"
      pattern: "\\.read(_ok)?\\("
    - from: "src-tauri/src/commands/history.rs"
      to: "src-tauri/src/graph/lanes.rs"
      via: "assign() trên lịch sử đầy đủ"
      pattern: "lanes::assign"
    - from: "src/lib/ipc.ts"
      to: "src-tauri/src/commands/history.rs"
      via: "invoke('get_commit_page')"
      pattern: "invoke<.*>\\('get_commit_page'"
---

<objective>
Bề mặt backend của phase: bộ phân tích `for-each-ref`, cache theo `RepoId`, các Tauri command,
và kiểu TypeScript khớp ở ranh giới IPC.

**Quyết định kiến trúc quan trọng, đã có trong nghiên cứu, dễ bỏ sót:** ARCHITECTURE.md nói
rõ *"lane assignment needs global context, can't be computed per-page in isolation"*. Không
gán lane theo trang được — lane của hàng 5000 phụ thuộc mọi hàng trước nó. Vì vậy:
`Vec<GraphRow>` tính **một lần cho toàn bộ lịch sử** và nằm trong cache (vài byte mỗi hàng,
rẻ), còn **metadata commit** mới là thứ phân trang. Cài ngược lại sẽ ra đồ thị sai ở mọi
trang sau trang đầu, và lỗi đó rất khó thấy trên repo nhỏ.

Purpose: HIST-01 (nạp theo trang), HIST-02 (đồ thị), HIST-06/07 (nhãn và thanh bên), và
HIST-10 phần backend.
Output: `parsers::refs`, `cache::RepoCache`, `commands::history`, kiểu TS trong `ipc.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-history-graph/CONTEXT.md
@.planning/phases/02-history-graph/02-02-SUMMARY.md
@.planning/phases/02-history-graph/02-03-SUMMARY.md
@.planning/research/ARCHITECTURE.md
@src-tauri/src/commands/repo.rs
@src-tauri/src/state/mod.rs
@src-tauri/src/git/runner.rs
@src-tauri/src/error.rs
@src/lib/ipc.ts

<interfaces>
Hợp đồng đã có, dùng trực tiếp:

Từ `src-tauri/src/state/mod.rs`:
```rust
pub type RepoId = String;
pub struct RepoHandle { pub id: RepoId, pub path: PathBuf, pub name: String, /* write_lock */ }
impl RepoHandle { pub fn write_lock(&self) -> Arc<tokio::sync::Mutex<()>>; }
pub struct AppState { pub command_log: Arc<CommandLog>, /* open_repos, active_repo */ }
impl AppState {
    pub fn runner(&self, repo: Arc<RepoHandle>) -> GitRunner;
    pub fn open_repo(&self, path: impl AsRef<Path>) -> Arc<RepoHandle>;
    pub fn get_repo(&self, id: &str) -> Option<Arc<RepoHandle>>;
    pub fn active_repo(&self) -> Option<Arc<RepoHandle>>;
}
```

Từ `src-tauri/src/git/runner.rs`:
```rust
impl GitRunner {
    pub async fn read(&self, cmd: GitCommand) -> Result<GitOutput>;
    pub async fn read_ok(&self, cmd: GitCommand) -> Result<GitOutput>;
    pub async fn write(&self, cmd: GitCommand) -> Result<GitOutput>;   // KHÔNG dùng ở Phase 2
}
```

Khuôn một command, chép từ `commands/repo.rs::current_branch` (theo đúng kiểu này):
```rust
#[tauri::command]
pub async fn current_branch(state: State<'_, AppState>) -> Result<String> {
    let repo = state.active_repo().ok_or(GitError::NoRepositoryOpen)?;
    let runner = state.runner(Arc::clone(&repo));
    let out = runner.read(GitCommand::new(&repo.path).args([...])).await?;
    ...
}
```
**Khác biệt bắt buộc ở plan này:** command mới nhận `repo_id: String` **tường minh** và dùng
`state.get_repo(&repo_id).ok_or(GitError::UnknownRepository(repo_id))?`, **không** dùng
`active_repo()`. Lý do: PLAT-05 chốt trạng thái khoá theo repo; `active_repo()` là lối tắt
của Phase 1 sẽ sai ngay khi có nhiều repo mở. `GitError::UnknownRepository` đã tồn tại.

Từ `src/lib/ipc.ts` (khuôn phải theo):
```typescript
export const ipc = {
  openRepository: (path: string) => invoke<RepoInfo>('open_repository', { path }),
  currentBranch: () => invoke<string>('current_branch'),
}
export type GitErrorCode = 'spawn_failed' | 'timeout' | /* … */ | 'io'
```
Thêm variant `GitError` mới thì **phải** thêm vào union `GitErrorCode` trong cùng thay đổi.
</interfaces>

**Lệnh git của plan này** (CONTEXT.md, nghiên cứu mục 5.2):
```bash
git for-each-ref --format="%(refname)%x1f%(objectname)%x1f%(upstream)%x1f%(upstream:track)%x1f%(HEAD)"
```
`%(upstream:track)` trả dạng `[ahead 3, behind 1]` hoặc `[gone]` hoặc rỗng — **định dạng có
dấu ngoặc vuông và có thể đổi theo locale**, nhưng `LC_ALL=C` đã ghim ở `exec.rs` nên phân
tích được. Có test cho cả ba dạng cộng dạng rỗng.

**Bài học Phase 1:** Tauri v2 **capability thất bại âm thầm lúc chạy, không lúc biên dịch**.
Plan này chỉ thêm command (thuộc `core:default`), không thêm plugin API mới — nhưng nếu có
thì quyền phải nằm trong cùng thay đổi.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: parse_refs + cache theo RepoId</name>
  <files>src-tauri/src/git/parsers/refs.rs, src-tauri/src/git/parsers/mod.rs, src-tauri/src/cache/mod.rs, src-tauri/src/cache/repo_cache.rs, src-tauri/src/state/mod.rs, src-tauri/src/lib.rs</files>
  <behavior>
    - `refs/heads/main` → `RefKind::LocalBranch`, `short_name == "main"`.
    - `refs/remotes/origin/main` → `RefKind::RemoteBranch`, `short_name == "origin/main"`.
    - `refs/tags/v1.0` → `RefKind::Tag`, `short_name == "v1.0"`.
    - `refs/stash` hoặc tên lạ → `RefKind::Other`, không panic.
    - `%(upstream:track)` == `[ahead 3, behind 1]` → `ahead == 3, behind == 1`.
    - `[ahead 2]` → `ahead == 2, behind == 0`. `[behind 5]` → `ahead == 0, behind == 5`.
    - `[gone]` → `ahead == 0, behind == 0` (upstream vẫn giữ tên, giao diện tự hiện trạng thái).
    - track rỗng → `ahead == 0, behind == 0`.
    - `%(HEAD)` == `*` → `is_head == true`; rỗng → `false`.
    - Tên nhánh chứa byte không UTF-8 → vẫn ra một `Ref`, không mất dòng (cùng nguyên tắc HIST-11).
    - `RepoCache`: `put_history` rồi `get_history` cùng `RepoId` → trả dữ liệu; `RepoId` khác → `None`.
    - `RepoCache::invalidate(repo_id)` → lần `get` sau trả `None`.
  </behavior>
  <action>
`src-tauri/src/git/parsers/refs.rs` — `pub const REFS_FORMAT: &str` (chuỗi `--format=` ở
`<context>`) và `pub fn parse_refs(stdout: &[u8]) -> Vec<Ref>`.

Tách dòng theo `\n`, trường theo `\x1f` bằng `memchr`. Giải mã lossy như `log.rs` (cùng lý do,
cùng nguyên tắc). Dòng thiếu trường → bỏ qua, không panic. Phân loại `RefKind` theo tiền tố
`refs/heads/`, `refs/remotes/`, `refs/tags/`; còn lại `Other`. `short_name` là phần sau tiền
tố. Phân tích `upstream:track` bằng cách tìm chuỗi `ahead ` và `behind ` rồi đọc số — **không**
dùng regex (không có crate regex trong dự án, và không cần thêm).

`src-tauri/src/cache/repo_cache.rs`:
```rust
pub struct RepoHistory {
    pub commits: Vec<Commit>,      // toàn bộ lịch sử đã nạp, theo thứ tự topo
    pub graph_rows: Vec<GraphRow>, // cùng độ dài với commits — bất biến của plan 02-03
    pub loaded_at_ms: u64,
}
pub struct RepoCache { /* HashMap<RepoId, ...> sau parking_lot::RwLock */ }
impl RepoCache {
    pub fn get_history(&self, repo: &str) -> Option<Arc<RepoHistory>>;
    pub fn put_history(&self, repo: &str, h: RepoHistory);
    pub fn get_refs(&self, repo: &str) -> Option<Arc<Vec<Ref>>>;
    pub fn put_refs(&self, repo: &str, refs: Vec<Ref>);
    pub fn invalidate(&self, repo: &str);
}
```
Trả `Arc` chứ không clone `Vec<Commit>`: 100k commit clone mỗi lần gọi command là chính xác
cái làm mất mốc dưới một giây.

Dùng `parking_lot::RwLock` (đã có trong `Cargo.toml`), **không** `dashmap` (STACK.md: "cache
của bạn nằm sau một khoá"). Khoá theo `RepoId` theo đúng ràng buộc PLAT-05 trong
`<inherited_state>` của CONTEXT.md — **không** biến toàn cục, không `Option` đơn lẻ.

`state/mod.rs`: thêm `pub cache: Arc<RepoCache>` vào `AppState` và khởi tạo trong `new()`.
Không đổi chữ ký hàm nào đang có — các test Phase 1 phải tiếp tục đỗ y nguyên.

`lib.rs`: `pub mod cache;`. `parsers/mod.rs`: `pub mod refs;`.

**Không** dùng `lru` ở plan này: `lru` đã thêm ở 02-01 cho cache diff của Phase 3. Cache lịch
sử ở đây khoá theo `RepoId`, và số repo mở là hữu hạn nhỏ, nên `HashMap` là đúng. Ghi một
dòng doc comment nói điều đó để người sau không nghĩ là bỏ sót.
  </action>
  <verify>
    <automated>cd src-tauri &amp;&amp; PATH="$HOME/.cargo/bin:$PATH" cargo test --lib parsers::refs cache -- --nocapture &amp;&amp; PATH="$HOME/.cargo/bin:$PATH" cargo clippy --all-targets -- -D warnings</automated>
  </verify>
  <done>
12 ca `<behavior>` có test riêng và đỗ. Có một test tích hợp chạy `for-each-ref` thật trên
fixture `linear` và `detached`, khẳng định: `linear` có ít nhất một `LocalBranch`;
`detached` **không** có ref nào `is_head == true` (HEAD tách rời không neo vào nhãn nhánh nào
— đây là ca biên thứ năm của nghiên cứu mục 4). Test Phase 1 vẫn đỗ hết. clippy sạch.
  </done>
</task>

<task type="auto">
  <name>Task 2: Tauri command lịch sử — phân trang, refs, tìm kiếm</name>
  <files>src-tauri/src/commands/history.rs, src-tauri/src/commands/mod.rs, src-tauri/src/error.rs, src-tauri/src/lib.rs</files>
  <action>
`src-tauri/src/commands/history.rs` — bốn command, mọi command nhận `repo_id: String` tường
minh và dùng `state.get_repo()`, không `active_repo()`.

**`get_commit_page(repo_id, skip, limit) -> Result<CommitPage>`** với
`CommitPage { commits: Vec<Commit>, graph_rows: Vec<GraphRow>, total: usize, skipped_records: usize }`
(`Serialize`, camelCase).

Luồng:
1. `cache.get_history(&repo_id)` — có thì cắt trang từ đó, **không** sinh tiến trình git.
2. Không có: chạy **một** lệnh
   `git log --all --topo-order --format=<LOG_FORMAT>` qua `GitRunner::read_ok()`
   (`GitCommand` + hằng `LOG_FORMAT` từ `parsers::log`; **không** viết lại chuỗi định dạng).
   `parse_log` → `lanes::assign` trên **toàn bộ** lịch sử → `put_history`.
3. Cắt `commits[skip..skip+limit]` và `graph_rows[skip..skip+limit]` bằng
   chỉ mục **có kiểm biên** (`.get(..)`, `saturating_*`) — `skip` đến từ webview, `skip`
   ngoài phạm vi phải trả trang rỗng, không panic.
4. `skipped_records > 0` → `tracing::warn!` kèm số. HIST-11 nói không mất dòng; nếu vẫn mất
   thì phải có vết trong log, không im lặng.

Ghi chú doc comment ngay trên hàm, đủ dài để người sau không "tối ưu" sai: gán lane **phải**
chạy trên toàn bộ lịch sử, không theo trang, vì lane của một hàng phụ thuộc mọi hàng trước
(ARCHITECTURE.md). Phân trang áp dụng cho **việc trả dữ liệu**, không cho việc tính toán.

**Hạn giờ:** `git log --all` trên repo 100k commit có thể vượt `DEFAULT_TIMEOUT` 30s. Dùng
`.timeout(...)` tường minh với một hằng của plan này (ví dụ 120s) và ghi lý do. Đây là lệnh
cục bộ nên không dùng `NETWORK_TIMEOUT`, nhưng 30s là quá ngắn cho lần nạp đầu của repo lớn.

**`list_refs(repo_id) -> Result<Vec<Ref>>`** — `cache.get_refs` trước, miss thì chạy
`for-each-ref` với `REFS_FORMAT`, `parse_refs`, `put_refs`.

**`get_commit_detail(repo_id, commit_id) -> Result<CommitDetail>`** với
`CommitDetail { commit: Commit, files: Vec<FileChange> }` và
`FileChange { status: char→String, path: String, old_path: Option<String> }`.
Lệnh: `git diff --name-status -z --find-renames <commit>^ <commit>` — dùng `-z` vì tên tệp
chứa khoảng trắng/Unicode (PITFALLS mục 3 nói rõ `-z` tránh hẳn việc `core.quotepath` escape
bát phân). Thêm `-c core.quotepath=false` cho chắc, như PITFALLS khuyến nghị.
**Ca biên bắt buộc:** commit **gốc** không có `<commit>^` — lệnh trên thất bại. Phát hiện
bằng `commit.parents.is_empty()` và dùng
`git diff --name-status -z --find-renames <cây rỗng> <commit>` với cây rỗng
`4b825dc642cb6eb9a060e54bf8d69288fbee4904` (hằng của git, có tên), hoặc
`git show --name-status -z --format= <commit>`. Chọn một, ghi lý do, và **có test** trên
fixture `linear` (commit đầu tiên của nó chính là commit gốc). Merge commit: diff với
`parents[0]`, ghi rõ lựa chọn đó trong doc comment.
Phân trang **không** áp dụng ở đây; giới hạn số tệp trả về (ví dụ 5000) kèm cờ `truncated`
để một commit khổng lồ không làm treo giao diện.

**`search_commits(repo_id, query) -> Result<Vec<String>>`** — HIST-10. Trả **mã commit**, không
trả `Commit` đầy đủ: giao diện đã có metadata trong trang, chỉ cần biết hàng nào khớp.
Tìm trên bốn trục theo HIST-10: thông điệp, tên tác giả, mã commit, đường dẫn tệp.
Cách cài: lọc **trong bộ nhớ** trên `cache.get_history()` cho ba trục đầu (không sinh tiến
trình git — người dùng gõ từng ký tự, ARCHITECTURE.md Anti-Pattern 4 cấm chạy git mỗi lần gõ);
riêng trục **đường dẫn tệp** phải hỏi git vì cache không có danh sách tệp:
`git log --all --format=%H -- <pathspec>` với `--` trước pathspec (ARCHITECTURE.md: luôn có
`--` trước pathspec để tên nhánh/đường dẫn không bị hiểu thành tham số). So khớp chuỗi
không phân biệt chữ hoa thường, khớp chuỗi con. Giới hạn kết quả (ví dụ 1000).

`error.rs`: nếu thêm variant nào (ví dụ `InvalidPageRange`), cập nhật `code()` và bổ sung
đúng chuỗi đó vào union `GitErrorCode` ở `src/lib/ipc.ts` trong Task 3. Nếu không cần variant
mới thì **không** thêm — dùng lại `UnknownRepository` và `CommandFailed`.

`commands/mod.rs`: `pub mod history; pub use history::*;`
`lib.rs`: thêm bốn command vào `tauri::generate_handler![...]`. Quên bước này là lỗi kinh
điển — command biên dịch được nhưng `invoke` thất bại lúc chạy.
  </action>
  <verify>
    <automated>bash scripts/fixtures/make-fixtures.sh &amp;&amp; cd src-tauri &amp;&amp; PATH="$HOME/.cargo/bin:$PATH" cargo test commands::history -- --nocapture</automated>
  </verify>
  <done>
Bốn command tồn tại, đăng ký trong `generate_handler!` (xác minh bằng
`/usr/bin/grep -c 'get_commit_page' src-tauri/src/lib.rs` ≥ 1). Test tích hợp trên fixture
`linear` và `octopus`:
- `get_commit_page(skip=0, limit=5)` → 5 commit và 5 `graph_rows`, độ dài **bằng nhau**.
- `get_commit_page` với `skip` lớn hơn tổng số commit → trang rỗng, **không panic**.
- Gọi `get_commit_page` hai lần → lần hai **không** thêm dòng nào vào `CommandLog` cho
  `git log` (chứng minh cache hoạt động — đếm entry trước và sau).
- `get_commit_detail` trên commit **gốc** của `linear` → danh sách tệp không rỗng, không lỗi.
- `list_refs` trên `linear` → có `LocalBranch`.
- `search_commits` với một từ có trong thông điệp commit → trả ≥1 mã; từ vô nghĩa → rỗng.
clippy sạch, fmt 0.
  </done>
</task>

<task type="auto">
  <name>Task 3: Kiểu TypeScript ở ranh giới IPC</name>
  <files>src/lib/ipc.ts</files>
  <action>
Thêm vào `src/lib/ipc.ts`, theo đúng khuôn đang có (interface + hàm trong object `ipc`):

```typescript
export interface Commit { id, parents: string[], authorName, authorEmail, authorTime: number,
  committerName, committerEmail, committerTime: number, subject, body, hasInvalidUtf8: boolean }
export interface Edge { fromLane: number; toLane: number; color: number }
export interface GraphRow { commitId: string; lane: number; color: number;
  passthrough: Edge[]; outEdges: Edge[]; truncatedParents: number; terminates: boolean }
export interface CommitPage { commits: Commit[]; graphRows: GraphRow[]; total: number; skippedRecords: number }
export type RefKind = 'localBranch' | 'remoteBranch' | 'tag' | 'other'
export interface GitRef { fullName, shortName, kind: RefKind, target,
  upstream: string | null, ahead: number, behind: number, isHead: boolean }
export interface FileChange { status: string; path: string; oldPath: string | null }
export interface CommitDetail { commit: Commit; files: FileChange[]; truncated: boolean }
```
Tên `GitRef` chứ không `Ref` — `Ref` trùng với kiểu của React và sẽ gây lẫn ở tệp component.

Thêm vào object `ipc`:
```typescript
getCommitPage: (repoId, skip, limit) => invoke<CommitPage>('get_commit_page', { repoId, skip, limit }),
listRefs: (repoId) => invoke<GitRef[]>('list_refs', { repoId }),
getCommitDetail: (repoId, commitId) => invoke<CommitDetail>('get_commit_detail', { repoId, commitId }),
searchCommits: (repoId, query) => invoke<string[]>('search_commits', { repoId, query }),
```
Tauri v2 chuyển `snake_case` của tham số Rust thành `camelCase` ở phía JS — tham số Rust
`repo_id` gọi bằng `repoId`. Sai chỗ này thì `invoke` thất bại lúc chạy chứ không lúc biên
dịch, nên có test khẳng định tên tham số.

`GitErrorCode`: thêm mọi variant mới sinh ở Task 2. Không có variant mới thì không đổi gì —
nhưng phải **kiểm** bằng cách đối chiếu `GitError::code()` trong `error.rs` với union này và
ghi kết quả đối chiếu vào SUMMARY.

Thêm `src/lib/ipc.history.test.ts`: giả lập `@tauri-apps/api/core` và khẳng định mỗi hàm mới
gọi `invoke` với **đúng tên command** và **đúng hình dạng đối số** (`{ repoId, skip, limit }`).
**Cạm bẫy Phase 1:** `vi.clearAllMocks()` xoá `mockResolvedValue` đặt trong factory của
`vi.mock` — dựng lại giá trị mặc định trong `beforeEach`, nếu không hàm giả trả `undefined`.
`vite.config.ts` đang đặt `restoreMocks: true`, nên bước này là bắt buộc.
  </action>
  <verify>
    <automated>npm run typecheck &amp;&amp; npm test</automated>
  </verify>
  <done>
`npm run typecheck` mã thoát 0. `npm test` ≥ 61 đỗ (57 cũ cộng test mới), không test cũ nào
đỏ. Test khẳng định tên command và hình dạng đối số cho cả bốn hàm mới. Mọi tên khoá trong
interface TS khớp từng chữ với JSON của Rust — đối chiếu với `02-02-SUMMARY.md` và ghi kết
quả đối chiếu `GitErrorCode` ↔ `GitError::code()` vào SUMMARY.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| webview → Tauri command | `repo_id`, `skip`, `limit`, `query`, `commit_id` đều đến từ JavaScript, không tin cậy |
| command → dòng lệnh git | Chuỗi từ webview có thể đi vào tham số git |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-11 | Tampering | `query` của `search_commits` đi vào pathspec của `git log` | mitigate | Luôn có `--` trước pathspec để chuỗi không bị hiểu là tham số; không dựng dòng lệnh bằng nối chuỗi shell — `GitCommand::args` truyền argv trực tiếp cho `tokio::process`, không qua shell, nên không có đường tiêm shell |
| T-02-12 | Elevation of Privilege | `repo_id` bất kỳ từ webview mở repo chưa được cấp phép | mitigate | `state.get_repo(&repo_id)` chỉ trả repo **đã mở** qua `open_repository`; `repo_id` lạ → `GitError::UnknownRepository`. Đường dẫn không bao giờ đến từ webview ở các command này |
| T-02-13 | Denial of Service | `limit` khổng lồ hoặc `skip` âm/ngoài biên | mitigate | Chặn trên `limit` (ví dụ 10000) trong command; cắt mảng bằng `.get(..)`/`saturating_*`, trang rỗng thay vì panic; có test cho `skip` ngoài phạm vi |
| T-02-14 | Denial of Service | `git log --all` trên repo khổng lồ vượt hạn giờ và treo giao diện | mitigate | Hạn giờ tường minh (120s) trên lệnh này; `GitError::Timeout` đã có sẵn và mang args để hiện cho người dùng |
| T-02-15 | Repudiation | Thao tác không có vết khi cache trả lời thay git | accept | Cache hit cố ý **không** sinh dòng nhật ký lệnh — nhật ký ghi lệnh git đã chạy, không ghi lời gọi IPC. Ghi rõ điều này trong doc comment để người đọc nhật ký không tưởng là thiếu |
</threat_model>

<verification>
1. `cd src-tauri && cargo test` → toàn bộ đỗ, gồm test tích hợp command trên fixture.
2. `cd src-tauri && cargo clippy --all-targets -- -D warnings` → sạch; `cargo fmt --all --check` → 0.
3. `/usr/bin/grep -c 'get_commit_page' src-tauri/src/lib.rs` → ≥1 (đã đăng ký handler).
4. `/usr/bin/grep -v '^\s*//' src-tauri/src/commands/history.rs | /usr/bin/grep -c 'runner.write'` → `0`
   (Phase 2 thuần đọc — ràng buộc số 9; lọc comment trước khi đếm).
5. `npm run typecheck` → 0. `npm test` → ≥61 đỗ.
6. `npx tauri build --debug --no-bundle` → thành công (chứng minh command đăng ký hợp lệ).
</verification>

<success_criteria>
- `get_commit_page` trả commit **và** `graph_rows` cùng độ dài, gán lane trên toàn bộ lịch sử
  chứ không theo trang.
- Gọi lại cùng trang không sinh tiến trình git (chứng minh bằng đếm `CommandLog`).
- `list_refs` phân biệt local/remote/tag và trả ahead/behind đúng cho bốn dạng `upstream:track`.
- `get_commit_detail` chạy đúng cả trên commit **gốc** (không có `^`).
- `search_commits` phủ bốn trục HIST-10, không sinh git cho ba trục trong bộ nhớ.
- Mọi command nhận `repo_id` tường minh, dùng `get_repo`, không `active_repo`.
- Không một lời gọi `runner.write` nào trong mã phase này.
- Kiểu TS khớp từng tên khoá; test khẳng định tên command và hình dạng đối số.
</success_criteria>

<output>
Sau khi hoàn thành, tạo `.planning/phases/02-history-graph/02-04-SUMMARY.md`.
Ghi rõ: chữ ký cuối của bốn command, hình dạng chính xác của `CommitPage`/`CommitDetail`,
hạn giờ đã chọn cho `git log --all` kèm lý do, cách xử lý commit gốc trong
`get_commit_detail`, và kết quả đối chiếu `GitErrorCode` ↔ `GitError::code()`.
Plan 02-05 và 02-06 đọc hình dạng payload từ đây.
</output>
