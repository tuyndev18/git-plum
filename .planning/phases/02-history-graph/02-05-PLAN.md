---
phase: 02-history-graph
plan: 05
type: execute
wave: 5
depends_on: [04]
files_modified:
  - package.json
  - src/stores/historyStore.ts
  - src/components/history/CommitList.tsx
  - src/components/history/GraphCanvas.tsx
  - src/lib/graph-render/types.ts
  - src/lib/graph-render/canvasRenderer.ts
  - src/lib/graph-render/geometry.ts
  - src/App.tsx
  - src/styles/app.css
autonomous: false
requirements: [HIST-01, HIST-02, HIST-04, HIST-11]
user_setup: []

must_haves:
  truths:
    - "Mở một repository rồi thấy danh sách commit thật ở vùng giữa, không còn chữ 'Phase 2 sẽ điền'"
    - "Đồ thị nhiều lane có màu vẽ bên trái danh sách, mỗi nút tròn nằm đúng hàng commit của nó"
    - "Cuộn tới đáy rồi lên đỉnh: đồ thị vẫn thẳng hàng với văn bản ở mọi vị trí"
    - "Đổi bộ vẽ đồ thị chỉ phải sửa một tệp — bộ vẽ nằm sau một interface"
    - "Trên màn HiDPI đồ thị không mờ"
  artifacts:
    - path: "src/lib/graph-render/types.ts"
      provides: "interface GraphRenderer — điểm nối để đổi canvas↔SVG"
      contains: "export interface GraphRenderer"
    - path: "src/lib/graph-render/canvasRenderer.ts"
      provides: "Bản cài canvas 2D, xử lý devicePixelRatio"
      contains: "devicePixelRatio"
    - path: "src/lib/graph-render/geometry.ts"
      provides: "Hàm thuần: GraphRow + chỉ số hàng -> toạ độ. Test được không cần DOM"
      exports: ["laneX", "rowY", "ROW_HEIGHT", "LANE_WIDTH"]
    - path: "src/components/history/CommitList.tsx"
      provides: "Một scroll container, một virtualizer, đồ thị + văn bản cùng virtualItems"
      contains: "useVirtualizer"
    - path: "src/stores/historyStore.ts"
      provides: "byRepo[repoId] cho trang commit và graphRows"
  key_links:
    - from: "src/components/history/CommitList.tsx"
      to: "@tanstack/react-virtual"
      via: "useVirtualizer — MỘT thể hiện duy nhất"
      pattern: "useVirtualizer"
    - from: "src/components/history/GraphCanvas.tsx"
      to: "src/lib/graph-render/types.ts"
      via: "gọi qua interface GraphRenderer, không gọi canvas API trực tiếp"
      pattern: "GraphRenderer"
    - from: "src/App.tsx"
      to: "src/components/history/CommitList.tsx"
      via: "vùng giữa của AppLayout"
      pattern: "CommitList"
---

<objective>
Vùng giữa của ứng dụng: danh sách commit ảo hoá cộng cột đồ thị, **thẳng hàng tuyệt đối**.

Đây là công việc giao diện khó nhất của dự án. Vấn đề khó **không** phải thuật toán lane (đã
xong ở plan 02-03) mà là giữ một cột canvas khớp từng pixel với một danh sách ảo hoá tới
100k hàng. ROADMAP đã chốt cách làm: **một scroll container, một virtualizer, đồ thị và văn
bản vẽ từ cùng một mảng `virtualItems`**. Hai vùng cuộn đồng bộ là **bị cấm** — `ScrollSync`
của react-virtualized có lỗi trễ khi cuộn nhanh tồn tại nhiều năm chưa sửa (issue #369).

Cách làm này khiến lệch hàng **bất khả thi về mặt cấu trúc**, chứ không phải "được sửa cho
thẳng". Đó là lý do chọn `@tanstack/react-virtual`: nó headless, không sở hữu phần tử bọc
nào để phải đấu với.

Plan này có checkpoint người kiểm — đây là checkpoint #2 (canvas hay SVG) và là chỗ duy nhất
tiêu chí thành công số 2 kiểm được thật.

Purpose: HIST-01, HIST-02, HIST-04 và tiêu chí thành công số 2.
Output: cột đồ thị + danh sách commit chạy trong ứng dụng thật, bộ vẽ nằm sau interface.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-history-graph/CONTEXT.md
@.planning/phases/02-history-graph/02-04-SUMMARY.md
@docs/04-phase2-degraded-graph.md
@.planning/research/ARCHITECTURE.md
@src/lib/ipc.ts
@src/stores/repoStore.ts
@src/App.tsx
@src/components/AppLayout.tsx
@src/styles/app.css
@docs/screenshots/main-1.png

<interfaces>
Hợp đồng đã có, dùng trực tiếp — **không** đi tìm trong codebase:

Từ `src/lib/ipc.ts` (plan 02-04 đã thêm):
```typescript
export interface Commit { id: string; parents: string[]; authorName: string; authorEmail: string;
  authorTime: number; committerName: string; committerEmail: string; committerTime: number;
  subject: string; body: string; hasInvalidUtf8: boolean }
export interface Edge { fromLane: number; toLane: number; color: number }
export interface GraphRow { commitId: string; lane: number; color: number; passthrough: Edge[];
  outEdges: Edge[]; truncatedParents: number; terminates: boolean }
export interface CommitPage { commits: Commit[]; graphRows: GraphRow[]; total: number; skippedRecords: number }
export const ipc = { getCommitPage: (repoId, skip, limit) => invoke<CommitPage>(...), /* … */ }
export function describeError(e: unknown): string
```

Khuôn store phải theo, từ `src/stores/repoStore.ts`:
```typescript
interface RepoState { byRepo: Record<string, RepoSlice>; activeRepoId: string | null; /* … */ }
export const useRepoStore = create<RepoState>((set, get) => ({ /* … */ }))
export function useActiveRepo(): RepoSlice | null
```
Dữ liệu **luôn** khoá theo `byRepo[repoId]` (PLAT-05). Test store gọi `useHistoryStore.getState()`.

Từ `src/components/AppLayout.tsx`:
```typescript
export function AppLayout({ sidebar, main, detail, bottom }: Props)
```
Vùng `main` hiện là chỗ trống ghi "Đồ thị commit và danh sách lịch sử thuộc Phase 2".

Từ `src/lib/commands.ts` (PLAT-04 — thao tác đi qua sổ đăng ký):
```typescript
export type CommandCategory = 'repo' | 'view' | 'history' | 'branch' | 'help'
export interface Command { id: string; title: string; category: CommandCategory;
  keybinding?: string; enabled?: () => boolean; run: () => void | Promise<void> }
export function registerCommands(commands: Command[]): void
export async function runCommand(id: string): Promise<void>
```
`'history'` **đã có** trong `CommandCategory` — không phải thêm. **`Command.run` không nhận
tham số**; thao tác có tham số thì truyền handler qua prop, xem ghi chú dài trong `App.tsx`.
</interfaces>

**Ràng buộc bộ vẽ (ROADMAP checkpoint #2 + CONTEXT.md):**
- Dựng **canvas trước**, nhưng giữ bộ vẽ **sau một interface** để đổi sang SVG chỉ sửa một tệp.
- **Xử lý device pixel ratio**, nếu không sẽ mờ trên màn HiDPI.
- **Canvas thuần trình bày.** Bắt sự kiện bấm bằng `div` của hàng, **không** dùng toạ độ canvas.
- Frontend **chỉ vẽ**, không bao giờ tự tính liên thông (ràng buộc số 1).

**Bài học Phase 1 áp dụng ở đây:**
- `react-resizable-panels` v4: `Group` chứa `Panel` render có điều kiện **phải** truyền
  `panelIds` cho `useDefaultLayout`, nếu không bố cục lần đầu sẽ sai. `AppLayout` đúng là
  trường hợp đó và hiện **không** truyền. Tham số `groupId` cũng đã deprecated, thay bằng `id`.
  Plan này đổi nội dung vùng `main` nhưng **không** thêm/bớt panel — nếu phải đổi số panel thì
  xử lý luôn cả hai điểm này.
- `vi.clearAllMocks()` xoá `mockResolvedValue` đặt trong factory `vi.mock` — dựng lại mặc
  định trong `beforeEach`. `vite.config.ts` đang bật `restoreMocks: true`.
- Giả lập IPC ở `vi.mock('@/lib/ipc')`, **không** vá `invoke` toàn cục.

**Về ảnh tham chiếu** `docs/screenshots/main-1.png`: học **khái niệm và bố cục** (cột
BRANCH/TAG, cột GRAPH, cột COMMIT MESSAGE, cột tác giả/thời gian). **Tự thiết kế phần nhìn** —
không sao chép bảng màu, icon hay kiểu chữ của GitKraken.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Hình học thuần + interface bộ vẽ + bản cài canvas</name>
  <files>package.json, src/lib/graph-render/types.ts, src/lib/graph-render/geometry.ts, src/lib/graph-render/canvasRenderer.ts</files>
  <behavior>
Test cho `geometry.ts` — thuần số, không cần DOM:
    - `rowY(0) === 0`, `rowY(1) === ROW_HEIGHT`, `rowY(10) === 10 * ROW_HEIGHT`.
    - `laneX(0)` bằng lề trái đã định; `laneX(3) - laneX(2) === LANE_WIDTH`.
    - `graphWidth(maxLane)` tăng đơn điệu theo `maxLane` và có chặn trên tại `MAX_VISIBLE_LANES`.
    - `rowY(i) - scrollOffset` với cùng `scrollOffset` mà virtualizer báo cho hàng `i` phải
      **bằng** `virtualItem.start - scrollOffset` — bất biến thẳng hàng, test bằng giá trị
      `virtualItem` giả lập. Đây là test quan trọng nhất của task này.
Test cho `canvasRenderer.ts` với canvas giả lập (ghi lại lời gọi):
    - Một `GraphRow` có `outEdges` một cạnh `0->0` → sinh đúng một đường thẳng dọc.
    - `outEdges` cạnh `0->2` → sinh một đường chéo có toạ độ x đầu/cuối khác nhau.
    - `passthrough` hai cạnh → sinh hai đoạn thẳng dọc, **cộng** nút tròn của hàng đó.
    - `truncatedParents > 0` → sinh chỉ báo (một lời gọi vẽ chữ hoặc dấu riêng), không bỏ qua.
    - `terminates === true` → sinh dấu hiệu kết thúc khác với nút tròn thường.
    - `devicePixelRatio === 2` → `canvas.width` bằng **hai lần** chiều rộng CSS và có đúng
      một lời gọi `scale(2, 2)`. Bỏ bước này là lỗi "mờ trên HiDPI" mà ROADMAP nêu tên.
    - `color` vượt số màu trong bảng → vẫn trả một màu hợp lệ (modulo), không `undefined`.
  </behavior>
  <action>
`package.json` — `npm install @tanstack/react-virtual@3.14.13` (phiên bản đã chốt ở STACK.md;
ghim chính xác, không dùng `^`). Cài ở plan này, cùng thay đổi với chỗ dùng nó.

`src/lib/graph-render/geometry.ts` — hàm **thuần**, không DOM, không React:
- `export const ROW_HEIGHT = 28` (hoặc số đã chọn) — **chiều cao hàng cố định** là điều kiện
  của cách làm này; nghiên cứu ghi rõ. Hằng này là nguồn duy nhất, `useVirtualizer` đọc từ đây.
- `export const LANE_WIDTH`, `export const GRAPH_PADDING_LEFT`, `export const NODE_RADIUS`.
- `export function rowY(index: number): number` → `index * ROW_HEIGHT`.
- `export function laneX(lane: number): number` → `GRAPH_PADDING_LEFT + lane * LANE_WIDTH`.
- `export function graphWidth(maxLane: number): number` — chiều rộng cột đồ thị, chặn trên
  theo `MAX_VISIBLE_LANES` (giá trị chốt ở `docs/04-phase2-degraded-graph.md`).
- `export const LANE_COLORS: readonly string[]` — bảng màu **tự thiết kế**, không sao chép
  GitKraken. Phải đủ tương phản trên nền tối và nền sáng. `colorFor(i)` = `LANE_COLORS[i % len]`.

`src/lib/graph-render/types.ts` — **điểm nối của checkpoint #2**:
```typescript
export interface GraphRenderRow { row: GraphRow; index: number; y: number }
export interface GraphRenderer {
  resize(cssWidth: number, cssHeight: number, dpr: number): void
  draw(rows: GraphRenderRow[], selectedCommitId: string | null): void
  dispose(): void
}
export type GraphRendererFactory = (host: HTMLElement) => GraphRenderer
```
Doc comment ghi rõ: đây là ranh giới của checkpoint #2. Đổi sang SVG nghĩa là viết một tệp
`svgRenderer.ts` cài đúng interface này và đổi **một** dòng import ở `GraphCanvas.tsx`. Không
được để bất kỳ lời gọi canvas API nào rò ra ngoài `canvasRenderer.ts` — nếu `GraphCanvas.tsx`
gọi `getContext('2d')` thì interface đã hỏng, và checkpoint #2 mất đường lùi.

`src/lib/graph-render/canvasRenderer.ts` — `export const createCanvasRenderer: GraphRendererFactory`.
- Tạo một `<canvas>` duy nhất trong `host`.
- `resize`: đặt `canvas.width = cssWidth * dpr`, `canvas.height = cssHeight * dpr`,
  `canvas.style.width = cssWidth + 'px'` (tương tự height), rồi `ctx.scale(dpr, dpr)`. Thiếu
  ba bước này là mờ trên HiDPI. Đọc `dpr` từ `window.devicePixelRatio` ở phía gọi, truyền vào
  — để test được mà không phải vá `window`.
- `draw(rows, selectedCommitId)`: `clearRect` rồi với mỗi hàng vẽ `passthrough` (đoạn dọc),
  `outEdges` (dọc nếu `fromLane === toLane`, chéo/đường bezier nếu khác), rồi nút tròn tại
  `(laneX(row.lane), y + ROW_HEIGHT/2)`. `terminates` → dấu hiệu riêng (ví dụ nút rỗng hoặc
  ba chấm). `truncatedParents > 0` → chỉ báo `+N` cạnh nút.
- **Chỉ vẽ.** Không tính lane, không suy ra liên thông, không đọc `parents`. Toàn bộ hình học
  đến từ `GraphRow` mà Rust đã tính (ràng buộc số 1). Doc comment nói rõ điều này.
- Không gắn event listener nào lên canvas (ràng buộc số 5 — canvas thuần trình bày).

Test canvas: dựng một đối tượng ngữ cảnh giả ghi lại lời gọi (`moveTo`, `lineTo`, `arc`,
`scale`, `fillText`, …) và truyền vào qua một tham số tuỳ chọn của factory, hoặc dùng
`happy-dom` với `getContext` bị vá **cục bộ trong test**. Không vá toàn cục.
  </action>
  <verify>
    <automated>npm run typecheck &amp;&amp; npm test -- src/lib/graph-render</automated>
  </verify>
  <done>
Toàn bộ 12 ca `<behavior>` có test riêng và đỗ. `/usr/bin/grep -c "getContext" src/components/history/GraphCanvas.tsx`
→ `0` (canvas API không rò ra ngoài bộ vẽ; tệp này tạo ở Task 2, kiểm lại sau Task 2).
`@tanstack/react-virtual` ghim đúng `3.14.13` trong `package.json` (không có `^`).
`npm run typecheck` mã thoát 0.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: historyStore + CommitList — một scroll container, một virtualizer</name>
  <files>src/stores/historyStore.ts, src/components/history/CommitList.tsx, src/components/history/GraphCanvas.tsx, src/styles/app.css</files>
  <behavior>
Test `historyStore` qua `useHistoryStore.getState()`, giả lập `@/lib/ipc`:
    - `loadFirstPage(repoId)` → `byRepo[repoId].commits` và `.graphRows` có dữ liệu, `total` đúng.
    - Dữ liệu của hai `repoId` khác nhau **không** trộn vào nhau (PLAT-05).
    - `ipc.getCommitPage` ném lỗi → `byRepo[repoId].error` được đặt, `isLoading` về `false`,
      store **không** ném ra ngoài (giao diện hiện banner, không trắng màn hình).
    - `commits.length !== graphRows.length` trong payload → store ghi cảnh báo và **không**
      crash; đây là bất biến của backend, nhưng giao diện không được sập nếu nó bị vi phạm.
    - `ensureRange(repoId, start, end)` gọi `getCommitPage` **một lần** cho một dải chưa có, và
      **không gọi lại** cho cùng dải (chống bão IPC khi cuộn — ARCHITECTURE.md Anti-Pattern 3).
Test `CommitList` bằng `@testing-library/react`:
    - Render với 3 commit giả → thấy ba `subject` trên màn hình.
    - Có đúng **một** phần tử cuộn (`[data-testid="commit-scroll"]`) trong cây. Test này là
      cách duy nhất chống được việc ai đó thêm vùng cuộn thứ hai sau này.
    - Bấm một hàng → callback `onSelect` nhận đúng `commitId` của hàng đó (bấm vào `div` của
      hàng, không phải canvas — ràng buộc số 5).
    - Commit có `hasInvalidUtf8 === true` vẫn render một hàng, không bị lọc bỏ (HIST-11: không
      dòng nào bị mất).
    - `total` lớn hơn số commit đã nạp → vẫn render, không lỗi (trang chưa nạp hết).
  </behavior>
  <action>
`src/stores/historyStore.ts` — zustand, khuôn `byRepo[repoId]` chép từ `repoStore.ts`:
```typescript
interface HistorySlice { commits: Commit[]; graphRows: GraphRow[]; total: number;
  loadedRanges: Array<[number, number]>; isLoading: boolean; error: string | null }
interface HistoryState { byRepo: Record<string, HistorySlice>
  loadFirstPage(repoId: string): Promise<void>
  ensureRange(repoId: string, start: number, end: number): Promise<void>
  reset(repoId: string): void }
```
`PAGE_SIZE` = 1000. Con số này đến từ checkpoint #4: *"Gộp lô lớn — 1000 commit một thông
điệp, không phải 1"*. Đặt thành hằng xuất ra để plan 02-07 đo được với giá trị khác.

`ensureRange`: khi virtualizer hiện dải hàng chưa có dữ liệu, gọi `getCommitPage` cho trang
chứa dải đó. Theo dõi dải đã nạp **và dải đang bay** để không gọi trùng. Lỗi thì đặt `error`
và **nuốt**, không ném — cuộn nhanh qua vùng chưa nạp không được làm sập giao diện.

`src/components/history/GraphCanvas.tsx` — component mỏng bọc bộ vẽ:
- Nhận props: `rows: GraphRenderRow[]`, `height: number`, `width: number`,
  `selectedCommitId: string | null`.
- `useRef` cho host `div`, `useEffect` tạo renderer qua `createCanvasRenderer` **một lần**, gọi
  `dispose` lúc dọn.
- `useEffect` gọi `resize(width, height, window.devicePixelRatio)` khi kích thước hoặc dpr đổi.
  Lắng nghe `window.matchMedia('(resolution: 1dppx)')` hoặc đơn giản đọc lại `devicePixelRatio`
  khi resize — người dùng kéo cửa sổ sang màn hình khác thì dpr đổi.
- `useEffect` gọi `draw(rows, selectedCommitId)` khi `rows` đổi.
- **Không** import gì từ `canvas` API. Chỉ import `GraphRenderer`, `createCanvasRenderer`.
- `pointer-events: none` trên canvas qua CSS — ràng buộc số 5, canvas không bắt sự kiện.

`src/components/history/CommitList.tsx` — **trái tim của tiêu chí thành công số 2**:
```
<div data-testid="commit-scroll" ref={scrollRef} className="commit-scroll">   ← MỘT vùng cuộn
  <div style={{ height: virtualizer.getTotalSize(), position: 'relative' }}>
    <GraphCanvas ... />            ← lớp phủ, position absolute, translateY theo cùng offset
    {virtualizer.getVirtualItems().map(v => (
      <div key={v.key} className="commit-row" style={{ position:'absolute', top:0,
           transform:`translateY(${v.start}px)`, height:v.size }}
           onClick={() => onSelect(commits[v.index]?.id)}>
        …cột văn bản…
      </div>
    ))}
  </div>
</div>
```
Yêu cầu tuyệt đối:
- **Đúng một** `useVirtualizer`, `count: total`, `estimateSize: () => ROW_HEIGHT`,
  `getScrollElement: () => scrollRef.current`, `overscan` khoảng 10.
- `GraphCanvas` nhận `rows` dựng từ **cùng mảng `getVirtualItems()`** đó:
  `virtualItems.map(v => ({ row: graphRows[v.index], index: v.index, y: v.start }))`.
  Đây là chỗ khiến lệch hàng bất khả thi — hai bên dùng chung một mảng, cùng một `v.start`,
  không có phép tính song song nào để lệch. Viết một comment ngắn ngay đây nói rõ, vì đây là
  thứ dễ bị "dọn dẹp" thành hai nguồn số liệu.
- Hàng nào chưa có dữ liệu (`commits[v.index]` là `undefined`) → hiện hàng chỗ trống có đúng
  `ROW_HEIGHT` và gọi `ensureRange`. **Không** làm hàng co lại — chiều cao đổi là lệch hàng.
- Gọi `ensureRange(repoId, first.index, last.index)` trong `useEffect` theo `virtualItems`.
- Cột văn bản theo bố cục ảnh tham chiếu: đồ thị | thông điệp (`subject`) | tác giả | thời
  gian | mã commit ngắn. Thời gian định dạng bằng `Intl.DateTimeFormat` từ `authorTime * 1000`
  — **không** `chrono` bên Rust, **không** thư viện ngày tháng nào bên JS.
- Hàng có `hasInvalidUtf8` → một dấu hiệu nhẹ (ví dụ `title` tooltip), **không** ẩn hàng.
- Hàng đang chọn có class riêng.
- **Không** `dangerouslySetInnerHTML` ở bất kỳ đâu — `subject`/`body` là dữ liệu không tin cậy.

`src/styles/app.css`: class `.commit-scroll` (`overflow-y: auto`, `position: relative`),
`.commit-row` (grid các cột, `white-space: nowrap`, `text-overflow: ellipsis`),
`.graph-canvas` (`position: absolute; left: 0; top: 0; pointer-events: none`). Tự thiết kế
phần nhìn; không sao chép bảng màu GitKraken.
  </action>
  <verify>
    <automated>npm run typecheck &amp;&amp; npm test -- src/stores/historyStore src/components/history &amp;&amp; test "$(/usr/bin/grep -c 'useVirtualizer' src/components/history/CommitList.tsx)" = "1" &amp;&amp; test "$(/usr/bin/grep -c 'getContext' src/components/history/GraphCanvas.tsx)" = "0"</automated>
  </verify>
  <done>
11 ca `<behavior>` có test riêng và đỗ. Đúng **một** lời gọi `useVirtualizer` trong toàn bộ
`src/components/history/` (kiểm bằng `/usr/bin/grep -rc`). Đúng **một** phần tử
`data-testid="commit-scroll"` trong cây render (test khẳng định). Không có `getContext` ngoài
`canvasRenderer.ts`. Không có `dangerouslySetInnerHTML` ở đâu
(`/usr/bin/grep -rc dangerouslySetInnerHTML src/` → `0`). `npm run typecheck` mã thoát 0.
  </done>
</task>

<task type="auto">
  <name>Task 3: Nối vào App.tsx — ứng dụng dùng được thật</name>
  <files>src/App.tsx, src/styles/app.css</files>
  <action>
Thay nội dung vùng `main` của `AppLayout` trong `App.tsx`: chưa mở repo thì giữ nguyên
`empty-state` + `RecentRepoList` như hiện tại; mở repo rồi thì render `<CommitList>` thay cho
khối `repo-summary` với chữ "Đồ thị commit và danh sách lịch sử thuộc Phase 2".

Quy tắc MVP của phase này: **sau plan này ứng dụng phải dùng được thật**, không phải nửa nối.
Mở một repository → thấy lịch sử commit thật với đồ thị. Không có bước nào "sẽ nối sau".

Chi tiết:
- `useEffect` gọi `useHistoryStore.getState().loadFirstPage(activeRepoId)` khi `activeRepoId`
  đổi, và `reset(id)` khi đóng repo.
- Trạng thái chọn commit: thêm `selectedCommitId` dạng `useState` trong `App.tsx` **hoặc** một
  store riêng. Chọn store riêng nhỏ (`selectionStore`) là đúng hơn theo ARCHITECTURE.md Pattern
  3 ("selectionStore giữ **chỉ** id, không giữ dữ liệu commit") — nhưng plan 02-06 mới cần nó
  cho vùng chi tiết. Ở plan này dùng cách nào cũng được; nếu dùng `useState` thì ghi vào
  SUMMARY để plan 02-06 biết phải nâng lên store.
- Đăng ký hai lệnh mới qua sổ đăng ký (PLAT-04), category `'history'` (đã có trong union):
  `history.refresh` (nạp lại trang đầu, `enabled` khi có repo đang mở) và
  `history.scrollToTop`. Không gắn thẳng vào `onClick` — quy ước của dự án là
  `onClick={() => runCommand('history.refresh')}`.
- Lỗi khi nạp lịch sử → banner `error` đang có, qua `describeError`. Không thêm cơ chế lỗi mới.
- Vùng `sidebar` và `detail` **vẫn** là chỗ trống ở plan này, nhưng đổi chữ thành "Plan 02-06
  sẽ điền" để không ai tưởng Phase 2 đã bỏ sót.
- **Không** thêm/bớt `Panel` trong `AppLayout`. Nếu buộc phải, xử lý luôn `panelIds` cho
  `useDefaultLayout` và đổi `groupId` → `id` (cảnh báo v4 trong CONTEXT.md), và nói rõ trong
  SUMMARY vì nó đụng tới G8 của Phase 1.

Cập nhật test `src/App.test.tsx` nếu đã có, hoặc thêm một test render: mở repo giả (giả lập
`@/lib/ipc`) → `CommitList` xuất hiện; chưa mở repo → `empty-state` xuất hiện. Nhớ dựng lại
`mockResolvedValue` trong `beforeEach` (`restoreMocks: true` đang bật).
  </action>
  <verify>
    <automated>npm run typecheck &amp;&amp; npm test &amp;&amp; npm run build</automated>
  </verify>
  <done>
`npm test` đỗ hết, không hồi quy (số test ≥ số của plan 02-04 cộng test mới).
`npm run typecheck` 0, `npm run build` thành công.
`/usr/bin/grep -c 'Phase 2 sẽ điền' src/App.tsx` → `0` cho vùng `main` (chuỗi có thể còn ở
sidebar/detail dưới dạng "Plan 02-06 sẽ điền" — đó là chuỗi khác).
`npx tauri build --debug --no-bundle` thành công.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Checkpoint #2 — kiểm bằng mắt sự thẳng hàng và độ nét</name>
  <what-built>
Cột đồ thị canvas và danh sách commit ảo hoá, dùng chung một scroll container và một
virtualizer. Bộ vẽ nằm sau `interface GraphRenderer`, xử lý `devicePixelRatio`. Ứng dụng mở
repository thật và hiện lịch sử.

Đã tự động hoá xong: toàn bộ test đơn vị, test "đúng một vùng cuộn", test dpr, test không rò
canvas API, build release. Còn lại đúng phần **mắt người mới thấy được** — test tự động không
chứng minh được hai thứ: hình có thẳng hàng thật không, và có mờ không.
  </what-built>
  <how-to-verify>
Chạy `npm run tauri:dev` (hoặc `dev.cmd`), mở repository của **chính dự án này**
(`C:\Users\tuyen\OneDrive\Desktop\git-plum`), rồi làm sáu bước:

1. **Thẳng hàng lúc nghỉ.** Mỗi nút tròn có nằm đúng giữa chiều cao hàng commit của nó không?
   Nhìn ở đỉnh danh sách, giữa, và đáy.
2. **Thẳng hàng lúc cuộn nhanh.** Kéo thanh cuộn thật nhanh lên xuống liên tục khoảng 10 giây.
   Có thời điểm nào nút tròn lệch khỏi hàng, dù chỉ một hàng? Đây là lỗi mà `ScrollSync` gây
   ra và là lý do phase này chọn một vùng cuộn. Lệch dù một lần là **trượt**.
3. **Cuộn bằng con lăn và bằng bàn phím** (PageDown, Ctrl+End). Vẫn thẳng hàng?
4. **Độ nét.** Đường kẻ và nút tròn có nét không, hay hơi mờ/nhoè? Nếu có màn HiDPI hoặc
   đặt tỉ lệ Windows 125%/150%, kiểm ở tỉ lệ đó — đây chính là ca mà `devicePixelRatio` chống.
   Đổi tỉ lệ màn hình trong lúc ứng dụng đang mở và xem đồ thị có tự nét lại.
5. **Bấm chọn.** Bấm vào một hàng: hàng được chọn có sáng lên không? Bấm **đúng lên đường kẻ
   đồ thị** — vẫn chọn được hàng đó chứ? (Canvas có `pointer-events: none`; nếu bấm vào đồ
   thị không chọn được hàng thì ràng buộc số 5 bị vi phạm.)
6. **Mở một repo mẫu có hình dạng lạ.** Mở `target/fixtures/octopus` rồi `target/fixtures/wide`.
   Merge bốn cha có bốn đường rẽ không? Repo `wide` có hiện chỉ báo `+N` chứ không tràn ngang
   vô hạn?

Ghi lại: **canvas hay SVG** — quyết định của checkpoint #2. Nếu canvas đạt cả sáu bước thì
chốt canvas và ghi rằng đã kiểm; interface vẫn ở đó làm đường lùi.
  </how-to-verify>
  <resume-signal>
Trả lời "approved" nếu cả sáu bước đạt. Nếu không, nêu **bước số mấy** và hiện tượng thấy
được (lệch bao nhiêu hàng ở tốc độ cuộn nào / mờ ở tỉ lệ màn hình nào / bấm chỗ nào không ăn).
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dữ liệu commit → DOM | `subject`, `body`, tên tác giả do người khác viết, đi vào React |
| `skip`/`limit` do virtualizer sinh → IPC | Số phát sinh từ vị trí cuộn |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-16 | Tampering | Thông điệp commit chứa HTML/script hiện trong danh sách | mitigate | React escape văn bản mặc định; **cấm** `dangerouslySetInnerHTML` cho mọi trường commit, kiểm bằng `grep -rc dangerouslySetInnerHTML src/` = 0 trong `<verify>` |
| T-02-17 | Denial of Service | Cuộn nhanh sinh hàng trăm lời gọi `getCommitPage` | mitigate | `ensureRange` theo dõi dải đã nạp **và** dải đang bay, gộp lô 1000; có test khẳng định không gọi trùng cho cùng dải |
| T-02-18 | Denial of Service | Repo có hình dạng bệnh lý làm canvas vẽ tràn | mitigate | `MAX_VISIBLE_LANES` chặn ở Rust (plan 02-03); `graphWidth` chặn trên ở JS; bước 6 của checkpoint kiểm bằng fixture `wide` thật |
| T-02-19 | Information Disclosure | Tên tác giả và email hiện trên giao diện | accept | Dữ liệu vốn công khai trong repo mà người dùng đã tự mở; không gửi đi đâu, không ghi ra ngoài |
</threat_model>

<verification>
1. `npm run typecheck` → 0. `npm test` → toàn bộ đỗ, không hồi quy.
2. `npm run build` → thành công. `npx tauri build --debug --no-bundle` → thành công.
3. `/usr/bin/grep -rc 'useVirtualizer' src/components/history/` → tổng `1`.
4. `/usr/bin/grep -rc 'dangerouslySetInnerHTML' src/` → `0`.
5. `/usr/bin/grep -c 'getContext' src/components/history/GraphCanvas.tsx` → `0`.
6. `/usr/bin/grep -c 'devicePixelRatio' src/lib/graph-render/canvasRenderer.ts` → ≥1.
7. Checkpoint Task 4: sáu bước kiểm bằng mắt, có câu trả lời "approved" của người dùng.
</verification>

<success_criteria>
- Mở repository → thấy danh sách commit thật kèm đồ thị nhiều lane có màu. Vùng giữa không
  còn là chỗ trống.
- Đúng **một** scroll container, đúng **một** `useVirtualizer`; đồ thị và văn bản dựng từ
  cùng mảng `virtualItems`.
- Bộ vẽ sau `interface GraphRenderer`; không lời gọi canvas API nào ngoài `canvasRenderer.ts`.
- `devicePixelRatio` được xử lý, có test khẳng định `scale(dpr, dpr)`.
- Canvas `pointer-events: none`; chọn hàng bằng `div` của hàng.
- Commit có byte không UTF-8 vẫn hiện, không dòng nào bị lọc bỏ.
- Người dùng đã kiểm bằng mắt sáu bước và chấp thuận; quyết định canvas-hay-SVG đã ghi lại.
</success_criteria>

<output>
Sau khi hoàn thành, tạo `.planning/phases/02-history-graph/02-05-SUMMARY.md`.
Ghi rõ: quyết định **canvas hay SVG** của checkpoint #2 kèm bằng chứng từ sáu bước kiểm,
giá trị `ROW_HEIGHT`/`LANE_WIDTH`/`PAGE_SIZE` đã chọn, cách quản trạng thái chọn commit
(`useState` hay store — plan 02-06 cần biết), và mọi hiện tượng thấy được ở bước cuộn nhanh
dù đã chấp thuận.
</output>
