---
phase: 05-hunk-staging
plan: 02
type: execute
wave: 2
depends_on: [05-01]
files_modified:
  - src-tauri/src/commands/hunk.rs
  - src-tauri/src/commands/mod.rs
  - src-tauri/src/error.rs
  - src-tauri/src/lib.rs
  - src-tauri/tests/hunk_commands.rs
autonomous: true
requirements: [WORK-03, WORK-04, WORK-05]
must_haves:
  truths:
    - "Stage đúng một hunk trong tệp ba hunk: index chứa đúng hunk đó, hai hunk kia còn ở worktree"
    - "Unstage một hunk đưa đúng hunk đó ra khỏi index"
    - "Tệp đã đổi sau khi vẽ diff → lỗi có mã `file_changed`, KHÔNG áp bản vá"
    - "Sau staging một phần, tệp worktree giữ nguyên CRLF, thiếu dòng cuối, và byte 0xE9"
    - "Bỏ `--recount` làm một test ĐỎ — fixture đã ĐO là phân biệt được"
  artifacts:
    - path: "src-tauri/src/commands/hunk.rs"
      provides: "stage_hunk / unstage_hunk qua git apply --cached"
      contains: "--recount"
    - path: "src-tauri/tests/hunk_commands.rs"
      provides: "Test tích hợp trên git thật, repo tempfile"
  key_links:
    - from: "src-tauri/src/commands/hunk.rs"
      to: "src-tauri/src/git/patch_build.rs"
      via: "dung_ban_va_mot_hunk"
      pattern: "dung_ban_va_mot_hunk"
    - from: "src-tauri/src/commands/hunk.rs"
      to: "src-tauri/src/commands/worktree.rs"
      via: "chay_lenh_ghi_co_thu_lai_phan_loai — DÙNG LẠI, không viết mới"
      pattern: "chay_lenh_ghi_co_thu_lai"
---

<objective>
Nối tầng byte thô của 05-01 vào `git apply --cached`, có `--recount`, có kiểm blob hash
trước khi áp, và có lỗi riêng cho ca tệp đã đổi.

Purpose: WORK-03 (stage/unstage một khối), WORK-04 (giữ nguyên byte), WORK-05 (báo và
đòi làm mới). Đây vẫn là **tầng Rust tự đứng** — toàn bộ plan kiểm bằng repo `tempfile`
và `git` thật, theo đúng khuôn `tests/worktree_commands.rs` đã có. Không một test nào
render một component.

Output: hai command Tauri `stage_hunk`/`unstage_hunk`, một biến thể lỗi mới, và bộ test
tích hợp trên git thật.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-hunk-staging/CONTEXT.md
@.planning/phases/05-hunk-staging/05-01-SUMMARY.md

@src-tauri/src/commands/worktree.rs
@src-tauri/src/commands/diff.rs
@src-tauri/src/error.rs
@src-tauri/tests/worktree_commands.rs

<interfaces>
<!-- Hợp đồng có sẵn. Dùng thẳng. -->

Từ 05-01 (plan trước, cùng phase):
  pub struct PatchTho { pub dau: Vec<u8>, pub hunks: Vec<Vec<u8>> }
  pub fn tach_hunk_tho(patch: &[u8]) -> Result<PatchTho, GitError>
  pub fn dung_ban_va_mot_hunk(p: &PatchTho, chi_so: usize) -> Option<Vec<u8>>

Từ src-tauri/src/commands/worktree.rs — 🔴 DÙNG LẠI, KHÔNG VIẾT MỚI:
  pub(crate) async fn chay_lenh_ghi_co_thu_lai_phan_loai(
      state: &AppState, repo: &Arc<RepoHandle>,
      dung_lenh: impl Fn() -> GitCommand,
      phan_loai: impl Fn(Vec<String>, &GitOutput) -> GitError,
  ) -> Result<GitOutput>
  pub async fn lay_trang_thai(state: &AppState, repo: Arc<RepoHandle>) -> Result<RepoStatus>

Hàm trên đã có: 3 lần thử, giãn cách `GIAN_CACH_MS`, **không bao giờ xoá `index.lock`**,
và nhận một hàm phân loại lỗi tiêm vào. Đó đúng là cơ chế ROADMAP đòi cho ca "tự làm mới
chồng lên stage do người dùng khởi tạo". Viết lại nó ở đây là sai plan.

Từ src-tauri/src/git/exec.rs:
  impl GitCommand { pub fn stdin_bytes(self, data: Vec<u8>) -> Self }
  pub struct GitOutput { pub stdout: Vec<u8>, pub stderr: Vec<u8>, pub status: i32 }

Từ src-tauri/src/error.rs — khuôn để thêm biến thể:
  pub enum GitError { ..., IndexLocked { args: Vec<String>, stderr: String }, ... }
  impl GitError { pub fn code(&self) -> &'static str }   // phải thêm nhánh match mới
</interfaces>
</context>

<constraints>
🔴 **`--recount` bắt buộc** trên mọi `git apply`. Xem Task 2 — fixture cho nó đã được
**đo** là phân biệt được, đừng thay bằng fixture khác mà không đo lại.

🔴 **`--check --cached` trước, rồi mới áp thật.** Hai lệnh, không một lệnh.

🔴 **Không bao giờ thử lại khi thất bại.** Không `--whitespace=fix`, không `-C1`, không
`--unidiff-zero`, không `--3way`. Thất bại → `GitError::FileChanged`. Có cổng grep.

🔴 **stdout và stderr tách riêng** — `GitOutput` đã tách sẵn; chỉ cần không ghép.
</constraints>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: `GitError::FileChanged` + kiểm blob hash trước khi áp</name>
  <files>src-tauri/src/error.rs, src-tauri/src/commands/hunk.rs, src-tauri/src/commands/mod.rs</files>
  <behavior>
    Biến thể lỗi mới, mã ổn định `file_changed`:

        #[error("Tệp đã đổi từ lúc bạn xem khác biệt này, hãy làm mới")]
        FileChanged { path: String },

    🔴 Thông điệp là **nguyên văn ROADMAP**. Giao diện sẽ hiện đúng câu này; đổi chữ ở
    đây nghĩa là tiêu chí thành công 3 không còn kiểm được bằng chuỗi.

    `code()` phải có nhánh `Self::FileChanged { .. } => "file_changed"`.

    `pub async fn kiem_blob_hash(state, repo, path, mong_doi: &str) -> Result<()>`:
    chạy `git hash-object -- <path>`, so với `mong_doi`, khác thì trả `FileChanged`.

    Test 1 — `code()` trả `"file_changed"`; serialize ra JSON có `code == "file_changed"`
    (khuôn test `serializes_with_code_and_command` đã có trong `error.rs`).

    Test 2 — 🔴 **liệt kê TOÀN BỘ khoá JSON rồi so bằng**, không chỉ kiểm khoá mình mong
    có mặt. Đây là cách `binary_khong_mang_byte_noi_dung` bắt được lỗi `rename_all` ở
    Phase 3 (ghi trong `domain/diff.rs`) — kiểm "có khoá X" xanh cả khi có thừa khoá Y.

    Test 3 — thông điệp lỗi **chứa nguyên văn** `"hãy làm mới"`. Đổi chữ → đỏ.
  </behavior>
  <action>
    Thêm biến thể vào `GitError`, thêm nhánh `code()`. Đường `command_args()` **không**
    cần nhánh mới (lỗi này không sinh từ một lệnh cụ thể) — để nó rơi vào `_ => None`.

    Tạo `src-tauri/src/commands/hunk.rs`, khai trong `commands/mod.rs`.

    `kiem_blob_hash` chạy qua `runner.read` (lệnh đọc, không giữ khoá ghi — xem ghi chú
    đầu `worktree.rs` về lý do). Lệnh:
    `git hash-object -- <path>`, có `--` ngăn cách (T-02-11: một tệp tên `-rf` hay `main`
    đến từ webview).

    🔴 So sánh hash: cắt khoảng trắng của stdout rồi so **bằng chuỗi**, không so tiền tố
    — hash rút gọn khớp tiền tố với một blob khác là ca có thật trên repo lớn.
  </action>
  <verify>
    <automated>cd src-tauri &amp;&amp; cargo test --lib file_changed 2>&amp;1 | tee /tmp/fc.txt | tail -10 &amp;&amp; bash -c 'N=$(grep -oE "[0-9]+ passed" /tmp/fc.txt | head -1 | grep -oE "[0-9]+"); echo "[passed=$N]"; test -n "$N" || { echo "🔴 không đọc được số test"; exit 1; }; test "$N" -ge 3 || { echo "🔴 cần >= 3 test, có $N"; exit 1; }'</automated>
  </verify>
  <done>
    `GitError::FileChanged` tồn tại, `code()` trả `"file_changed"`, thông điệp chứa
    `"hãy làm mới"`, test so **toàn bộ** khoá JSON xanh.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: `stage_hunk` / `unstage_hunk` + fixture ĐÃ ĐO phân biệt được `--recount`</name>
  <files>src-tauri/src/commands/hunk.rs, src-tauri/src/lib.rs, src-tauri/tests/hunk_commands.rs</files>
  <behavior>
    Hai command, cùng khuôn "trả trạng thái MỚI" của WORK-02 (ràng buộc 2.5 CONTEXT.md
    Phase 4 — kiểu trả về là chỗ ghim ràng buộc, không phải tiện lợi):

        #[tauri::command]
        pub async fn stage_hunk(repo_id: String, path: String, hunk_index: usize,
                                blob_hash: String, state: State<'_, AppState>)
            -> Result<RepoStatus>

        #[tauri::command]
        pub async fn unstage_hunk(...cùng tham số...) -> Result<RepoStatus>

    Cộng hai hàm thuần tách khỏi `#[tauri::command]` để test tích hợp gọi được mà không
    dựng `App` có webview — đúng khuôn `lay_trang_thai`/`stage_duong_dan` đã có.

    Đường đi của `stage_hunk`:
      1. `kiem_blob_hash` — khác → `FileChanged`, **dừng**, không lệnh nào nữa.
      2. `git diff --no-ext-diff -U3 -- <path>` (unstage thì thêm `--cached`), lấy
         **`stdout`** thô.
      3. `tach_hunk_tho` + `dung_ban_va_mot_hunk(chi_so)`.
      4. `git apply --check --cached --recount` với bản vá qua `stdin_bytes`.
         Thất bại → `FileChanged`. **Không thử lại bằng cờ khác.**
      5. `git apply --cached --recount` (unstage: thêm `--reverse`), qua
         `chay_lenh_ghi_co_thu_lai_phan_loai` để giữ đường thử lại `index.lock`.
      6. Trả `lay_trang_thai(...)`.

    ### Test tích hợp trên git thật (`tests/hunk_commands.rs`)

    Dựng repo tạm bằng `tempfile` theo khuôn `repo_ban()` của `worktree_commands.rs`.

    **Test A — stage hunk THỨ HAI của tệp ba hunk.** Sau khi chạy:
      * `git diff --cached` cho **đúng 1** hunk, và nội dung của nó là thay đổi ở
        **giữa** tệp (khẳng định chuỗi `M2`/`CHANGED15` có trong diff cached);
      * `git diff` (chưa stage) còn **đúng 2** hunk.
      🔴 Khẳng định **cả hai** số. Chỉ khẳng định cái đầu thì một cài đặt stage **cả
      tệp** vẫn xanh ở vế "cached có hunk đó".

    **Test B — 🔴 ca `--recount` ĐÃ ĐO.** Xem `<recount_fixture>` bên dưới — đây là
    phần quan trọng nhất của plan, đọc nó nguyên vẹn trước khi viết test.

    **Test C — WORK-04, đo bằng byte.** Tệp CRLF + không dòng cuối + byte `0xE9`, ba
    hunk, stage hunk giữa. Sau đó khẳng định trên **tệp worktree**:
      * số byte `\r` **không đổi** so với trước;
      * byte cuối cùng **không** phải `\n`;
      * byte `0xE9` vẫn có mặt.
    Đọc bằng `std::fs::read` → `Vec<u8>`, so trên byte. **Không** `read_to_string` —
    nó sẽ lỗi hoặc lossy trên `0xE9` và test sẽ nói dối.

    **Test D — WORK-05.** Sau khi lấy `blob_hash`, ghi đè tệp bằng nội dung khác, rồi
    gọi `stage_hunk` với hash cũ. Khẳng định `err.code() == "file_changed"` **và**
    `git diff --cached` vẫn **rỗng** (không có gì lọt vào index).

    **Test E — tệp nhị phân (R7).** `git diff` trên tệp nhị phân in `Binary files ...
    differ` và **không** có `@@`. `tach_hunk_tho` cho `hunks.len() == 0` →
    `dung_ban_va_mot_hunk` trả `None` → lỗi rõ ràng, **không panic** và không áp gì.

    **Test F — unstage hunk.** Stage cả tệp, rồi `unstage_hunk` một hunk; khẳng định
    index còn đúng các hunk kia.
  </behavior>
  <recount_fixture>
🔴 **ĐỌC NGUYÊN VẸN. Fixture cho `--recount` đã được đo trên git 2.54.0.windows.1, và
phép đo cho một kết quả TRÁI với giả định trong ROADMAP.**

Giả định tự nhiên là: "chọn hunk thứ hai trong ba hunk, bỏ `--recount` thì đỏ". **Đã đo
— sai.** Repo 26 dòng, sửa dòng 2/13/25, `-U3`, lấy hunk 2 với header chép **nguyên
văn**:

    @@ -10,7 +10,7 @@ i
     j
     k
     l
    -m
    +M2
     n
     o
     p

    git apply --check --cached --recount h2.patch  → exit 0
    git apply --check --cached           h2.patch  → exit 0      ← KHÔNG ĐỎ

Lý do: số đếm trong `@@` là **của riêng hunk đó**, không phải tích luỹ qua cả tệp. Chép
nguyên văn header của hunk 2 thì nó **vẫn đúng**. Đây đúng lớp lỗi "hình dạng đúng, dữ
liệu vô hại" của CONTEXT.md 4.2 — fixture trông như đang kiểm `--recount` nhưng không
kiểm gì.

**Đột biến không biểu diễn được lỗi thì không nói gì về cổng** (CONTEXT.md 4.1). Nên
fixture phải làm header **thật sự sai**. Cái làm nó sai là **cắt bớt dòng thân** trong
khi giữ số đếm cũ — đúng việc mà một bộ dựng bản vá con làm khi nó tỉa ngữ cảnh:

    @@ -10,7 +10,7 @@ i      ← nói 7 dòng
     j
     k
     l
    -m
    +M2
     n                       ← chỉ còn 5 dòng thân

Đo được:

    git apply --check --cached --recount h2_trim.patch  → exit 0
    git apply --check --cached           h2_trim.patch  → exit 128
                                    error: corrupt patch at h2_trim.patch:12

**Đây là fixture phân biệt được, và nó được CHỨNG MINH chứ không được chỉ định.**

### Cách viết Test B

Test B **không** gọi `stage_hunk` (cài đặt đúng sẽ không sinh header lệch). Nó kiểm
trực tiếp rằng lệnh `git apply` của ta mang `--recount`, bằng cách dựng bản vá cắt-thân
rồi chạy **cùng tập cờ** mà `hunk.rs` dùng:

  1. Trích tập cờ từ mã: `hunk.rs` khai chúng trong một hằng
     `const CO_APPLY: &[&str] = &["apply", "--cached", "--recount"];` để test đọc được
     **cùng một nguồn** thay vì chép lại. Chép lại là hai nguồn lệch nhau — đúng lỗi
     mà `STATUS_ARGS` đã tồn tại để tránh.
  2. Dựng bản vá cắt-thân từ `ba-khoi.txt` của fixture 05-01.
  3. Chạy `git` với `CO_APPLY` + `--check` → khẳng định **exit 0**.
  4. Chạy lại với `CO_APPLY` **đã lọc bỏ** `--recount` → khẳng định **exit khác 0**.

Bước 4 là phần làm cổng có khả năng đỏ: nó chứng minh fixture phân biệt được **ngay
trong chính test**, nên cổng không thể âm thầm mất hiệu lực. Nếu ai bỏ `--recount` khỏi
`CO_APPLY`, bước 3 đỏ.

🔴 Khẳng định tiền đề trước bước 3:

    assert!(CO_APPLY.contains(&"--recount"),
        "🔴 tiền đề sai: CO_APPLY không có `--recount`, test này không kiểm gì");
  </recount_fixture>
  <action>
    Cài hai command theo `<behavior>`. Khai hằng `CO_APPLY` ở cấp module, `pub(crate)`
    để test tích hợp đọc được (test tích hợp là crate riêng — cần `pub`).

    Đăng ký `stage_hunk`, `unstage_hunk` vào `generate_handler![...]` trong `lib.rs`.
    🔴 Thiếu bước này thì mã biên dịch **sạch ở cả hai phía** và command không tồn tại
    lúc chạy — lớp lỗi mà `worktree.rs` đã ghi chú ở dòng 881.

    Viết `tests/hunk_commands.rs` theo khuôn `tests/worktree_commands.rs`: hàm `git()`
    đồng bộ để dựng bối cảnh, `tempfile::tempdir()`, `AppState::new()` + `open_repo`.

    🔴 Repo tạm của Test C đặt `core.autocrlf true` **tường minh**; các repo tạm khác
    đặt `false` (như `repo_ban()` đã làm) để một chuyển đổi CRLF không làm `git status`
    báo tệp sửa ngoài ý muốn.

    **Bảng đột biến** — chạy thật, ghi kết quả đo:

    | # | Đột biến | Đỏ mong đợi |
    |---|---|---|
    | M6 | Bỏ `--recount` khỏi `CO_APPLY` | Test B bước 3 |
    | M7 | Bỏ bước `--check`, áp thẳng | Test D — index bị bẩn |
    | M8 | `kiem_blob_hash` luôn trả `Ok(())` | Test D — `code()` khác `file_changed` |
    | M9 | Đổi `dung_ban_va_mot_hunk(i)` thành `(0)` | Test A — sai hunk vào index |
    | M10 | Ghép `stderr` vào bản vá trước khi apply | Test A hoặc C |
    | M11 | So blob hash bằng `starts_with` thay vì `==` | (dự đoán: **không đỏ** — hash đủ dài thì tiền tố hiếm khi khớp. Nếu 0 đỏ, **kiểm xem có test nào hỏi về điều này chưa** — lỗi #9 — rồi viết test dựng hash cắt ngắn thay vì ghi "chấp nhận được") |

    🔴 Hoàn nguyên đột biến bằng `cp` từ bản sao trong scratchpad, **không**
    `git checkout --` (CONTEXT.md 4.1).

    🔴 `cargo test --lib --tests` có thể thất bại `os error 5` nếu `git-plum.exe` đang
    chạy. **Không kill nó** — cổng dogfood cần app chạy được. Báo `--lib` là **đo được**
    và `--tests` là **chưa đo** trong SUMMARY, đúng như CONTEXT.md mục 5 yêu cầu.
  </action>
  <verify>
    <automated>cd src-tauri &amp;&amp; cargo test --test hunk_commands 2>&amp;1 | tee /tmp/hc.txt | tail -20 &amp;&amp; bash -c 'N=$(grep -oE "[0-9]+ passed" /tmp/hc.txt | head -1 | grep -oE "[0-9]+"); F=$(grep -oE "[0-9]+ failed" /tmp/hc.txt | head -1 | grep -oE "[0-9]+"); echo "[passed=$N failed=$F]"; test -n "$N" || { echo "🔴 không đọc được số test — suite có thể đã chạy 0 test (lỗi #8)"; exit 1; }; test "$N" -ge 6 || { echo "🔴 cần >= 6 test tích hợp, có $N"; exit 1; }; test "${F:-0}" -eq 0 || exit 1'</automated>
  </verify>
  <done>
    Sáu test tích hợp xanh trên git thật. Test B chứng minh **trong chính nó** rằng bỏ
    `--recount` làm bản vá cắt-thân thất bại. `stage_hunk`/`unstage_hunk` có trong
    `generate_handler!`. Bảng đột biến 6 hàng có kết quả đo.
  </done>
</task>

<task type="auto">
  <name>Task 3: Cổng "không bao giờ thử lại mờ"</name>
  <files>src-tauri/src/commands/hunk.rs</files>
  <action>
    Test đọc mã nguồn khẳng định đường apply **không** chứa cờ khớp mờ. Đây là ràng buộc
    ROADMAP xếp "không thương lượng", và nó là loại ràng buộc mà một lần "sửa cho nó
    chạy" trong tương lai sẽ phá — nên nó cần cổng, không cần lời hứa.

    🔴 **Bỏ chú thích trước khi tìm** (lỗi #1, #5) — tệp này có văn xuôi giải thích *vì
    sao không dùng* `--whitespace=fix`, nên grep thô sẽ khớp chính lời giải thích đó:

        let src = include_str!("hunk.rs");
        let than: String = src.lines()
            .filter(|l| { let t = l.trim_start();
                          !t.starts_with("//") &amp;&amp; !t.starts_with("///") &amp;&amp; !t.starts_with("//!") })
            .collect::&lt;Vec&lt;_&gt;&gt;().join("\n");
        let than = than.split("mod tests").next().unwrap();

    🔴 **Khẳng định tiền đề** (lỗi #3):

        assert!(than.contains("CO_APPLY"),
            "🔴 tiền đề sai: không thấy `CO_APPLY` trong thân đã lọc — cổng đang tìm \
             trong tệp rỗng và sẽ XANH với mọi mã");

    Rồi:

        for cam in ["--whitespace=fix", "--unidiff-zero", "--3way", "--reject", "-C1"] {
            assert!(!than.contains(cam),
                "🔴 `{cam}` là khớp mờ — ROADMAP xếp 'không thương lượng'. \
                 Thất bại phải thành FileChanged, không thành một lần thử lại lỏng hơn");
        }

    Và khẳng định tích cực rằng `--recount` **có** mặt — cổng phủ định một mình xanh
    trên một tệp không có lệnh apply nào:

        assert!(than.contains("--recount"),
            "🔴 `--recount` phải có trên đường apply");

    **Đột biến kiểm cổng:**

    | # | Đột biến | Đỏ mong đợi |
    |---|---|---|
    | M12 | Thêm `"--whitespace=fix"` vào `CO_APPLY` | cổng này |
    | M13 | Thêm `--3way` **chỉ trong một chú thích** | 🔴 **không được đỏ** — nếu đỏ thì phép lọc chú thích hỏng, đúng lỗi #5 |

    M13 là đột biến kiểm **chính cổng**, không kiểm mã. Nó phải cho **0 đỏ**; nếu nó đỏ
    thì cổng đang khớp chú thích và mọi kết luận khác từ cổng này vô giá trị.
  </action>
  <verify>
    <automated>cd src-tauri &amp;&amp; cargo test --lib hunk 2>&amp;1 | tail -10 &amp;&amp; cargo test --lib 2>&amp;1 | tee /tmp/all2.txt | tail -3 &amp;&amp; bash -c 'N=$(grep -oE "[0-9]+ passed" /tmp/all2.txt | head -1 | grep -oE "[0-9]+"); echo "[lib passed=$N]"; test -n "$N" || exit 1; test "$N" -ge 309 || { echo "🔴 tụt dưới mốc 309"; exit 1; }'</automated>
  </verify>
  <done>
    Cổng xanh, M12 **đã đo là đỏ**, M13 **đã đo là 0 đỏ** (chứng minh phép lọc chú thích
    hoạt động). `cargo test --lib` >= 309.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| webview → `stage_hunk` | `path`, `hunk_index`, `blob_hash` đều là chuỗi/số tuỳ ý từ JS |
| bản vá → `git apply --cached` | byte đi thẳng vào index, tức vào dữ liệu người dùng |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-04 | Tampering | `stage_hunk` pathspec | mitigate | `--` trước pathspec ở **mọi** lệnh (T-02-11); một tệp tên `main` hay `-rf` không thành revision/cờ |
| T-05-05 | Tampering | bản vá áp sai vị trí | mitigate | `--check --cached --recount` trước, blob hash trước, không thử lại mờ (cổng Task 3) |
| T-05-06 | Elevation | `hunk_index` ngoài biên | mitigate | `dung_ban_va_mot_hunk` trả `Option`, không index trực tiếp; Test E ghim |
| T-05-07 | Denial of service | tệp rất nhiều hunk | mitigate | Dùng lại cổng `MAX_DIFF_BLOB_BYTES` của `diff.rs` trước khi chạy `git diff` |
| T-05-08 | Repudiation | lệnh apply không vào nhật ký | mitigate | Đi qua `runner` (PLAT-08), không tự spawn |
</threat_model>

<verification>
- `cargo test --test hunk_commands` >= 6 test, 0 failed, **có in số** trước khi tin.
- `cargo test --lib` >= 309.
- Test B chứng minh fixture phân biệt `--recount` **bên trong chính test**.
- Cổng Task 3: M12 đỏ, M13 **không** đỏ.
- `stage_hunk`/`unstage_hunk` có trong `generate_handler!` (kiểm bằng grep có tiền đề).
</verification>

<success_criteria>
Stage và unstage một khối chạy đúng trên git thật, giữ nguyên byte, và từ chối bản vá
không khớp bằng một lỗi có mã. Toàn bộ kiểm được **không cần mở ứng dụng**.
</success_criteria>

<output>
`.planning/phases/05-hunk-staging/05-02-SUMMARY.md`. Ghi số đo thật, bảng đột biến
M6–M13 với kết quả **đo được**, và nói rõ `cargo test --lib --tests` **đo được hay
không** (nếu `os error 5` vì app đang chạy thì ghi "chưa đo", không suy ra).
</output>
