---
phase: 05-hunk-staging
plan: 03
type: execute
wave: 3
depends_on: [05-01, 05-02]
files_modified:
  - src-tauri/src/commands/trash.rs
  - src-tauri/src/commands/mod.rs
  - src-tauri/src/domain/trash.rs
  - src-tauri/src/domain/mod.rs
  - src-tauri/src/lib.rs
  - src-tauri/tests/trash_commands.rs
autonomous: true
requirements: [WORK-06, WORK-07]
must_haves:
  truths:
    - "Trước mỗi lần huỷ, nội dung bị huỷ được lưu thành object git CÓ THỂ TỚI ĐƯỢC"
    - "Huỷ theo tệp và huỷ theo khối đều đi qua đường lưu đó — không đường nào bỏ qua"
    - "Người dùng khôi phục lại được nội dung vừa huỷ từ danh sách"
    - "Tệp CHƯA THEO DÕI cũng được lưu — `git stash create` trả RỖNG cho ca này"
  artifacts:
    - path: "src-tauri/src/commands/trash.rs"
      provides: "luu_truoc_khi_huy / huy_tep / huy_hunk / danh_sach_thung_rac / khoi_phuc"
      contains: "refs/git-plum-trash/"
    - path: "src-tauri/tests/trash_commands.rs"
      provides: "Vòng huỷ → khôi phục trên git thật"
  key_links:
    - from: "src-tauri/src/commands/trash.rs"
      to: "refs/git-plum-trash/"
      via: "git update-ref"
      pattern: "update-ref"
---

<objective>
An toàn khi huỷ — WORK-06 và WORK-07. Mọi đường huỷ **phải** đi qua một lần lưu trước,
và object lưu ra phải **tới được** để `git gc` không dọn mất nó.

Purpose: WORK-07 tồn tại vì đúng một lý do: huỷ nhầm mất việc (R3). Đây là phase đầu
tiên ghi vào **nội dung tệp** của người dùng chứ không chỉ vào index, nên một lần huỷ
sai là mất dữ liệu thật, không phải một hiển thị sai.

Output: bốn command Tauri + bộ test tích hợp vòng huỷ→khôi phục trên git thật. Vẫn
**không** phụ thuộc giao diện.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-hunk-staging/CONTEXT.md
@.planning/phases/05-hunk-staging/05-02-SUMMARY.md

@src-tauri/src/commands/worktree.rs
@src-tauri/src/commands/hunk.rs
@src-tauri/tests/worktree_commands.rs

<interfaces>
Từ 05-02 (đã có):
  pub(crate) const CO_APPLY: &[&str]           // ["apply", "--cached", "--recount"]
  pub fn dung_ban_va_mot_hunk(p: &PatchTho, chi_so: usize) -> Option<Vec<u8>>
  GitError::FileChanged { path: String }       // code() == "file_changed"

Từ worktree.rs — DÙNG LẠI cho mọi lệnh ghi:
  pub(crate) async fn chay_lenh_ghi_co_thu_lai_phan_loai(...) -> Result<GitOutput>
  pub async fn lay_trang_thai(state: &AppState, repo: Arc<RepoHandle>) -> Result<RepoStatus>

Từ domain/status.rs:
  pub enum StatusGroup { Staged, Unstaged, Untracked }   // đã có, dùng để phân nhánh "Xoá"
</interfaces>
</context>

<measured_ground_truth>
🔴 **Đã đo trên git 2.54.0.windows.1. Hai kết quả dưới đây trái với giả định tự nhiên —
đừng thiết kế lại theo trực giác.**

**1. `git stash create` trả về RỖNG khi chỉ có tệp chưa theo dõi.**

    $ printf 'untracked content\n' > u.txt     # tệp DUY NHẤT thay đổi
    $ git stash create "t2"
    []                                          ← chuỗi rỗng, exit 0

Nó **im lặng không lưu gì**. Một cài đặt tin `stash create` luôn trả sha sẽ ghi một ref
trỏ vào chuỗi rỗng (hoặc bỏ qua), và người dùng mất tệp chưa theo dõi mà **không có
thông báo nào**. Đây đúng là ca mà ROADMAP bắt dùng động từ **"Xoá"** — tức ca này chắc
chắn xảy ra trong giao diện.

Đường thay thế **đã đo là chạy được**:

    $ B=$(git hash-object -w -- u.txt)
    $ echo $B
    3f4c56d3d8d73bb43575b07d84c82e30210900d1
    $ git cat-file -p $B
    untracked content

**2. `git update-ref` chấp nhận trỏ thẳng vào một BLOB, không chỉ commit.**

    $ git update-ref refs/git-plum-trash/blobtest 3f4c56d3...
    exit=0
    $ git for-each-ref refs/git-plum-trash/
    96b5c6f6... commit  refs/git-plum-trash/1790087324
    3f4c56d3... blob    refs/git-plum-trash/blobtest

Nên **cả hai** dạng object đều neo được dưới cùng một không gian ref. Không cần dựng
commit giả cho tệp chưa theo dõi.

**3. Vòng huỷ → khôi phục đã đo đầy đủ:**

    $ printf 'one\nCHANGED\nthree\n' > a.txt
    $ SHA=$(git stash create "trash")           # 96b5c6f6...
    $ git update-ref refs/git-plum-trash/1790087324 $SHA
    $ git checkout -- a.txt                     # huỷ
    $ cat a.txt                                 → one/two/three  (đã mất CHANGED)
    $ git show refs/git-plum-trash/1790087324:a.txt
    one
    CHANGED
    three                                       ← khôi phục được
</measured_ground_truth>

<constraints>
🔴 **Không đường huỷ nào được bỏ qua bước lưu.** Cài bằng **kiểu**, không bằng kỷ luật:
hàm huỷ nhận một `BienNhan` mà chỉ `luu_truoc_khi_huy` tạo ra được. Một đường huỷ mới
trong tương lai sẽ **không biên dịch được** nếu nó quên lưu. Xem Task 1.

🔴 **Với tệp chưa theo dõi, động từ là "Xoá"** — ROADMAP, không thương lượng. Phía Rust
ghim điều này bằng cách trả nhãn động từ trong kiểu, để giao diện không tự đoán.

🔴 Không tự spawn git. Mọi lệnh ghi qua `chay_lenh_ghi_co_thu_lai_phan_loai`.
</constraints>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: `luu_truoc_khi_huy` + `BienNhan` — ràng buộc bằng kiểu</name>
  <files>src-tauri/src/domain/trash.rs, src-tauri/src/domain/mod.rs, src-tauri/src/commands/trash.rs, src-tauri/src/commands/mod.rs</files>
  <behavior>
    Kiểu miền:

        pub struct MucThungRac {
            pub ref_name: String,      // "refs/git-plum-trash/<epoch_ms>-<n>"
            pub object_id: String,     // commit (stash) HOẶC blob (tệp chưa theo dõi)
            pub la_blob: bool,         // phân nhánh cách khôi phục
            pub paths: Vec<String>,
            pub luc: i64,              // giây Unix — Intl.DateTimeFormat lo định dạng
            pub nhan: String,          // "Huỷ bỏ 2 tệp" / "Xoá bar.txt"
        }

        /// Bằng chứng đã lưu xong. 🔴 Trường là private và KHÔNG có hàm dựng công khai:
        /// chỉ `luu_truoc_khi_huy` trong module này tạo được. Một đường huỷ mới quên
        /// lưu sẽ không biên dịch được, thay vì lặng lẽ mất dữ liệu.
        pub struct BienNhan { muc: MucThungRac }

    `pub async fn luu_truoc_khi_huy(state, repo, paths: &[String]) -> Result<BienNhan>`:

      1. `git stash create "git-plum trash"` → stdout cắt khoảng trắng.
      2. 🔴 **Rỗng** → ca tệp chưa theo dõi (đã đo). Rơi sang `git hash-object -w --
         <path>` cho **từng** đường dẫn. Nhiều tệp chưa theo dõi → nhiều ref, hoặc một
         ref cho mỗi tệp; đơn giản hơn là một `MucThungRac` cho mỗi tệp.
      3. `git update-ref refs/git-plum-trash/<epoch_ms>-<n> <oid>`.
      4. 🔴 **Khẳng định ref đọc lại được** bằng `git rev-parse --verify <ref>` trước
         khi trả `BienNhan`. Nếu `update-ref` thất bại mà ta vẫn trả biên nhận thì kiểu
         đang **nói dối** và ràng buộc mất hết giá trị. Thất bại → `Err`, **không huỷ**.

    Test 1 — repo tạm, sửa một tệp đã theo dõi, `luu_truoc_khi_huy` → `git rev-parse
    --verify <ref_name>` thoát 0 và `git show <ref>:<path>` cho nội dung **đã sửa**.

    Test 2 — 🔴 **ca chưa theo dõi.** Repo tạm có **duy nhất** một tệp chưa theo dõi
    thay đổi. Khẳng định `luu_truoc_khi_huy` vẫn trả `Ok` và `git cat-file -p <oid>`
    cho đúng nội dung. Một cài đặt chỉ dùng `stash create` sẽ **đỏ ở đây** — đó là điểm
    của test.
    🔴 Fixture phải có **duy nhất** tệp chưa theo dõi đó thay đổi. Nếu repo còn một tệp
    đã theo dõi bẩn, `stash create` trả sha khác rỗng và ca này **không phân biệt được
    gì** — đúng lớp lỗi "hình dạng đúng, dữ liệu vô hại" (CONTEXT.md 4.2).

    Test 3 — `BienNhan` không dựng được từ ngoài module. Test **đọc mã nguồn** khẳng
    định `domain/trash.rs` không có `impl BienNhan` với `pub fn new`. Bỏ chú thích
    trước khi tìm, khẳng định tiền đề `than.contains("struct BienNhan")` trước.

    Test 4 — ref nằm dưới đúng `refs/git-plum-trash/`. 🔴 **Không** dưới `refs/stash`
    (ghi đè stash thật của người dùng) và **không** dưới `refs/heads` (hiện thành nhánh
    trong thanh bên).
  </behavior>
  <action>
    Tạo `domain/trash.rs` (kiểu thuần, có `Serialize`) và `commands/trash.rs` (lệnh git).

    🔴 `#[serde(rename_all = "camelCase")]` trên `MucThungRac`, và test liệt kê **toàn
    bộ** khoá JSON rồi so bằng — lớp lỗi `rename_all` đã cắn ở Phase 3 (`domain/diff.rs`
    ghi lại) và nó **không** gây lỗi biên dịch ở bên nào.

    Tên ref dùng `<epoch_ms>-<n>` chứ không `<epoch_s>`: hai lần huỷ trong cùng một giây
    là chuyện bình thường khi người dùng bấm nhanh, và trùng tên ref nghĩa là lần huỷ
    thứ hai **ghi đè** bản lưu của lần thứ nhất — mất đúng dữ liệu mà cơ chế này tồn tại
    để giữ.
  </action>
  <verify>
    <automated>cd src-tauri &amp;&amp; cargo test --lib trash 2>&amp;1 | tee /tmp/tr.txt | tail -12 &amp;&amp; bash -c 'N=$(grep -oE "[0-9]+ passed" /tmp/tr.txt | head -1 | grep -oE "[0-9]+"); echo "[passed=$N]"; test -n "$N" || { echo "🔴 không đọc được số test"; exit 1; }; test "$N" -ge 2 || { echo "🔴 cần >= 2 test đơn vị, có $N"; exit 1; }'</automated>
  </verify>
  <done>
    `BienNhan` không dựng được ngoài module; `luu_truoc_khi_huy` lưu được **cả** tệp đã
    theo dõi (qua stash) **và** tệp chưa theo dõi (qua `hash-object -w`); ref đọc lại
    được bằng `rev-parse --verify` trước khi biên nhận được trả.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: `huy_tep` / `huy_hunk` — mỗi đường đều đòi `BienNhan`</name>
  <files>src-tauri/src/commands/trash.rs, src-tauri/src/lib.rs, src-tauri/tests/trash_commands.rs</files>
  <behavior>
        pub async fn huy_tep(state, repo, paths: &[String], bien_nhan: BienNhan)
            -> Result<RepoStatus>
        pub async fn huy_hunk(state, repo, path: &str, hunk_index: usize,
                              blob_hash: &str, bien_nhan: BienNhan) -> Result<RepoStatus>

    `huy_tep`: tệp **đã theo dõi** → `git checkout -- <path>`; tệp **chưa theo dõi** →
    xoá tệp trên đĩa (`tokio::fs::remove_file`). Phân nhánh theo `StatusGroup` đọc từ
    `lay_trang_thai`, **không** đoán theo việc tệp có trong index hay không.

    `huy_hunk`: dựng bản vá một hunk (05-01), rồi `git apply --reverse` **trên worktree**
    (không `--cached`), có `--recount`, có `--check` trước. Kiểm `blob_hash` trước —
    cùng hợp đồng WORK-05 của 05-02.

    Command Tauri: `discard_files`, `discard_hunk`, `list_trash`, `restore_trash`. Mỗi
    command tự gọi `luu_truoc_khi_huy` rồi chuyển `BienNhan` xuống — **giao diện không
    bao giờ** gọi được đường huỷ mà bỏ qua bước lưu, vì command là điểm vào duy nhất.

    `pub async fn danh_sach_thung_rac(...) -> Result<Vec<MucThungRac>>`:
    `git for-each-ref refs/git-plum-trash/` → sắp **mới nhất trước**. Chặn trên 50 mục
    hiển thị (danh sách "Vừa huỷ gần đây", không phải kho lưu trữ).

    `pub async fn khoi_phuc(...)`: `la_blob` → `git cat-file -p <oid>` ghi thẳng ra tệp
    (byte thô, `Vec<u8>`, **không** qua String — nội dung tệp người dùng);
    ngược lại → `git checkout <ref> -- <paths>`.

    ### Test tích hợp (`tests/trash_commands.rs`) — git thật, repo tempfile

    **Test A — vòng huỷ→khôi phục tệp đã theo dõi.** Sửa tệp, `discard_files`, khẳng
    định nội dung **đã mất** trên đĩa, rồi `restore_trash` và khẳng định nội dung **trở
    lại đúng byte cũ**. 🔴 Khẳng định **cả hai** vế — chỉ khẳng định vế khôi phục thì
    một cài đặt **không huỷ gì cả** cũng xanh.

    **Test B — vòng huỷ→khôi phục tệp CHƯA THEO DÕI.** Sau `discard_files` tệp **không
    còn trên đĩa**; sau `restore_trash` nó **trở lại** với đúng nội dung.

    **Test C — huỷ theo khối.** Tệp ba hunk, `discard_hunk(1)`. Khẳng định hunk giữa
    **mất** khỏi worktree và hai hunk kia **còn** (đếm `@@` = 2). Rồi khôi phục và
    khẳng định lại đủ 3.

    **Test D — 🔴 object còn sống sau `git gc --prune=now`.** Đây là test chứng minh
    "có thể tới được" thật sự, không phải chỉ ghi ref cho có:
      1. huỷ (tạo trash ref);
      2. `git gc --prune=now --aggressive` trong repo tạm;
      3. `git show <ref>:<path>` vẫn cho nội dung cũ.
    Một cài đặt dùng `stash create` mà **quên** `update-ref` sẽ xanh ở Test A (object
    còn trong repo) và **đỏ ở đây**. Đó là điểm của test.

    **Test E — byte không UTF-8 qua vòng khôi phục.** Tệp chứa `0xE9`, huỷ rồi khôi
    phục, so **byte với byte** bằng `std::fs::read`. Không `read_to_string`.
  </behavior>
  <action>
    Cài theo `<behavior>`. Đăng ký cả bốn command vào `generate_handler!` trong `lib.rs`
    — thiếu bước này biên dịch vẫn sạch ở cả hai phía.

    🔴 `git gc` trong Test D chạy trong repo `tempfile`, **không bao giờ** trong repo dự
    án và **không bao giờ** trong `D:/MyCompanyProjects/quanly-truong-phong-so` (repo
    thật của chủ dự án, chỉ-đọc).

    **Bảng đột biến:**

    | # | Đột biến | Đỏ mong đợi |
    |---|---|---|
    | M14 | Bỏ `git update-ref`, chỉ giữ `stash create` | **Test D** (Test A vẫn xanh — đó là lý do D tồn tại) |
    | M15 | `luu_truoc_khi_huy` bỏ nhánh `hash-object -w` cho tệp chưa theo dõi | Test B |
    | M16 | `huy_hunk` bỏ `--reverse` | Test C |
    | M17 | `khoi_phuc` đọc qua `String::from_utf8_lossy` | Test E |
    | M18 | Bỏ bước `rev-parse --verify` trong `luu_truoc_khi_huy` | (dự đoán: **không đỏ** ở đường hạnh phúc. Nếu 0 đỏ → **kiểm xem có test nào hỏi về nó chưa** (lỗi #9); nếu rỗng, viết test dựng ref-name không hợp lệ thay vì ghi "chấp nhận được") |

    🔴 Hoàn nguyên đột biến bằng `cp` từ scratchpad. **Không** `git checkout --` trên
    tệp chưa commit (CONTEXT.md 4.1 — đã xoá mất việc của một executor hôm nay).
  </action>
  <verify>
    <automated>cd src-tauri &amp;&amp; cargo test --test trash_commands 2>&amp;1 | tee /tmp/tc.txt | tail -20 &amp;&amp; bash -c 'N=$(grep -oE "[0-9]+ passed" /tmp/tc.txt | head -1 | grep -oE "[0-9]+"); F=$(grep -oE "[0-9]+ failed" /tmp/tc.txt | head -1 | grep -oE "[0-9]+"); echo "[passed=$N failed=$F]"; test -n "$N" || { echo "🔴 suite có thể chạy 0 test — lỗi #8"; exit 1; }; test "$N" -ge 5 || { echo "🔴 cần >= 5 test, có $N"; exit 1; }; test "${F:-0}" -eq 0 || exit 1'</automated>
  </verify>
  <done>
    Năm test tích hợp xanh. Test D chứng minh object sống qua `git gc --prune=now`.
    M14 **đã đo là đỏ ở D và xanh ở A** — đó là bằng chứng D không thừa.
  </done>
</task>

<task type="auto">
  <name>Task 3: Nhãn động từ "Xoá" cho tệp chưa theo dõi</name>
  <files>src-tauri/src/domain/trash.rs, src-tauri/src/commands/trash.rs</files>
  <action>
    ROADMAP: *"Với tệp chưa theo dõi, động từ phải là **Xoá**, không phải Huỷ bỏ."*

    🔴 Ghim ở phía **Rust**, không để giao diện tự đoán. Giao diện đoán nghĩa là hai
    nguồn sự thật cho cùng một câu hỏi, và nhánh đoán đó không có test nào ở tầng Rust.

        /// Động từ hiện cho người dùng. `Xoa` với tệp chưa theo dõi — nội dung đó
        /// KHÔNG có ở đâu khác, nên "Huỷ bỏ" nói sai mức độ nghiêm trọng.
        #[derive(Serialize)] #[serde(rename_all = "camelCase")]
        pub enum DongTu { HuyBo, Xoa }

        pub fn dong_tu_cho(group: StatusGroup) -> DongTu

    Test 1 — `dong_tu_cho(StatusGroup::Untracked) == DongTu::Xoa`; hai nhóm kia trả
    `HuyBo`. 🔴 Khẳng định **cả ba** nhánh — chỉ khẳng định nhánh `Untracked` thì một
    cài đặt trả `Xoa` cho **mọi** thứ vẫn xanh (lớp lỗi "fixture không phân biệt được",
    #4 của CONTEXT.md 4.1).

    Test 2 — chuỗi hiển thị đúng dấu: `"Xoá"` (có dấu sắc trên `o`), không `"Xoa"`.
    Nếu chuỗi hiện cho người dùng sinh ở phía Rust, khẳng định nguyên văn.

    Test 3 — serialize: `DongTu::Xoa` ra JSON là `"xoa"`, và test liệt kê **toàn bộ**
    giá trị có thể thay vì kiểm một giá trị.

    **Đột biến:**

    | # | Đột biến | Đỏ mong đợi |
    |---|---|---|
    | M19 | `dong_tu_cho` trả `HuyBo` cho mọi nhóm | Test 1 |
    | M20 | `dong_tu_cho` trả `Xoa` cho mọi nhóm | Test 1 — 🔴 nếu **không** đỏ thì test chỉ kiểm một nhánh, sửa test |

    M20 là đột biến kiểm **chính test**, đúng bài học "fixture phải được chứng minh phân
    biệt được". Chạy cả hai và ghi kết quả đo.
  </action>
  <verify>
    <automated>cd src-tauri &amp;&amp; cargo test --lib dong_tu 2>&amp;1 | tail -10 &amp;&amp; cargo test --lib 2>&amp;1 | tee /tmp/all3.txt | tail -3 &amp;&amp; bash -c 'N=$(grep -oE "[0-9]+ passed" /tmp/all3.txt | head -1 | grep -oE "[0-9]+"); echo "[lib passed=$N]"; test -n "$N" || exit 1; test "$N" -ge 309 || { echo "🔴 tụt dưới mốc 309"; exit 1; }'</automated>
  </verify>
  <done>
    Cả ba nhánh `StatusGroup` có khẳng định riêng; M19 **và** M20 đều **đã đo là đỏ**.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| webview → `discard_*` | đường dẫn và chỉ số hunk tuỳ ý; đây là đường **mất dữ liệu** |
| trash ref → worktree | `restore_trash` ghi byte vào tệp người dùng |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-09 | Tampering | đường huỷ bỏ qua bước lưu | mitigate | `BienNhan` private, chỉ `luu_truoc_khi_huy` dựng được → không biên dịch được nếu quên |
| T-05-10 | Denial of service | trash ref tích luỹ vô hạn | accept | Object nhỏ, người dùng hiếm khi huỷ hàng nghìn lần; dọn định kỳ để v2. Ghi rõ trong SUMMARY |
| T-05-11 | Tampering | `restore_trash` ghi đè tệp đang sửa | mitigate | Khôi phục cũng gọi `luu_truoc_khi_huy` trước — khôi phục là một lần ghi đè, cùng lớp rủi ro |
| T-05-12 | Tampering | ref name từ webview | mitigate | `restore_trash` chỉ nhận ref **có tiền tố** `refs/git-plum-trash/`; từ chối mọi ref khác, tránh `git checkout refs/heads/main -- .` |
</threat_model>

<verification>
- `cargo test --test trash_commands` >= 5 test, 0 failed, **in số trước khi tin**.
- `cargo test --lib` >= 309.
- Test D (`git gc --prune=now`) xanh — chứng minh "tới được", không chỉ "đã tạo".
- M14 đỏ ở Test D và xanh ở Test A (chứng minh D không thừa).
- M19 và M20 đều đỏ (chứng minh test nhánh động từ phân biệt được cả hai chiều).
</verification>

<success_criteria>
Mọi đường huỷ đi qua một lần lưu mà **kiểu** bắt buộc; object lưu ra sống qua
`git gc --prune=now`; tệp chưa theo dõi — ca mà `git stash create` im lặng bỏ qua —
được lưu bằng đường riêng và khôi phục được.
</success_criteria>

<output>
`.planning/phases/05-hunk-staging/05-03-SUMMARY.md`. Bảng đột biến M14–M20 với kết quả
**đo**. Nếu M18 sống sót, ghi kết quả của phép kiểm "có test nào hỏi về nó chưa".
</output>
