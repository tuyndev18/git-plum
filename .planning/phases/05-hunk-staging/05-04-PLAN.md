---
phase: 05-hunk-staging
plan: 04
type: execute
wave: 4
depends_on: [05-02, 05-03]
files_modified:
  - src/lib/ipc.ts
  - src/stores/hunkStore.ts
  - src/components/worktree/HunkBar.tsx
  - src/components/worktree/HunkBar.test.tsx
  - src/stores/hunkStore.test.ts
autonomous: true
requirements: [WORK-03, WORK-05, WORK-06]
must_haves:
  truths:
    - "Người dùng bấm được nút stage/unstage/huỷ cho một khối cụ thể"
    - "Lỗi `file_changed` hiện nguyên văn câu 'hãy làm mới' kèm nút làm mới"
    - "Đường vào staging theo khối KHÔNG chết khi ChangeList hoặc DiffViewer hỏng"
  artifacts:
    - path: "src/stores/hunkStore.ts"
      provides: "Trạng thái chọn khối + gọi IPC, tách khỏi component"
    - path: "src/components/worktree/HunkBar.tsx"
      provides: "Thanh nút cho một khối"
  key_links:
    - from: "src/stores/hunkStore.ts"
      to: "stage_hunk / unstage_hunk / discard_hunk"
      via: "ipc"
      pattern: "stageHunk"
---

<objective>
Đưa staging theo khối tới người dùng: kiểu TypeScript, store, và thanh nút cho từng
khối — cộng thông báo WORK-05 đọc hiểu được.

Purpose: WORK-03, WORK-05, WORK-06 ở phía giao diện.

🔴 **Ràng buộc chi phối plan này** (CONTEXT.md mục 0, hệ quả 2): *"Tầng giao diện của
Phase 5 phải chịu được việc Phase 4 còn lỗi. Nếu `ChangeList` hoá ra hiển thị sai,
staging theo khối vẫn phải dùng được từ một đường vào khác."* Phase 4 chưa có bằng
chứng từ mắt người cho **bất kỳ** requirement nào, và wave 5 của nó tìm được một lỗi
khoá vòng bốn điều kiện mà không test nào thấy. Nên plan này **không** treo đường vào
duy nhất của mình lên `ChangeList`.

Output: store + component + test. **Chưa** nối vào `App.tsx` — đó là 05-05, nơi có mắt
người.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-hunk-staging/CONTEXT.md
@.planning/phases/05-hunk-staging/05-02-SUMMARY.md
@.planning/phases/05-hunk-staging/05-03-SUMMARY.md

@src/lib/ipc.ts
@src/stores/statusStore.ts
@src/components/worktree/ChangeList.tsx

<interfaces>
<!-- Hợp đồng đã có. Dùng thẳng, không đi dò codebase. -->

Từ src/lib/ipc.ts (đã có):
  export interface Hunk { oldStart: number; oldCount: number;
                          newStart: number; newCount: number;
                          heading: string; lines: DiffLine[] }
  export interface FileDiff { path: string; oldPath: string | null;
                              status: string; kind: DiffKind }
  export type DiffKind = { kind: 'text'; hunks: Hunk[]; truncated: boolean } | ...
  export interface GitErrorPayload { code: GitErrorCode; message: string;
                                     command: string | null }
  export function describeError(e: unknown): string
  export type GitErrorCode = ...   // 🔴 phải THÊM 'file_changed' vào union

Từ src/stores/statusStore.ts:
  useStatusStore — có `applyExternal(repoId, status)` để nhận RepoStatus mới.
  🔴 Đường stage/unstage ghi status TỪ GIÁ TRỊ TRẢ VỀ, **không** gọi `refresh` sau đó.
  Doc comment của store nói rõ vì sao, và có test ghim (đột biến M2 của 04-02).
  `stage_hunk`/`unstage_hunk`/`discard_hunk` cũng trả `RepoStatus` — theo đúng khuôn đó.

Từ 05-02 / 05-03 (command Tauri đã đăng ký):
  stage_hunk(repoId, path, hunkIndex, blobHash)   -> RepoStatus
  unstage_hunk(repoId, path, hunkIndex, blobHash) -> RepoStatus
  discard_hunk(repoId, path, hunkIndex, blobHash) -> RepoStatus
  discard_files(repoId, paths)                    -> RepoStatus
  list_trash(repoId)                              -> MucThungRac[]
  restore_trash(repoId, refName)                  -> RepoStatus
  GitError code mới: 'file_changed'
</interfaces>
</context>

<constraints>
🔴 **happy-dom không tính CSS layout và không có cuộn thật** (CONTEXT.md 4.5). Mọi
khẳng định về **bố cục** ở plan này là **có mã, chưa kiểm**. Không plan nào được ghi
"Đạt" cho tiêu chí hình ảnh dựa trên test happy-dom — SUMMARY phải nói thẳng điều đó.

🔴 **Không phụ thuộc `ChangeList` render đúng.** `hunkStore` là store độc lập; `HunkBar`
nhận mọi thứ qua props và không đọc `ChangeList`.
</constraints>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Kiểu IPC + `hunkStore`</name>
  <files>src/lib/ipc.ts, src/stores/hunkStore.ts, src/stores/hunkStore.test.ts</files>
  <behavior>
    `ipc.ts`: thêm `'file_changed'` vào union `GitErrorCode`; thêm sáu hàm bọc lời gọi
    và kiểu `MucThungRac`:

        export interface MucThungRac { refName: string; objectId: string;
                                       laBlob: boolean; paths: string[];
                                       luc: number; nhan: string }
        export type DongTu = 'huyBo' | 'xoa'

    🔴 Tên trường phải khớp **camelCase** mà Rust serialize ra. Lệch một bên **không gây
    lỗi biên dịch ở cả hai phía** — đúng lỗi `oldSize`/`old_size` đã cắn ở Phase 3
    (`domain/diff.rs` ghi lại). Cách bắt: test ở 05-03 đã liệt kê **toàn bộ** khoá JSON
    phía Rust; ở đây khẳng định danh sách khoá TypeScript **bằng nhau** với danh sách đó.

    `hunkStore.ts` (zustand), theo khuôn `statusStore`:

        interface HunkState {
          /** Khối đang chọn: `${path}#${index}`. `null` = chưa chọn. */
          dangChon: string | null
          /** Lỗi `file_changed` đang hiện, kèm đường dẫn để nút làm mới biết làm gì. */
          canLamMoi: { path: string; message: string } | null
          chon: (path: string, index: number) => void
          boChon: () => void
          stageHunk: (repoId, path, index, blobHash) => Promise<void>
          unstageHunk: (...) => Promise<void>
          discardHunk: (...) => Promise<void>
          xoaCanLamMoi: () => void
        }

    Test 1 — `stageHunk` gọi IPC **đúng một lần** với đúng tham số (mock `invoke`).

    Test 2 — `stageHunk` thành công ghi `RepoStatus` trả về vào `statusStore` qua
    `applyExternal`, và 🔴 **không** gọi `getStatus`. Khuôn của đột biến M2 ở 04-02.
    Khẳng định `getStatus` được gọi **0 lần**.

    Test 3 — 🔴 **ca `file_changed`.** IPC ném `{ code: 'file_changed', message: 'Tệp đã
    đổi từ lúc bạn xem khác biệt này, hãy làm mới' }`. Khẳng định:
      * `canLamMoi` khác `null` và `path` đúng;
      * `canLamMoi.message` **chứa nguyên văn** `'hãy làm mới'`;
      * `statusStore` **không** bị ghi (không có gì lọt vào).

    Test 4 — lỗi **khác** `file_changed` (ví dụ `index_locked`) **không** đặt
    `canLamMoi`. 🔴 Không có test này thì một cài đặt đặt `canLamMoi` cho **mọi** lỗi
    vẫn xanh ở Test 3 — lớp lỗi "fixture không phân biệt được" (#4).

    Test 5 — `chon`/`boChon` đổi `dangChon` đúng; chọn khối khác thay thế khối cũ chứ
    không tích luỹ.
  </behavior>
  <action>
    Viết `hunkStore.ts` theo `<behavior>`. Bọc IPC trong `src/lib/ipc.ts` cùng khuôn các
    lời gọi đã có ở đó.

    🔴 Phân nhánh lỗi bằng `code`, **không** bằng so khớp chuỗi `message` — `ipc.ts` đã
    có `isGitErrorPayload` cho đúng việc này, dùng lại. So chuỗi thông điệp sẽ vỡ ngay
    lần đầu ai đó sửa một dấu câu.

    🔴 `vitest` chạy: `rm -f .vitest/json/output.json` **trước** mỗi lần, đọc **sau** khi
    lệnh xong, đọc lại nếu số vô lý — `rtk` ghi đè tệp đó sau lệnh `--outputFile` của
    chính mình (CONTEXT.md mục 5).
  </action>
  <verify>
    <automated>rm -f .vitest/json/output.json &amp;&amp; npx vitest run src/stores/hunkStore.test.ts --reporter=json --outputFile=.vitest/json/hunk.json 2>&amp;1 | tail -5; node -e 'const r=require("./.vitest/json/hunk.json"); console.log("[total="+r.numTotalTests+" failed="+r.numFailedTests+"]"); if(!(r.numTotalTests>=5)) { console.error("🔴 cần >= 5 test, có "+r.numTotalTests+" — suite 0 test sẽ XANH nếu chỉ kiểm numFailedTests (lỗi #8)"); process.exit(1);} if(r.numFailedTests!==0) process.exit(1);'</automated>
  </verify>
  <done>
    `hunkStore` có >= 5 test xanh; Test 2 khẳng định `getStatus` gọi **0 lần**; Test 3 và
    Test 4 cùng tồn tại (phân biệt được `file_changed` với lỗi khác).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: `HunkBar` + đường vào không chết theo Phase 4</name>
  <files>src/components/worktree/HunkBar.tsx, src/components/worktree/HunkBar.test.tsx</files>
  <behavior>
    Component thuần props, một khối một thanh:

        interface HunkBarProps {
          repoId: string; path: string; index: number; blobHash: string
          staged: boolean          // quyết định nút là "Đưa vào" hay "Lấy ra"
          untracked: boolean       // 🔴 quyết định động từ huỷ: "Xoá" hay "Huỷ bỏ"
        }

    Test 1 — ba nút hiện ra với nhãn đúng; `staged=false` → `"Đưa khối vào vùng chờ"`,
    `staged=true` → `"Lấy khối khỏi vùng chờ"`. Khẳng định **cả hai** chiều.

    Test 2 — 🔴 **ROADMAP, không thương lượng.** `untracked=true` → nút huỷ ghi
    **`"Xoá"`**; `untracked=false` → **`"Huỷ bỏ"`**. Khẳng định **cả hai** nhánh, và
    khẳng định nhánh `untracked` **không** chứa chuỗi `"Huỷ bỏ"` (một nhãn `"Xoá / Huỷ
    bỏ"` sẽ qua được phép kiểm "có chứa Xoá").

    Test 3 — bấm nút gọi đúng hành động của `hunkStore` với đúng `index`. Dùng `index`
    khác 0 (ví dụ **1**) — `index: 0` không phân biệt được với một cài đặt hardcode `0`.
    Đây đúng lớp lỗi "hình dạng đúng, dữ liệu vô hại" (CONTEXT.md 4.2).

    Test 4 — 🔴 **ràng buộc chống-Phase-4.** Render `HunkBar` **một mình**, không có
    `ChangeList`, không có `DiffViewer`, không có provider nào của Phase 4. Khẳng định
    nút vẫn bấm được và vẫn gọi IPC. Khuôn chép từ
    `nut_stage_van_bam_duoc_khi_diff_hong` của 04-02 — cùng lớp ràng buộc, cùng cách
    ghim. **Đây là test quan trọng nhất của plan.**

    Test 5 — `canLamMoi` khác `null` → hiện thông báo chứa nguyên văn `"hãy làm mới"`
    kèm một nút làm mới bấm được. 🔴 Neo `waitFor`/truy vấn vào **phần tử chỉ có ở trạng
    thái này**, không vào phần tử có ở **mọi** trạng thái (lỗi #6 của CONTEXT.md 4.1).
    Dùng `data-testid="hunk-can-lam-moi"`.
  </behavior>
  <action>
    Viết component + test. Không `useVirtualizer` — số khối trong một tệp là hàng chục,
    và `CommitList` phải giữ **đúng một** lời gọi `useVirtualizer` trong toàn ứng dụng
    (bất biến WORK-11, lớp lỗi "lệch một hàng" đã xảy ra hai lần ở Phase 2).

    🔴 Thêm khối doc comment đầu tệp ghi rõ **bố cục chưa kiểm chứng** — happy-dom không
    tính CSS layout, nên "nút không đè lên nhau", "thanh không tràn khỏi khối" là **có
    mã, chưa kiểm** cho tới checkpoint 05-05. Khuôn chép từ `ChangeList.tsx` mục
    "⚠️ Bố cục CHƯA KIỂM CHỨNG".

    **Bảng đột biến:**

    | # | Đột biến | Đỏ mong đợi |
    |---|---|---|
    | M21 | Nhãn huỷ luôn là `"Huỷ bỏ"` | Test 2 |
    | M22 | Nhãn huỷ luôn là `"Xoá"` | Test 2 — 🔴 nếu **không** đỏ thì Test 2 chỉ kiểm một nhánh |
    | M23 | `onClick` truyền `0` thay vì `index` | Test 3 (vì Test 3 dùng index=1) |
    | M24 | Thông báo `file_changed` đổi `"hãy làm mới"` → `"thử lại"` | Test 5 |
    | M25 | `HunkBar` đọc một store của Phase 4 và `return null` khi chưa có | Test 4 |

    🔴 Trước khi kết luận một đột biến sống sót vì mã đúng, grep **toàn bộ**
    `src/**/*.test.*` xem có test nào hỏi về thứ đó không. **Rỗng nghĩa là cổng thiếu**
    — đó là lỗi #9, đắt nhất trong chín lỗi, và nó sống sót qua 666 test.
    Dùng công cụ Grep, **không** `grep` qua shell với mẫu có `\|` (rtk làm hỏng mẫu và
    trả `0 matches` cho mẫu thật sự khớp).

    🔴 Hoàn nguyên đột biến bằng `cp` từ scratchpad, **không** `git checkout --`.
  </action>
  <verify>
    <automated>rm -f .vitest/json/hunkbar.json &amp;&amp; npx vitest run src/components/worktree/HunkBar.test.tsx --reporter=json --outputFile=.vitest/json/hunkbar.json 2>&amp;1 | tail -5; node -e 'const r=require("./.vitest/json/hunkbar.json"); console.log("[total="+r.numTotalTests+" failed="+r.numFailedTests+"]"); if(!(r.numTotalTests>=5)){console.error("🔴 cần >= 5 test, có "+r.numTotalTests); process.exit(1);} if(r.numFailedTests!==0) process.exit(1);'</automated>
  </verify>
  <done>
    `HunkBar` có >= 5 test xanh. Test 4 (render một mình, không Phase 4) xanh và M25 **đã
    đo là đỏ** — đó là bằng chứng ràng buộc chống-Phase-4 được **cài**, không chỉ hứa.
    M21 và M22 **đều** đỏ.
  </done>
</task>

<task type="auto">
  <name>Task 3: Cổng toàn suite — không làm tụt mốc</name>
  <files>src/stores/hunkStore.test.ts, src/components/worktree/HunkBar.test.tsx</files>
  <action>
    Chạy **toàn bộ** suite frontend và khẳng định không tụt dưới mốc CONTEXT.md mục 6:
    **668 passed, 0 failed**. Plan này chỉ **thêm** test, nên con số phải **tăng**.

    🔴 `rm -f` tệp đầu ra trước mỗi lần chạy; `rtk` ghi đè `.vitest/json/output.json` sau
    lệnh `--outputFile` của chính mình. Dùng tên tệp **riêng** (`all05.json`) để tránh
    va chạm, và đọc **sau** khi lệnh xong.

    🔴 Cổng khẳng định `numTotalTests` **trước** khi tin `numFailedTests` — lỗi #8: một
    cổng chỉ kiểm `numFailedTests > 0` sẽ **XANH trên suite chạy 0 test**.

    Cũng chạy `npx tsc --noEmit` và đòi **sạch** (mốc CONTEXT.md mục 6).

    🔴 **Không** đưa `npm run lint` vào cổng — script đó **không tồn tại** trong
    `package.json` (đã kiểm). Một cổng gọi script không có sẽ thất bại vì lý do sai.
  </action>
  <verify>
    <automated>rm -f .vitest/json/all05.json &amp;&amp; npx vitest run --reporter=json --outputFile=.vitest/json/all05.json 2>&amp;1 | tail -5; node -e 'const r=require("./.vitest/json/all05.json"); console.log("[total="+r.numTotalTests+" passed="+(r.numTotalTests-r.numFailedTests)+" failed="+r.numFailedTests+"]"); if(!(r.numTotalTests>=668)){console.error("🔴 mốc là 668 test, đếm được "+r.numTotalTests+" — suite tụt hoặc chạy thiếu"); process.exit(1);} if(r.numFailedTests!==0) process.exit(1);' &amp;&amp; npx tsc --noEmit &amp;&amp; echo "[tsc sạch]"</automated>
  </verify>
  <done>
    Toàn suite >= **668** test (mốc cũ) + số test mới, **0 failed**. `npx tsc --noEmit`
    sạch. Cổng in cả `numTotalTests` lẫn `numFailedTests` để người đọc nhìn thấy số.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| người dùng → `HunkBar` | một cú bấm sinh một lần ghi vào index hoặc **xoá nội dung** |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-13 | Tampering | bấm huỷ nhầm | mitigate | Mọi đường huỷ đi qua `discard_hunk`, vốn tự lưu trước (ràng buộc kiểu `BienNhan` ở 05-03) |
| T-05-14 | Spoofing | nhãn nói sai việc sắp làm | mitigate | Động từ "Xoá" cho tệp chưa theo dõi, ghim bằng Test 2 + M21/M22 |
| T-05-15 | Tampering | `blobHash` cũ từ giao diện | mitigate | Rust kiểm lại hash trước khi áp (05-02); giao diện không phải chỗ tin cậy |
</threat_model>

<verification>
- `hunkStore.test.ts` >= 5 test, 0 failed.
- `HunkBar.test.tsx` >= 5 test, 0 failed; Test 4 render **không có** thành phần Phase 4.
- Toàn suite >= 668, 0 failed; `npx tsc --noEmit` sạch.
- M21–M25 có kết quả **đo được**; M22 và M25 là hai đột biến kiểm **chính test**.
- SUMMARY nói rõ: bố cục **chưa kiểm chứng** (happy-dom), chờ 05-05.
</verification>

<success_criteria>
Staging theo khối dùng được từ một component **không** phụ thuộc bất kỳ thành phần nào
của Phase 4, và điều đó được **ghim bằng test** chứ không bằng lời hứa. Thông báo
WORK-05 mang nguyên văn câu ROADMAP.
</success_criteria>

<output>
`.planning/phases/05-hunk-staging/05-04-SUMMARY.md`. Ghi số đo thật của toàn suite. 🔴
**Không** ghi "Đạt" cho bất kỳ tiêu chí hình ảnh nào — happy-dom không kiểm được chúng
(CONTEXT.md mục 0, hệ quả 3).
</output>
