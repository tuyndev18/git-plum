---
phase: 05-hunk-staging
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src-tauri/src/git/patch_build.rs
  - src-tauri/src/git/mod.rs
  - scripts/fixtures/make-hunk-fixtures.sh
  - src-tauri/src/testing/mod.rs
autonomous: true
requirements: [WORK-03, WORK-04]
must_haves:
  truths:
    - "Một bản vá chứa đúng một hunk được dựng từ byte thô của `git diff`, không qua String"
    - "Bản vá dựng ra được `git apply --check --cached --recount` chấp nhận trên git thật"
    - "Bỏ `--recount` làm một fixture cụ thể ĐỎ, đã đo chứ không chỉ định"
    - "Byte 0xE9, CRLF, và thiếu dòng trống cuối đi qua đường dựng bản vá không đổi"
  artifacts:
    - path: "src-tauri/src/git/patch_build.rs"
      provides: "Tách bản vá theo hunk trên Vec<u8>"
      contains: "pub fn tach_hunk_tho"
    - path: "scripts/fixtures/make-hunk-fixtures.sh"
      provides: "Repo mẫu hunk-cases: ba hunk, CRLF, không dòng cuối, byte Latin-1"
  key_links:
    - from: "src-tauri/src/git/patch_build.rs"
      to: "src-tauri/src/domain/diff.rs"
      via: "chỉ số hunk, KHÔNG dùng DiffLine.content"
      pattern: "tach_hunk_tho"
---

<objective>
Dựng tầng **byte thô** để tách một bản vá nhiều hunk thành bản vá chứa đúng một hunk,
cộng bộ repo mẫu `hunk-cases` mà mọi wave sau dùng chung.

Purpose: WORK-03 và WORK-04 đứng hoặc đổ ở tầng này. Đây là wave **đầu tiên** vì
`CONTEXT.md` mục 0 hệ quả 1 nói rõ: tầng Rust của Phase 5 phải tự đứng được, không phụ
thuộc giao diện Phase 4 (vốn chưa ai nhìn). Mọi thứ ở plan này kiểm được bằng `git`
thật trên repo tạm — **không** một dòng nào cần `ChangeList` render đúng.

Output: `patch_build.rs` + `make-hunk-fixtures.sh` + test đơn vị trên byte tự dựng.
Chưa có command Tauri nào ở plan này; nối vào IPC là việc của 05-02.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-hunk-staging/CONTEXT.md

@src-tauri/src/git/parsers/patch.rs
@src-tauri/src/git/exec.rs
@src-tauri/src/testing/mod.rs
@scripts/fixtures/make-status-fixtures.sh

<interfaces>
<!-- Hợp đồng đã có trong mã. Dùng thẳng, KHÔNG đi dò lại codebase. -->

Từ src-tauri/src/domain/diff.rs — `Hunk` đã tồn tại, giữ chỉ số dòng:
  pub struct Hunk { pub old_start: u32, pub old_count: u32,
                    pub new_start: u32, pub new_count: u32,
                    pub heading: String, pub lines: Vec<DiffLine> }

🔴 `DiffLine.content` là `String` giải mã **lossy** — doc comment của nó nói thẳng:
"Đường bản vá của Phase 5 không được dùng trường này". Plan này **không** đọc nó.
`Hunk` chỉ dùng để biết **có bao nhiêu hunk** và **thứ tự** của chúng.

Từ src-tauri/src/git/exec.rs — đã có sẵn, không sửa:
  pub struct GitOutput { pub stdout: Vec<u8>, pub stderr: Vec<u8>, pub status: i32 }
  impl GitCommand { pub fn stdin_bytes(self, data: Vec<u8>) -> Self }   // nhận Vec<u8>
`stdin_bytes` đã nhận `Vec<u8>` và doc comment của nó đã nói "Dùng cho `git apply
--cached -`" — đường byte thô đã được chuẩn bị từ Phase 1, không phải dựng mới.

Từ src-tauri/src/testing/mod.rs:
  pub fn fixture_root() -> PathBuf
  pub fn fixture_path(name: &str) -> Option<PathBuf>
  pub fn require_fixture(name: &str) -> Option<PathBuf>   // in nhắc rồi trả None
</interfaces>
</context>

<constraints>
🔴 **Bản vá không bao giờ đi qua `String`.** Chữ ký hàm nhận `&[u8]` và trả `Vec<u8>`.
Một `String::from_utf8` hay `to_string_lossy` trên đường bản vá là lỗi, kể cả khi test
xanh — có cổng grep ở Task 3.

🔴 **Chỉ `stdout`, không bao giờ ghép `stderr`.** `diff.rs` dòng 388 đã ghi chú T-03-15
đúng lớp lỗi này: một cảnh báo clean filter lọt vào stdout bị `parse_patch` đọc thành
dòng diff. Ở đây hậu quả nặng hơn: nó thành **nội dung tệp người dùng**.

🔴 Không tự spawn `git` — mọi lệnh đi qua `git/exec.rs` (bài học `GIT_EXTERNAL_DIFF`,
CONTEXT.md 4.4).
</constraints>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Repo mẫu `hunk-cases` — ba hunk, CRLF, không dòng cuối, byte Latin-1</name>
  <files>scripts/fixtures/make-hunk-fixtures.sh, src-tauri/src/testing/mod.rs</files>
  <behavior>
    Script sinh repo `hunk-cases` trong `fixture_root()`, mỗi tệp một hình dạng bệnh lý.
    Repo này để **bẩn có chủ ý** — giá trị nằm ở thư mục làm việc, như `status-cases`.

    Bốn tệp bắt buộc, mỗi tệp một lý do đã đo:

    | Tệp | Hình dạng | Vì sao |
    |---|---|---|
    | `ba-khoi.txt` | 26 dòng LF, sửa dòng 2, 13, 25 → **đúng 3 hunk** ở `-U3` | ca `--recount`, xem Task 2 |
    | `crlf-khong-dong-cuoi.txt` | 30 dòng **CRLF**, **không** `\n` cuối, dòng 6 chứa byte `0xE9`, sửa dòng 3/16/29 → 3 hunk | WORK-04, cả ba lớp cùng một tệp |
    | `nhi-phan.bin` | có byte NUL, đã theo dõi, đã sửa | R7 — phải từ chối rõ ràng |
    | `doi-ten-va-sua.txt` | `git mv` rồi sửa nội dung | ca ROADMAP "vừa đổi tên vừa sửa" |

    🔴 `core.autocrlf` đặt **tường minh** `true` trong repo mẫu này (`git config
    core.autocrlf true`). Mặc định trên Windows là `true` và đó là máy chủ dự án —
    fixture đặt `false` sẽ né mất chính ca R2 mà nó tồn tại để kiểm.

    🔴 Byte `0xE9` **không tạo được bằng shell** — `printf 'caf\351'` bị chuyển sang
    UTF-8 trước khi tới hệ thống tệp (đã đo, ghi trong `make-fixtures.sh` mục non-utf8).
    Dùng `node -e` với `Buffer.from(s,"binary")` (node có sẵn, 22.16) hoặc `printf` qua
    `perl -e 'print "\xe9"'`. Sau khi sinh, script **tự kiểm** bằng `od -c` rằng byte
    `351` có mặt và thoát khác 0 nếu không — một fixture sinh sai mà im lặng là đúng
    lớp lỗi #4 của CONTEXT.md 4.1.

    `testing/mod.rs`: thêm `pub fn require_hunk_fixture() -> Option<PathBuf>` theo đúng
    khuôn `require_status_fixture` đã có (in nhắc rồi trả `None`, **không** panic).
  </behavior>
  <action>
    Viết `scripts/fixtures/make-hunk-fixtures.sh` theo khuôn `make-status-fixtures.sh`
    (đọc nó trước — cùng cấu trúc `xoa_that`/`REPO=$DEST/repo`, cùng biến môi trường
    `GIT_AUTHOR_*` để repo tái lập được từng byte).

    Số dòng và vị trí sửa của `ba-khoi.txt` **đã được đo** cho đúng 3 hunk ở `-U3`:
    26 dòng, sửa dòng 2, 13, 25. Đừng đổi mà không đo lại — hai chỗ sửa cách nhau
    dưới 7 dòng sẽ **gộp thành một hunk** và fixture mất khả năng phân biệt.

    Cuối script in bảng tổng kết và số hunk đo được của từng tệp, để người chạy nhìn
    thấy fixture đúng hình dạng thay vì tin.

    Thêm `require_hunk_fixture` vào `testing/mod.rs` và thêm `"hunk-cases"` vào chỗ
    liệt kê fixture nếu có danh sách chung.
  </action>
  <verify>
    <automated>bash scripts/fixtures/make-hunk-fixtures.sh &amp;&amp; bash -c 'R=$(node -e "console.log(process.env.GIT_PLUM_FIXTURES||\"target/fixtures\")")/hunk-cases; cd "$R" || exit 1; H=$(git --no-pager diff --no-ext-diff -U3 -- ba-khoi.txt | grep -c "^@@"); echo "[ba-khoi hunks=$H]"; test "$H" -eq 3 || { echo "🔴 ba-khoi.txt phải có ĐÚNG 3 hunk, đo được $H"; exit 1; }; od -c crlf-khong-dong-cuoi.txt | grep -q "351" || { echo "🔴 thiếu byte 0xE9"; exit 1; }; od -c crlf-khong-dong-cuoi.txt | tail -2 | grep -q "\\\\r" || { echo "🔴 thiếu CRLF"; exit 1; }; echo OK'</automated>
  </verify>
  <done>
    `bash scripts/fixtures/make-hunk-fixtures.sh` chạy sạch; `ba-khoi.txt` cho **đúng 3**
    hunk đo bằng `grep -c '^@@'`; `crlf-khong-dong-cuoi.txt` có byte `351` và `\r` trong
    `od -c`, và **không** kết thúc bằng `\n`. Lệnh xác minh ở trên thoát 0.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: `tach_hunk_tho` — tách bản vá theo hunk trên Vec&lt;u8&gt;</name>
  <files>src-tauri/src/git/patch_build.rs, src-tauri/src/git/mod.rs</files>
  <behavior>
    `pub fn tach_hunk_tho(patch: &[u8]) -> Result<PatchTho, GitError>` chia byte thô của
    `git diff` thành phần đầu tệp và các hunk, **không giải mã**.

      pub struct PatchTho {
          /// Mọi dòng TRƯỚC `@@` đầu tiên: `diff --git`, `index`, `---`, `+++`,
          /// và cả `old mode`/`new mode`/`rename from`/`rename to` nếu có.
          pub dau: Vec<u8>,
          /// Thân từng hunk, KỂ CẢ dòng `@@` của chính nó.
          pub hunks: Vec<Vec<u8>>,
      }

      pub fn dung_ban_va_mot_hunk(p: &PatchTho, chi_so: usize) -> Option<Vec<u8>>

    Test 1 — `tach_hunk_tho` trên bản vá 3 hunk tự dựng cho `hunks.len() == 3`, và
    `dau` chứa `diff --git` nhưng **không** chứa `@@`.

    Test 2 — `dung_ban_va_mot_hunk(p, 1)` trả bản vá chỉ chứa hunk thứ **hai**; kết quả
    chứa đúng **một** dòng bắt đầu bằng `@@`.

    Test 3 — 🔴 **byte không UTF-8 đi qua nguyên vẹn.** Dựng bản vá chứa byte `0xE9` ở
    giữa một dòng nội dung, tách rồi dựng lại, khẳng định byte `0xE9` **vẫn còn** trong
    đầu ra. Một cài đặt lỡ đi qua `String::from_utf8_lossy` sẽ đổi nó thành `EF BF BD`
    (U+FFFD) và test này đỏ. **Đây là test quan trọng nhất của plan.**

    Test 4 — bản vá có `\ No newline at end of file`: dòng đó thuộc về hunk **chứa nó**,
    không bị rơi vào `dau` và không bị bỏ. Khẳng định chuỗi `\ No newline` có mặt trong
    `hunks` đúng một lần.

    Test 5 — bản vá **rỗng** (tệp không đổi) cho `hunks.len() == 0`, không panic.

    Test 6 — CRLF: dòng nội dung kết thúc `\r` (tệp CRLF chưa qua clean filter) không
    bị `trim` mất. Khẳng định số byte `\r` trong đầu ra bằng đầu vào.

    🔴 Tách theo **byte**, dùng `memchr` cho `\n` như `CLAUDE.md` chốt. Nhận biết hunk
    là dòng bắt đầu bằng `@@` **ở đầu dòng** — một dòng nội dung có thể bắt đầu bằng
    ` @@` (dấu cách ngữ cảnh) hoặc `+@@`, và cả hai **không** phải header.
  </behavior>
  <action>
    Tạo `src-tauri/src/git/patch_build.rs`, khai `pub mod patch_build;` trong
    `src-tauri/src/git/mod.rs`.

    Cài `tach_hunk_tho` bằng cách quét `\n` với `memchr::memchr_iter`, phân loại từng
    dòng theo byte đầu. Trạng thái: chưa gặp `@@` → đẩy vào `dau`; gặp `@@` → mở hunk
    mới; ngược lại → đẩy vào hunk đang mở.

    🔴 Giữ **nguyên** byte kết dòng: nếu dòng cuối của đầu vào không có `\n` thì đầu ra
    cũng không được thêm. `git apply` nhạy với điều này ở ca `\ No newline`.

    `dung_ban_va_mot_hunk` = `dau` nối với `hunks[chi_so]`, trả `None` khi chỉ số vượt
    biên (đầu vào đến từ webview — chỉ số sai là dữ liệu có thật, không phải giả định).

    Viết sáu test theo `<behavior>`. Byte đầu vào dựng bằng `b"..."` và
    `Vec::extend_from_slice`, **không** bằng `format!` — `format!` nhận `&str` và sẽ
    không biểu diễn được byte `0xE9`.
  </action>
  <verify>
    <automated>cd src-tauri &amp;&amp; cargo test --lib patch_build 2>&amp;1 | tee /tmp/pb.txt | tail -20 &amp;&amp; bash -c 'N=$(grep -oE "[0-9]+ passed" /tmp/pb.txt | head -1 | grep -oE "[0-9]+"); echo "[passed=$N]"; test -n "$N" || { echo "🔴 không đọc được số test — cổng không có tiền đề"; exit 1; }; test "$N" -ge 6 || { echo "🔴 phải có >= 6 test patch_build, đếm được $N"; exit 1; }'</automated>
  </verify>
  <done>
    `cargo test --lib patch_build` xanh với **>= 6** test; test byte `0xE9` có mặt và
    đỏ được khi đổi cài đặt sang `String::from_utf8_lossy` (kiểm ở Task 3).
    `cargo test --lib` tổng thể **>= 309** (mốc CONTEXT.md mục 6, không được tụt).
  </done>
</task>

<task type="auto">
  <name>Task 3: Cổng byte thô + bảng đột biến</name>
  <files>src-tauri/src/git/patch_build.rs</files>
  <action>
    Thêm một test **đọc mã nguồn** khẳng định đường bản vá không giải mã, theo đúng
    khuôn đã dùng ở `worktree.rs` (test `duong_ghi_di_qua_runner_write`).

    🔴 **Bỏ chú thích trước khi tìm** — quy tắc bắt buộc CONTEXT.md 4.1, lỗi #1 và #5.
    Tệp này có doc comment nói *về* `from_utf8_lossy`, nên grep trên nguồn thô sẽ khớp
    chính văn xuôi giải thích vì sao không được dùng nó:

        let src = include_str!("patch_build.rs");
        let than: String = src
            .lines()
            .filter(|l| {
                let t = l.trim_start();
                !t.starts_with("//") &amp;&amp; !t.starts_with("///") &amp;&amp; !t.starts_with("//!")
            })
            .collect::&lt;Vec&lt;_&gt;&gt;()
            .join("\n");
        // cắt bỏ `mod tests` — test tự nó có quyền nhắc tên hàm bị cấm
        let than = than.split("mod tests").next().unwrap();

    🔴 **Khẳng định tiền đề trước** — lỗi #3 (đường dẫn sai → grep rỗng → xanh):

        assert!(than.contains("pub fn tach_hunk_tho"),
            "🔴 tiền đề sai: không thấy `tach_hunk_tho` trong thân đã lọc — \
             cổng này đang tìm trong một tệp rỗng và sẽ XANH với mọi mã");

    Rồi mới khẳng định điều cần:

        for cam in ["from_utf8_lossy", "to_string_lossy", "String::from_utf8"] {
            assert!(!than.contains(cam),
                "🔴 `{cam}` trên đường bản vá — WORK-04 đổ ở đây, xem CONTEXT.md 2.1");
        }

    **Bảng đột biến** — chạy từng đột biến, ghi kết quả thật vào SUMMARY:

    | # | Đột biến | Đỏ mong đợi |
    |---|---|---|
    | M1 | `tach_hunk_tho` trả `Vec<u8>` qua `from_utf8_lossy(..).into_bytes()` | test byte `0xE9` (Task 2 Test 3) **và** cổng grep này |
    | M2 | Nhận biết hunk đổi từ `starts_with(b"@@")` sang `contains(b"@@")` | Test 1 — dòng nội dung chứa `@@` bị đọc thành header |
    | M3 | `dung_ban_va_mot_hunk` bỏ phần `dau` | Test 2 — bản vá không có `diff --git` |
    | M4 | Bỏ dòng `\ No newline` khi tách | Test 4 |
    | M5 | `trim_end()` mỗi dòng trước khi đẩy vào hunk | Test 6 (CRLF) |

    🔴 **Hoàn nguyên đột biến bằng cách chép tệp ra trước**, không bao giờ
    `git checkout -- src-tauri/src/git/patch_build.rs` — tệp này **chưa commit** trong
    lúc bạn làm việc và `checkout` sẽ xoá sạch (CONTEXT.md 4.1, đã xảy ra hôm nay):

        cp src-tauri/src/git/patch_build.rs "$SCRATCH/patch_build.bak"
        # ... áp đột biến, chạy test, ghi kết quả ...
        cp "$SCRATCH/patch_build.bak" src-tauri/src/git/patch_build.rs

    🔴 Nếu một đột biến cho **0 đỏ**: trước khi kết luận "mã đúng", kiểm xem có test nào
    **hỏi về thứ đó** không (lỗi #9). Grep `mod tests` cho tên thuộc tính/hàm liên quan
    — **rỗng nghĩa là cổng thiếu**, phải viết thêm test, không phải ghi "sống sót, chấp
    nhận được".
  </action>
  <verify>
    <automated>cd src-tauri &amp;&amp; cargo test --lib patch_build 2>&amp;1 | tail -5 &amp;&amp; cargo test --lib 2>&amp;1 | tee /tmp/all.txt | tail -3 &amp;&amp; bash -c 'N=$(grep -oE "[0-9]+ passed" /tmp/all.txt | head -1 | grep -oE "[0-9]+"); echo "[lib passed=$N]"; test -n "$N" || exit 1; test "$N" -ge 309 || { echo "🔴 cargo test --lib tụt dưới mốc 309"; exit 1; }'</automated>
  </verify>
  <done>
    Cổng grep xanh và **đã kiểm là đỏ được** bằng M1. Bảng đột biến có 5 hàng với kết
    quả **đo được** (không phải dự đoán) ghi trong SUMMARY. `cargo test --lib` >= 309.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| webview → command | `chi_so` hunk và đường dẫn tệp đến từ JavaScript, là số/chuỗi tuỳ ý |
| git stdout → bản vá | byte do git sinh, nhưng clean filter của người dùng ghi được vào stderr |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering | `dung_ban_va_mot_hunk` | mitigate | Chỉ số ngoài biên trả `None`, không `panic!` và không index trực tiếp `hunks[i]` |
| T-05-02 | Information disclosure | đường bản vá | mitigate | Chỉ đọc `stdout`; `stderr` không bao giờ được ghép vào (T-03-15) |
| T-05-03 | Denial of service | `tach_hunk_tho` | accept | `parse_patch` đã chặn `MAX_HUNKS=2000`/`MAX_DIFF_LINES=100_000` ở tầng trên; plan 05-02 dùng lại cổng đó |
</threat_model>

<verification>
- `bash scripts/fixtures/make-hunk-fixtures.sh` sinh `hunk-cases` với `ba-khoi.txt` cho
  **đúng 3** hunk (đo bằng `grep -c '^@@'`, in ra số).
- `cargo test --lib patch_build` >= 6 test, xanh.
- `cargo test --lib` >= **309** (mốc CONTEXT.md mục 6).
- Bảng đột biến 5 hàng, mỗi hàng có kết quả **đo được**.
- Không có `from_utf8_lossy`/`to_string_lossy`/`String::from_utf8` trong thân
  `patch_build.rs` **đã bỏ chú thích và đã cắt `mod tests`**.
</verification>

<success_criteria>
Tầng tách bản vá theo hunk chạy trên byte thô, có repo mẫu tái lập được, và có ít nhất
một đột biến **đã đo là đỏ** chứng minh cổng byte thô không phải trang trí. Không một
dòng nào của plan này phụ thuộc giao diện Phase 4.
</success_criteria>

<output>
Tạo `.planning/phases/05-hunk-staging/05-01-SUMMARY.md`. Ghi **số đo thật**: số hunk của
từng tệp fixture, số test, bảng đột biến với kết quả đo. Nếu một đột biến sống sót, ghi
rõ **đã kiểm có test nào hỏi về thứ đó chưa** (lỗi #9) — không ghi "chấp nhận được" mà
không có phép kiểm đó.
</output>
