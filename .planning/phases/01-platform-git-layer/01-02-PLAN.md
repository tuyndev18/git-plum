---
phase: 01-platform-git-layer
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - vite.config.ts
  - tsconfig.json
  - src/test/setup.ts
  - src/test/setup.test.tsx
  - src/lib/commands.test.ts
  - src/stores/repoStore.test.ts
autonomous: true
requirements: [PLAT-04, PLAT-05]

must_haves:
  truths:
    - "`npm test` chạy và báo cáo số test thật, không phải 'no test files found'"
    - "Sổ đăng ký lệnh có test chứng minh: trùng mã định danh bị từ chối, lệnh chưa đăng ký ném lỗi, lệnh bị vô hiệu hoá không chạy"
    - "repoStore có test chứng minh nhiều repository cùng tồn tại trong byRepo, đóng một cái không ảnh hưởng cái kia"
  artifacts:
    - path: "vite.config.ts"
      provides: "khối test của vitest + alias @ tường minh"
      contains: "happy-dom"
    - path: "src/lib/commands.test.ts"
      provides: "Test ràng buộc kiến trúc PLAT-04"
    - path: "src/stores/repoStore.test.ts"
      provides: "Test ràng buộc kiến trúc PLAT-05"
  key_links:
    - from: "vite.config.ts"
      to: "src/test/setup.ts"
      via: "test.setupFiles"
      pattern: "setupFiles"
    - from: "src/stores/repoStore.test.ts"
      to: "src/lib/ipc.ts"
      via: "vi.mock"
      pattern: "vi\\.mock"
---

<objective>
Đóng khoảng cách **G4**: dựng hạ tầng kiểm thử phía giao diện và viết test cho hai thứ
mang ràng buộc kiến trúc không sửa được về sau — sổ đăng ký lệnh (PLAT-04) và
`repoStore` (PLAT-05).

Purpose: `vitest` và `@testing-library/react` đã cài, `npm test` đã khai báo trong
`package.json`, nhưng **không có tệp cấu hình vitest nào và không có tệp test nào**.
Job `frontend` trong `.github/workflows/ci.yml` chạy `npm test` — hiện nó chạy vào
khoảng không. Hai ràng buộc PLAT-04 và PLAT-05 là loại "làm ngay hoặc không bao giờ";
không có test khoá chúng lại thì một lần tái cấu trúc vô ý ở Phase 2 có thể gỡ bỏ chúng
mà không ai biết.

Output: khối cấu hình `test` trong `vite.config.ts`, tệp setup, và hai bộ test chạy được.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-platform-git-layer/CONTEXT.md
@src/lib/commands.ts
@src/stores/repoStore.ts
@src/lib/ipc.ts
@vite.config.ts

<interfaces>
### `src/lib/commands.ts` — API cần kiểm thử (PLAT-04)

```typescript
export type CommandCategory = 'repo' | 'view' | 'history' | 'branch' | 'help'

export interface Command {
  id: string
  title: string
  category: CommandCategory
  keybinding?: string
  enabled?: () => boolean
  run: () => void | Promise<void>
}

export function registerCommand(command: Command): void   // ném lỗi nếu trùng id
export function registerCommands(commands: Command[]): void
export function unregisterCommand(id: string): void
export function clearCommands(): void
export function getCommand(id: string): Command | undefined
export function listCommands(): Command[]
export function listEnabledCommands(): Command[]
export async function runCommand(id: string): Promise<void>  // ném lỗi nếu chưa đăng ký
```

Sổ đăng ký là một `Map` **ở phạm vi module** — trạng thái dùng chung giữa các test.
Bắt buộc gọi `clearCommands()` trong `beforeEach`, nếu không test sẽ rò rỉ lẫn nhau.

### `src/stores/repoStore.ts` — API cần kiểm thử (PLAT-05)

```typescript
interface RepoSlice { info: RepoInfo; currentBranch: string | null; error: string | null }

interface RepoState {
  byRepo: Record<string, RepoSlice>
  activeRepoId: string | null
  isOpening: boolean
  openRepository: (path: string) => Promise<void>
  closeRepository: (id: string) => Promise<void>
  setActiveRepo: (id: string | null) => void
  refreshBranch: (id: string) => Promise<void>
  setError: (id: string, error: string | null) => void
}

export const useRepoStore = create<RepoState>(...)   // zustand 5
export function useActiveRepo(): RepoSlice | null
```

Store zustand truy cập ngoài React bằng `useRepoStore.getState()` và
`useRepoStore.setState(...)` — không cần render component để kiểm thử logic.

### `src/lib/ipc.ts` — bề mặt cần giả lập

```typescript
export interface RepoInfo { id: string; path: string; name: string }
export const ipc = {
  openRepository: (path: string) => Promise<RepoInfo>,
  listRepositories: () => Promise<RepoInfo[]>,
  closeRepository: (id: string) => Promise<void>,
  commandLog: () => Promise<CommandLogEntry[]>,
  clearCommandLog: () => Promise<void>,
  gitVersion: () => Promise<string>,
  currentBranch: () => Promise<string>,
}
export function isGitError(e: unknown): e is GitErrorPayload
export function describeError(e: unknown): string
```

`ipc.ts` import `invoke` từ `@tauri-apps/api/core`, thứ này **không chạy ngoài webview
Tauri**. Test phải `vi.mock('@/lib/ipc', ...)` để chặn ở ranh giới module — đây chính
là lý do `ipc.ts` tồn tại như một điểm thắt nút duy nhất (xem bình luận đầu tệp đó).
</interfaces>

<constraints>
Phiên bản đã chốt, không nâng, không hạ (`.planning/research/STACK.md`):
`vitest@5.0.1`, `@testing-library/react@16.3.3`, `happy-dom@20.14.5`, `vite@8.3.0`,
`typescript@6.0.3`, `zustand@5.0.15`.

Vite 8 dùng Rolldown, **không có esbuild**. Không đặt `minify: 'esbuild'`, không thêm
plugin nào phụ thuộc esbuild.

`tsconfig.json` hiện bật `noUnusedLocals`, `noUnusedParameters`,
`noUncheckedIndexedAccess`. Test phải qua được `npm run typecheck` với các cờ đó —
`noUncheckedIndexedAccess` nghĩa là `byRepo[id]` có kiểu `RepoSlice | undefined`,
phải xử lý tường minh.
</constraints>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Dựng cấu hình vitest và tệp setup</name>
  <files>vite.config.ts, tsconfig.json, src/test/setup.ts</files>
  <action>
    Hiện **không có tệp cấu hình vitest nào** trong dự án, và `vite.config.ts` không khai
    báo alias `@`. `npm test` vì thế chạy không có môi trường DOM và không giải được
    `@/lib/...`. Sửa cả hai trong một lần.

    **`vite.config.ts`** — giữ nguyên toàn bộ phần đang có (`plugins`, `clearScreen`,
    `server`, `envPrefix`, `build`), thêm hai thứ:

    1. Khối `resolve.alias` ánh xạ `@` sang thư mục `src`, dùng
       `fileURLToPath(new URL('./src', import.meta.url))` — **không** dùng `path.resolve`
       với `__dirname`, vì `vite.config.ts` là ESM (`"type": "module"` trong package.json)
       nên `__dirname` không tồn tại. Alias này phục vụ cả vitest lẫn `vite build`.
    2. Khối `test` của vitest:
       - `environment: 'happy-dom'`
       - `globals: true` (để `describe`/`it`/`expect` dùng được không cần import; vẫn nên
         import tường minh trong tệp test cho rõ ràng — `globals` chủ yếu để
         `@testing-library` dọn dẹp tự động hoạt động)
       - `setupFiles: ['./src/test/setup.ts']`
       - `include: ['src/**/*.{test,spec}.{ts,tsx}']`
       - `restoreMocks: true` — mỗi test bắt đầu với mock sạch
       Vitest 5 đọc khối `test` ngay trong `vite.config.ts`; `defineConfig` nhập từ
       `'vite'` không mang kiểu cho khối này, nên nhập `defineConfig` từ `'vitest/config'`
       thay vì `'vite'` để TypeScript không báo lỗi thuộc tính lạ.

    **`src/test/setup.ts`** — tệp mới. Nội dung:
    - `import '@testing-library/react'` không cần thiết; thay vào đó gọi `cleanup()` của
      `@testing-library/react` trong `afterEach` để component render giữa các test không
      rò rỉ. Với `globals: true` thì `afterEach` có sẵn ở phạm vi toàn cục, nhưng vẫn
      nhập tường minh từ `'vitest'` cho rõ.
    - Bình luận tiếng Việt ngắn nêu tệp này tồn tại vì sao.

    **`tsconfig.json`** — thêm `"src/test"` và các tệp `*.test.ts` vào phạm vi `include`
    nếu chúng chưa nằm trong (hiện `include: ["src"]` đã phủ hết, nên nhiều khả năng
    không cần đổi). Thứ **chắc chắn cần**: thêm `"vitest/globals"` vào mảng
    `compilerOptions.types` để `globals: true` không làm `tsc --noEmit` báo lỗi
    `describe is not defined`. Nếu `types` chưa tồn tại thì tạo mới:
    `"types": ["vitest/globals"]`. Cẩn thận: khai báo `types` sẽ **tắt** việc tự nạp
    mọi `@types/*` khác — nếu `npm run typecheck` sau đó báo thiếu kiểu React, thêm
    `"vite/client"` vào cùng mảng.

    **Cuối cùng, viết một test khói chứng minh môi trường thật sự chạy.** Tạo
    `src/test/setup.test.tsx` với đúng một `it`: render một phần tử tầm thường bằng
    `render()` của `@testing-library/react`, rồi khẳng định tìm thấy nó qua `screen`.

    Lý do bắt buộc có: `happy-dom` và `@testing-library/react` đã được cài và sắp được
    cấu hình, nhưng hai test ở nhiệm vụ 2 và 3 đều là logic thuần, **không render component
    nào**. Không có test khói này thì cấu hình DOM có sai cũng không ai biết cho tới tận
    Phase 2, lúc đó lẫn vào một đống thay đổi khác và khó lần ra.

    Test khói phải dùng cả `@/` trong một import để chứng minh `resolve.alias` hoạt động
    dưới vitest — đây chính là thứ `tsconfig` không bảo chứng được.
  </action>
  <verify>
    <automated>npm run typecheck &amp;&amp; npm run build &amp;&amp; node -e "const c=require('fs').readFileSync('vite.config.ts','utf8'); if(!/happy-dom/.test(c)) throw new Error('thiếu environment happy-dom'); if(!/setupFiles/.test(c)) throw new Error('thiếu setupFiles'); if(!/alias/.test(c)) throw new Error('thiếu alias @'); console.log('vite.config.ts OK')" &amp;&amp; test -f src/test/setup.ts &amp;&amp; npm test</automated>
  </verify>
  <done>
    `vite.config.ts` có khối `test` với `environment: 'happy-dom'`, `setupFiles`, và
    `resolve.alias` cho `@`. `src/test/setup.ts` tồn tại.

    Quan trọng nhất: **`npm test` phải thoát mã 0 với đúng 1 test đỗ.** Trước nhiệm vụ này
    `npm test` thoát mã 1 ("No test files found") — job `frontend` trong CI đang đỏ. Test
    khói ở `src/test/setup.test.tsx` vừa chấm dứt tình trạng đó vừa chứng minh `happy-dom`
    và `@/` alias thật sự hoạt động dưới vitest.

    `npm run typecheck` và `npm run build` vẫn xanh (không làm hỏng bản dựng đang chạy được).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Test sổ đăng ký lệnh — khoá ràng buộc PLAT-04</name>
  <files>src/lib/commands.test.ts</files>
  <behavior>
    - `registerCommand` rồi `getCommand(id)` trả về đúng lệnh đó.
    - Đăng ký hai lệnh trùng `id` → lần thứ hai **ném lỗi**, và thông điệp lỗi chứa `id`
      đó. (Ràng buộc: không im lặng ghi đè — ghi đè im lặng nghĩa là hai tính năng cùng
      giành một mã lệnh và cái thua biến mất không dấu vết.)
    - `runCommand('mã.không.tồn.tại')` → **reject**, thông điệp chứa mã đó.
      (Ràng buộc: lỗi lập trình phải lộ ra lúc phát triển, không phải nút bấm im lặng.)
    - `runCommand` với lệnh có `enabled: () => false` → hàm `run` **không** được gọi,
      và lời gọi **không** ném lỗi (đó là trạng thái hợp lệ, không phải lỗi).
    - `runCommand` với lệnh có `enabled: () => true` → `run` được gọi đúng một lần.
    - `runCommand` với `run` trả về Promise → `await runCommand(...)` chỉ hoàn tất sau khi
      promise đó giải quyết xong. (Ràng buộc: người gọi phải chờ được lệnh bất đồng bộ.)
    - `registerCommands([a, b])` → `listCommands()` có đủ cả hai.
    - `listEnabledCommands()` lọc bỏ lệnh có `enabled: () => false`, giữ lệnh không khai
      báo `enabled`.
    - `unregisterCommand(id)` → `getCommand(id)` trả `undefined`.
    - `clearCommands()` → `listCommands()` rỗng.
  </behavior>
  <action>
    Tạo `src/lib/commands.test.ts`.

    Bắt buộc `beforeEach(() => clearCommands())` — sổ đăng ký là `Map` ở phạm vi module,
    trạng thái dùng chung giữa các test. Bỏ bước này thì test đỗ hay trượt phụ thuộc thứ
    tự chạy.

    Dùng `vi.fn()` cho `run` để đếm số lần gọi. Với trường hợp bất đồng bộ, dùng một
    promise giải quyết thủ công (tạo `let resolve; const p = new Promise(r => resolve = r)`)
    để chứng minh `runCommand` thật sự `await`, chứ không chỉ gọi rồi bỏ đi.

    Với các test mong đợi lỗi, dùng `await expect(runCommand('x')).rejects.toThrow(/x/)`
    cho hàm bất đồng bộ và `expect(() => registerCommand(...)).toThrow()` cho hàm đồng bộ —
    `registerCommand` là đồng bộ, `runCommand` là bất đồng bộ; nhầm hai dạng này làm test
    đỗ giả.

    Viết mô tả `describe`/`it` bằng tiếng Việt. Mỗi `it` có một dòng bình luận nêu ràng
    buộc PLAT-04 mà nó khoá lại, để người xoá test sau này biết mình đang xoá cái gì.

    KHÔNG import `@tauri-apps/api` trong tệp này — `commands.ts` thuần tuý, không đụng IPC.
  </action>
  <verify>
    <automated>npm test -- src/lib/commands.test.ts &amp;&amp; npm run typecheck</automated>
  </verify>
  <done>
    Ít nhất 10 test đỗ trong `src/lib/commands.test.ts`. `npm run typecheck` xanh.
    Xoá thử phép kiểm tra trùng `id` trong `registerCommand` làm ít nhất một test đỏ.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Test repoStore — khoá ràng buộc PLAT-05</name>
  <files>src/stores/repoStore.test.ts</files>
  <behavior>
    - `openRepository(path)` → `byRepo` có một khoá đúng bằng `info.id` trả về từ IPC,
      và `activeRepoId` bằng chính id đó.
    - Mở **hai** repository khác nhau → `byRepo` có **hai** khoá cùng lúc, mỗi khoá giữ
      đúng `info` của nó. (Đây là ràng buộc cốt lõi của PLAT-05: trạng thái là map, không
      phải một biến phẳng. Test này là thứ ngăn ai đó rút gọn `byRepo` thành
      `activeRepo: RepoInfo | null` ở Phase 2.)
    - Đóng repo A khi có cả A và B → `byRepo` **còn** B nguyên vẹn, chỉ mất A.
    - Đóng repo đang hoạt động → `activeRepoId` thành `null`.
    - Đóng repo **không** đang hoạt động → `activeRepoId` **giữ nguyên**.
    - `openRepository` thất bại (IPC reject) → lời gọi ném lỗi ra ngoài, và `isOpening`
      trở lại `false` (không kẹt ở trạng thái "Đang mở…" vĩnh viễn).
    - `refreshBranch(id)` thành công → `byRepo[id].currentBranch` mang tên nhánh, `error`
      là `null`.
    - `refreshBranch(id)` thất bại → `currentBranch` thành `null` và **không** ném lỗi
      (repo chưa có commit nào là trạng thái bình thường, không phải sự cố).
    - `setError(id, 'thông điệp')` → chỉ slice của `id` đó mang lỗi, slice khác không đổi.
  </behavior>
  <action>
    Tạo `src/stores/repoStore.test.ts`.

    Giả lập toàn bộ module IPC ở đầu tệp:
    `vi.mock('@/lib/ipc', () => ({ ipc: { openRepository: vi.fn(), closeRepository: vi.fn(), currentBranch: vi.fn() } }))`
    — chỉ khai báo những hàm mà `repoStore` thật sự gọi. `vi.mock` được cẩu lên đầu tệp,
    nên không tham chiếu biến khai báo bên ngoài trong factory; lấy tham chiếu có kiểu
    bằng cách `import { ipc } from '@/lib/ipc'` rồi ép
    `vi.mocked(ipc.openRepository)` trong thân test.

    Reset trạng thái store trong `beforeEach`:
    `useRepoStore.setState({ byRepo: {}, activeRepoId: null, isOpening: false })`.
    Store zustand là singleton ở phạm vi module — bỏ bước này thì test rò rỉ lẫn nhau.
    Kèm `vi.clearAllMocks()`.

    Gọi hành động qua `useRepoStore.getState().openRepository(...)` và đọc kết quả qua
    `useRepoStore.getState().byRepo` — **không** render component. Logic store kiểm thử
    được độc lập với React; đưa `renderHook` vào đây chỉ thêm chỗ hỏng.

    Lưu ý `noUncheckedIndexedAccess`: `byRepo[id]` có kiểu `RepoSlice | undefined`.
    Dùng `expect(byRepo[id]).toBeDefined()` trước, rồi truy cập qua một biến cục bộ đã
    thu hẹp kiểu, hoặc dùng `expect(byRepo[id]?.info.name).toBe(...)`. Đừng dùng `!`
    để làm câm trình kiểm kiểu — đó chính là thứ cờ này tồn tại để chặn.

    Chú ý một hành vi có thật trong mã: `openRepository` gọi `refreshBranch` ngay sau khi
    thành công, và `refreshBranch` gọi `ipc.currentBranch()`. Mock `currentBranch` phải
    trả về một promise (ví dụ `mockResolvedValue('main')`) trong **mọi** test mở repo
    thành công, nếu không sẽ có unhandled rejection làm nhiễu kết quả.

    Viết mô tả bằng tiếng Việt, mỗi `it` kèm một dòng bình luận nêu ràng buộc PLAT-05
    mà nó khoá lại.
  </action>
  <verify>
    <automated>npm test -- src/stores/repoStore.test.ts &amp;&amp; npm run typecheck</automated>
  </verify>
  <done>
    Ít nhất 9 test đỗ trong `src/stores/repoStore.test.ts`. `npm run typecheck` xanh.
    `npm test` (không tham số) báo cáo tổng số test của cả hai tệp, không phải
    "no test files found".
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| webview (React) → Tauri IPC | Mọi lời gọi xuống Rust đi qua `src/lib/ipc.ts`; webview không tự dựng tham số tiến trình |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-06 | Tampering | ranh giới IPC | mitigate | Test giả lập ở mức module `@/lib/ipc`, không vá `invoke` toàn cục — giữ nguyên nguyên tắc một điểm thắt nút duy nhất để về sau kiểm toán được mọi lời gọi xuống Rust |
| T-01-07 | Elevation of Privilege | phụ thuộc test | accept | `vitest`, `happy-dom`, `@testing-library/react` là `devDependencies`, không vào gói phát hành; phiên bản đã ghim cứng trong `package.json` |
| T-01-08 | Repudiation | sổ đăng ký lệnh | mitigate | Test khoá quy tắc "trùng id thì ném lỗi" — hai tính năng không thể âm thầm giành cùng một mã lệnh |
</threat_model>

<verification>
```
npm test
npm run typecheck
npm run build
```
`npm test` phải báo ít nhất 19 test đỗ trên 2 tệp. Không còn "no test files found".
</verification>

<success_criteria>
- `npm test` chạy thật và báo cáo số test, job `frontend` trong `ci.yml` có việc để làm.
- Sổ đăng ký lệnh có test khoá ba ràng buộc PLAT-04: không ghi đè im lặng, lệnh lạ ném
  lỗi, lệnh bị vô hiệu hoá không chạy.
- `repoStore` có test khoá ràng buộc PLAT-05: hai repository cùng tồn tại trong `byRepo`,
  đóng một cái không đụng cái kia.
- `npm run typecheck` và `npm run build` vẫn xanh.
</success_criteria>

<output>
Sau khi hoàn thành, tạo `.planning/phases/01-platform-git-layer/01-02-SUMMARY.md`
</output>
