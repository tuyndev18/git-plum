//! Kiểu dữ liệu miền — struct thuần, không biết gì về git hay về Tauri.
//!
//! Tách riêng để kiểm thử được mà không cần repository thật và không cần webview.
//! Phase 1 chưa có kiểu nào ngoài `RepoInfo` (nằm trong `state`).
